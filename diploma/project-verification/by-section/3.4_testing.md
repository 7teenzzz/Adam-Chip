# Section 3.4 — Тестирование и апробация нейроагентной инсталляции

## 3.4.1 Задачи и методика

**Verification.** Apparatus:
- ✅ Event bus (`events.py`) + JSONL log = автоматическое логирование.
- ✅ `metrics.py` — latency per turn.
- ✅ `/api/agent/turns` API группирует события по turn_id.
- ✅ Log Viewer на порту 8083 (всегда доступен, даже при падении orchestrator).
- ✅ `scripts/adam_pull_logs.py` для удалённого pull.

**Status:** **FULL** — методика логирования полностью реализована.

## 3.4.2 Метрики удержания роли и нормативности

**Связь:** Crit 3 (FULL) + Crit 4 (FULL).

**Что измерять:**
- Persona consistency: % турнов, где TuningStore.config совпадает с persona files.
- Echo gate trigger rate: % турнов, где EchoGate отклонил повтор.
- Action layer reject rate: % LLM tags, отвергнутых whitelist.
- Drift detection: similarity между ответами в начале и конце сессии.

**Status:** **FULL** — все метрики достижимы через существующее логирование.

## 3.4.3 Метрики памяти и темпоральной связности

**Связь:** Crit 5 (FULL).

**Что измерять:**
- Retrieval relevance: % турнов, где episodic memory retrieve вернул >=1 relevant эпизод.
- Salience accuracy: совпадение salience scoring с manual annotation.
- Consolidation effectiveness: качество summaries (manual eval).
- Cross-session continuity: % реплик, ссылающихся на прошлые сессии.

**Status:** **FULL**.

## 3.4.4 Метрики интеракционности и инициативы

**Связь:** Crit 1 (PARTIAL) + Crit 6 (PARTIAL).

**Что измерять:**
- Average dialogue length (turns per session).
- Response latency (ASR end → TTS start).
- Wake word accuracy (true positive / false positive).
- Half-duplex violations: 0 expected (mute должен работать).
- Proactive engagement rate: % сессий с background activity.
- **Ограничение:** spontaneous reply rate ≈ 0 (proactive speech не реализован).

**Status:** **PARTIAL** — измеряется честно с честным признанием ограничений.

## 3.4.5 Интерпретация результатов и ограничения

**Связь:** Crit 8 (PARTIAL).

**Эмерджентные наблюдения** (см. `crit_08_emergence.md`):
1. Композиционный персонаж
2. Контекст-зависимая модуляция
3. Distributed body presence
4. Latency-driven personality

**Ограничения, которые надо отметить:**
- Нет proactive speech generator.
- Gemma 4 E4B SWA cache reset → ~9с full prefill каждый turn.
- LLM model mismatch с дипломным заявлением (Gemma вместо Cosmos).
- AIIM-формула не реализована буквально.
- Single-agent (no multi-agent emergence).

**Status:** **FULL** — интерпретация и ограничения честно фиксируются.
