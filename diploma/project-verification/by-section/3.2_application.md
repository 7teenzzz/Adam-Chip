# Section 3.2 — Разработка нейроагентного приложения

## 3.2.1 Общая архитектура

**Verification.** Модульная событийная архитектура.
- ✅ `Orchestrator.py` (85 edges) — центральный диспетчер.
- ✅ Blocks: Speech (ASR/TTS), Interlayers (prompt, action), Memory (episodic + memory), Tools (device, scene).
- ⚠️ Названия модулей в коде слегка отличаются: PromtBuilder.py (с опечаткой) → `prompt.py`; Commander.py → `action.py`; Communication.py → `device.py`.

**Status:** **FULL** (с переименованиями).

## 3.2.2 Программный стек

**Verification.**
- ✅ Jetson Orin NX 16GB, Ubuntu 22.04 + JetPack
- ✅ Containerized services (docker-compose + systemd units)
- ✅ llama.cpp как LLM engine
- ⚠️ **LLM model mismatch.** Диплом заявляет «Cosmos Reasoning 2 2B ~65 tok/s». Production использует `gemma-4-E4B-it-UD-Q4_K_XL` (Config.json) — другая модель.
- ✅ VLM в Docker (VILA 1.5-3B confirmed в Config.json)
- ✅ Python orchestration
- ⚠️ TUI.py — нет; вместо него FastAPI + web UI + Log Viewer (на порту 8083).

**Status:** **PARTIAL** (LLM model swapped, UI расширен).

## 3.2.3 Системный промпт и идентичность

**Verification.** См. `by-criterion/crit_03_identity.md`. AIIM-формула не реализована буквально, но иерархическая сборка промпта реализована полностью через `PromptBuilder`.

**Status:** **FULL** (functional eq).

## 3.2.4 Память и контекст

**Verification.** См. `by-criterion/crit_05_temporal.md`. Многоуровневая память реализована:
- Working: SessionAccumulator
- Summaries: data/adam/summaries/ (consolidator output)
- Notes: data/adam/notes/
- Bio: Agent Adam Chip/About/*.md
- SQLite: data/adam/memory.sqlite3
- JSONL: events.jsonl

**Status:** **FULL**.

## 3.2.5 Перцептивный и речевой контуры

**Verification.** См. `by-criterion/crit_06_interaction.md`. Full pipeline: WakeWord → VAD → WhisperX → LLM → Silero TTS.

**Status:** **FULL**.

## 3.2.6 Командный контур

**Verification.** ActionLayer (вместо Commander.py) парсит state markers, MCUClient (вместо Communication.py) отсылает на ESP32. Whitelist scenes из Config.json.

**Status:** **FULL** (с переименованиями).
