---
phase: 41-audio-pipeline-latency-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "System/adam/local_mic_reader.py"
  - "System/Config.json"
  - "System/Config.schema.json"
  - "System/Orchestrator.py"
autonomous: true
requirements: [APLF-02, APLF-03]
must_haves:
  truths:
    - "_find_pulse_source retries before returning None, so cold-start boot races resolve the WebCamera source instead of falling back to the default device"
    - "The uncommitted _raw_chunk_for_monitor change in Orchestrator.py is a deliberate, documented decision (kept), not stray uncommitted state"
  artifacts:
    - path: "System/adam/local_mic_reader.py"
      provides: "_find_pulse_source with retry-with-backoff"
      contains: "_find_pulse_source"
    - path: "System/Orchestrator.py"
      provides: "OWW fed raw pre-DSP audio via _raw_chunk_for_monitor"
      contains: "_ww_buf.append(_raw_chunk_for_monitor)"
  key_links:
    - from: "LocalMicReader.__init__"
      to: "_find_pulse_source(card_name)"
      via: "retry loop over pactl list short sources"
      pattern: "_find_pulse_source"
---

<objective>
Two independent code fixes that do not require a live mic and can run in parallel with Plan 41-01:

1. APLF-02: Add retry-with-backoff to `LocalMicReader._find_pulse_source`. It currently runs once at init and returned `None` on 3/7 cold starts today because PipeWire/USB was not yet enumerated at boot. Without `PULSE_SOURCE`, arecord binds to the default PipeWire source (possibly not WebCamera).

2. APLF-03: Finalize the uncommitted `_raw_chunk_for_monitor` decision in `Orchestrator.py`. The working diff already feeds OWW raw pre-DSP audio (`_ww_buf.append(_raw_chunk_for_monitor)`). Per 41-CONTEXT.md the recommendation is to KEEP this change (raw full-spectrum signal matches the model's training distribution), while explicitly correcting the inline comment — the original "220Hz HPF kills OWW score" hypothesis was WRONG (HPF was active on the working days 06-08/06-09).

Purpose: Eliminate the cold-start `pulse_source: None` race and convert a stray uncommitted change into a deliberate, documented decision.
Output: Patched `local_mic_reader.py` (+ Config keys) + finalized `Orchestrator.py` OWW-input decision with accurate inline rationale.

BRANCH NOTE: Current branch is `SmartFlora`. Do NOT switch branches. These code changes are scope-mismatched with flora and must land on a separate `audio-pipeline-fix` branch at `/commit-push` time (handled by orchestrator, not here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-audio-pipeline-latency-fix/41-CONTEXT.md
@System/adam/local_mic_reader.py

<interfaces>
From System/adam/local_mic_reader.py (current state — executor MUST read full file before editing):
- `LocalMicReader.__init__` calls `self._pulse_source = self._find_pulse_source(card_name)` (line ~101) when card_name is set.
- `_find_pulse_source(self, card_name: str) -> str | None` (lines 147-160): runs `subprocess.run(["pactl","list","short","sources"], ..., timeout=5)`, scans lines for one whose name (parts[1]) contains card_name (case-insensitive), returns it or None. On exception emits `local_mic_pulse_source_error`.
- `self._emit(event_type, payload)` (line 360) — use for diagnostics events. Config values come from the `audio_cfg` dict already passed to __init__ (no Settings import inside this class). `time`, `subprocess`, `os` already imported at module top.

From System/Orchestrator.py (OWW input, current uncommitted state):
- Line ~1267: `_raw_chunk_for_monitor = chunk` (captured BEFORE `chunk = self._input_dsp.process(chunk)`).
- Line ~1349: `self._ww_buf.append(_raw_chunk_for_monitor)` with a comment block (lines ~1344-1348) explaining OWW receives raw pre-DSP audio. This is the uncommitted change to finalize.
- LocalMicReader is constructed with the `media.audio` section dict as `audio_cfg` (same dict that already supplies sample_rate/frame_ms), so new media.audio keys are visible to it without code wiring changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add retry-with-backoff to LocalMicReader._find_pulse_source</name>
  <files>System/adam/local_mic_reader.py, System/Config.json, System/Config.schema.json</files>
  <read_first>
    - System/adam/local_mic_reader.py (FULL file — confirm current _find_pulse_source at lines 147-160 and the __init__ call site at line ~101; note this class reads config from the audio_cfg dict, NOT from Settings — Config-First values must be threaded through audio_cfg)
    - System/Config.json (media.audio section — where new keys go)
    - System/Config.schema.json (media.audio.properties — where new key descriptions go)
    - .planning/phases/41-audio-pipeline-latency-fix/41-CONTEXT.md (§"Побочная находка: _find_pulse_source race condition")
  </read_first>
  <behavior>
    - When `pactl list short sources` returns a matching WebCamera source on the first attempt, `_find_pulse_source` returns it immediately (no added delay on the happy path; subprocess.run called exactly once).
    - When the first attempt returns no match (cold-start boot race), the method retries up to N total attempts with a fixed sleep between attempts, returning the first match found.
    - When all attempts fail, the method returns None (current behavior preserved) and emits a diagnostic event noting attempts exhausted.
    - The retry count and delay are Config-driven (read from audio_cfg), not hardcoded literals in the loop body.
  </behavior>
  <action>
    Add two Config keys under `media.audio` in BOTH System/Config.json and System/Config.schema.json (English descriptions per project convention):
    - `pulse_source_retries` (integer, default 3) — number of attempts to resolve the WebCamera PulseAudio/PipeWire source at LocalMicReader start; covers the cold-start boot race where PipeWire/USB has not yet enumerated the source.
    - `pulse_source_retry_delay_sec` (number, default 1.0) — fixed delay in seconds between resolution attempts.

    In `LocalMicReader.__init__`, read both from `audio_cfg` into `self._pulse_source_retries = int(audio_cfg.get("pulse_source_retries", 3))` and `self._pulse_source_retry_delay_sec = float(audio_cfg.get("pulse_source_retry_delay_sec", 1.0))`. Read them BEFORE the `_find_pulse_source` call at line ~101.

    Refactor `_find_pulse_source(self, card_name)`: keep the existing single pactl scan + try/except (which emits `local_mic_pulse_source_error` per attempt) as the body of a loop `for attempt in range(self._pulse_source_retries):`. On a match, emit `local_mic_pulse_source_resolved` with `{"source": <name>, "attempt": attempt}` and return the name. On no match when `attempt < self._pulse_source_retries - 1`, call `time.sleep(self._pulse_source_retry_delay_sec)` and continue. After the loop with no match, emit `local_mic_pulse_source_unresolved` with `{"attempts": self._pulse_source_retries, "card_name": card_name}` and return None. Do NOT hardcode 3 or 1.0 inside the loop — use the instance attributes.

    Add `tests/test_local_mic_reader.py` (create if absent) with:
    - `test_find_pulse_source_retries`: monkeypatch `subprocess.run` to return a no-match result on the first call and a WebCamera-matching result on the second; construct LocalMicReader with audio_cfg overriding retries=3 and retry_delay_sec near 0 (monkeypatch time.sleep to no-op to keep the test fast); assert `_find_pulse_source("WebCamera")` returns the WebCamera source name.
    - `test_find_pulse_source_immediate`: subprocess.run returns a WebCamera match on the first call; assert the returned source is correct AND subprocess.run was called exactly once (no retry/sleep on happy path).
    Use a stub event_log object exposing `.append(type, payload)` for the LocalMicReader constructor.
  </action>
  <verify>
    <automated>cd /home/i17jet/Agents/Adam-Chip && PYTHONPATH=System ./.venv/bin/python -m pytest tests/test_local_mic_reader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_local_mic_reader.py::test_find_pulse_source_retries` passes (first call no-match, second match → returns WebCamera source).
    - `tests/test_local_mic_reader.py::test_find_pulse_source_immediate` passes and asserts subprocess.run called exactly once on the happy path.
    - `grep -n 'pulse_source_retries' System/Config.json System/Config.schema.json` shows the key under media.audio in both files; same for `pulse_source_retry_delay_sec`.
    - `grep -nE 'range\(3\)|sleep\(1\.0\)|sleep\(1\)' System/adam/local_mic_reader.py` returns no hardcoded retry/delay literals (values come from self._pulse_source_retries / self._pulse_source_retry_delay_sec).
  </acceptance_criteria>
  <done>_find_pulse_source retries Config-driven; unit tests green; new keys present in Config.json + Config.schema.json.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize the _raw_chunk_for_monitor OWW-input decision (keep, corrected rationale)</name>
  <files>System/Orchestrator.py</files>
  <read_first>
    - System/Orchestrator.py lines 1255-1390 (the _raw_chunk_for_monitor capture at ~1267, _input_dsp.process at ~1268, and the OWW input append at ~1349 with its comment block ~1344-1348)
    - .planning/phases/41-audio-pipeline-latency-fix/41-CONTEXT.md (§"Сегодняшний uncommitted фикс" and decisions §4 — KEEP recommendation + WRONG-hypothesis note)
  </read_first>
  <action>
    DECISION (per 41-CONTEXT.md §4): KEEP `self._ww_buf.append(_raw_chunk_for_monitor)`. Do NOT revert to `chunk`. Do NOT change any logic — comment text only.

    Rewrite the inline comment block at lines ~1344-1348. The current comment asserts "Post-DSP 220 Hz HPF removes the low-frequency fundamentals the model relies on → score drops to 0.001." This causal claim is WRONG: the 220Hz HPF was already active on the working days (06-08/06-09) when OWW scored 0.77, so HPF is NOT the cause of the score collapse (PipeWire burst-delivery is, fixed in Plan 41-01). Replace the comment with the actual rationale: OWW is fed raw pre-DSP `_raw_chunk_for_monitor` because OpenWakeWord was trained on full-spectrum audio, so raw input best matches its training distribution — a correctness/robustness choice, NOT the fix for the score-collapse regression (that root cause was PipeWire node latency, addressed in Plan 41-01). State that VAD/ASR continue to use the post-DSP `chunk`.

    Confirm `_raw_chunk_for_monitor` is still captured at ~1267 BEFORE `_input_dsp.process(chunk)` so it is genuinely pre-DSP. Do NOT add a Config flag — this is a fixed architectural decision, not a tunable parameter.
  </action>
  <verify>
    <automated>cd /home/i17jet/Agents/Adam-Chip && test "$(grep -c '_ww_buf.append(_raw_chunk_for_monitor)' System/Orchestrator.py)" -ge 1 && PYTHONPATH=System ./.venv/bin/python -c "import ast; ast.parse(open('System/Orchestrator.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_ww_buf.append(_raw_chunk_for_monitor)' System/Orchestrator.py` returns >= 1 (the decision line is kept, not reverted to `chunk`).
    - `_raw_chunk_for_monitor = chunk` still appears BEFORE the `_input_dsp.process(chunk)` line (capture is genuinely pre-DSP).
    - The inline comment at ~1344-1348 no longer claims the 220Hz HPF caused the score collapse; it states the training-distribution rationale and references PipeWire latency (Plan 41-01) as the actual root cause.
    - `python -c "import ast; ast.parse(...)"` confirms Orchestrator.py still parses (no syntax break).
  </acceptance_criteria>
  <done>OWW-input decision finalized: raw pre-DSP audio kept with corrected rationale; Orchestrator.py parses cleanly.</done>
</task>

</tasks>

<verification>
- `_find_pulse_source` retries up to `pulse_source_retries` times with `pulse_source_retry_delay_sec` spacing; unit tests pass.
- New Config keys present in both Config.json and Config.schema.json under media.audio.
- OWW input kept as raw pre-DSP `_raw_chunk_for_monitor`; comment rationale corrected; Orchestrator.py parses.
</verification>

<success_criteria>
Cold-start boot race for `PULSE_SOURCE` resolution is mitigated by retry-with-backoff (Config-driven, no hardcoded literals). The `_raw_chunk_for_monitor` OWW-input change is finalized as a deliberate, correctly-documented decision. Both fixes are independent of the live mic and committable alongside Plan 41-01 on the `audio-pipeline-fix` branch.
</success_criteria>

<output>
After completion, create `.planning/phases/41-audio-pipeline-latency-fix/41-02-SUMMARY.md` recording: the retry parameters chosen, test results, the Config keys added, and confirmation that the OWW-input decision (keep raw pre-DSP) was finalized with corrected rationale.
</output>
