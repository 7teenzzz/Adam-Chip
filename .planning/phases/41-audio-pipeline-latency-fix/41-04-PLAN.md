---
phase: 41-audio-pipeline-latency-fix
plan: 04
type: execute
wave: 3
depends_on: ["41-03"]
files_modified: []
autonomous: false
requirements: [APLF-04, APLF-05]
must_haves:
  truths:
    - "A full voice turn (wake word → ASR → LLM → TTS) completes end-to-end without timeout, at normal audio cadence"
    - "The complete pipeline confirms the latency fix holds under real dialogue load, not just isolated standby measurement"
  artifacts: []
  key_links:
    - from: "wake_word_detected"
      to: "adam_reply + tts playback"
      via: "single turn_id trace through asr → llm → tts"
      pattern: "turn_id"
---

<objective>
Integration verification: run a complete voice loop end-to-end at restored audio cadence and confirm the whole pipeline works — wake word "адам" → ASR transcription → LLM response → TTS playback — without timeouts or stretched timers, under real dialogue load.

Waves 1-2 fixed and calibrated the components in isolation (throughput, pulse_source, OWW threshold, timers). This wave proves they compose correctly in a live turn. It also catches any regression that only appears under load (e.g. cadence dropping during TTS half-duplex mute, or REPLY-window timing interacting badly with the restored cadence).

Purpose: Final goal-backward confirmation that the phase goal — "OWW detects 'адам' and the voice loop works like 06-08/06-09" — is met.
Output: A documented end-to-end turn trace proving the pipeline is healthy. No code/config changes expected (verify-only); any defect found is recorded as a gap for follow-up, not patched here.

PRECONDITION: Requires the orchestrator running with the full inference stack (LLM :8081, ASR :8095, TTS :8082) available, voice loop active, and the user present to speak. NOT autonomous. Depends on Plan 41-03 (calibrated threshold must be active).

BRANCH NOTE: Current branch is `SmartFlora`. This is a verification-only plan (no files_modified). No commit results from this plan directly; the phase's code/config commits from Plans 41-02/41-03 land on `audio-pipeline-fix` at `/commit-push` time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-audio-pipeline-latency-fix/41-CONTEXT.md

<interfaces>
From the running system (verification surface — do not modify):
- Each dialogue turn carries a `turn_id` linking asr → llm → tts → action events (see README "Логи Pipeline").
- `scripts/adam_pull_logs.py --last 5` groups recent turns by turn_id with per-stage latencies.
- API: `GET /api/agent/turns?limit=5` returns grouped turns; `GET /api/agent/events?turn_id=<id>` returns one turn's events.
- Key events in a healthy turn: `wake_word_detected` → `asr_result` (non-empty transcript) → `adam_reply` → TTS playback (output_target esp32_speaker or jetson_hdmi).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end voice turn — wake → ASR → LLM → TTS without timeout</name>
  <read_first>
    - .planning/phases/41-audio-pipeline-latency-fix/41-CONTEXT.md (phase goal + the full diagnostic chain this verifies)
    - README.md (§"Логи Pipeline" — adam_pull_logs.py and /api/agent/turns usage for reading the turn trace)
  </read_first>
  <what-built>
    Components were fixed and calibrated in Waves 1-2. This task runs the assembled pipeline live: the user says "адам, который час" (or any short question), and Claude confirms the turn completes through every stage by reading the turn_id trace. This is the goal-backward end state of the phase.
  </what-built>
  <action>
    Confirm the full stack is up: check service health (`curl --noproxy '*' -fsS http://127.0.0.1:8080/api/agent/status`) and that LLM/ASR/TTS endpoints respond (services :8081/:8095/:8082). Confirm the calibrated threshold from Plan 41-03 is active (read wake_word.threshold via /api/agent/gate or the engine state — it must NOT be 0.01).

    Run 2-3 complete turns: user says "адам" + a short question, waits for Adam's spoken reply. After each turn, pull the trace with `scripts/adam_pull_logs.py --last 3` or `GET /api/agent/turns?limit=3` and verify the turn_id chain contains, in order: `wake_word_detected` → non-empty `asr_result` transcript → `adam_reply` (LLM produced text) → TTS playback completion. Record per-stage latencies.

    Watch for the specific regressions this phase targets:
    - No `voice_loop_error` events with stage=esp32_mic / stage=asr caused by starved audio.
    - ASR transcript is non-empty and plausibly matches what was said (proves audio reaching ASR is continuous, not bursty fragments).
    - End-of-speech detection and TTS start happen at normal wall-clock (turn does not hang ~10s in endpointing).
    - During TTS playback (half_duplex_mute), audio cadence resumes normally after unmute (next "адам" still detected) — confirms the latency fix survives the mute/unmute cycle.

    This is verify-only. If a turn fails (timeout, empty ASR, no TTS), do NOT patch here — record the failing stage, the turn_id, and the relevant events as a gap in the SUMMARY for a follow-up gap-closure plan.
  </action>
  <how-to-verify>
    1. `GET /api/agent/status` healthy; LLM/ASR/TTS endpoints reachable; active wake_word.threshold != 0.01.
    2. For 2-3 live turns, the turn_id trace shows wake_word_detected → non-empty asr_result → adam_reply → TTS playback, in order.
    3. No starvation-related voice_loop_error (stage=esp32_mic/asr) during the turns.
    4. After TTS playback + unmute, a subsequent "адам" is still detected (cadence survives the mute cycle).
  </how-to-verify>
  <resume-signal>Type "pipeline ok" with the turn_ids and per-stage latencies, or "failed at <stage>" with the turn_id and event details.</resume-signal>
  <acceptance_criteria>
    - At least 2 of 3 live turns produce a complete turn_id trace: `wake_word_detected` → non-empty `asr_result` → `adam_reply` → TTS playback, confirmed via `scripts/adam_pull_logs.py --last 3` or `/api/agent/turns`.
    - Zero `voice_loop_error` events with stage in {esp32_mic, asr} attributable to audio starvation during the test turns.
    - After a TTS playback completes and mic unmutes, a follow-up "адам" produces a new `wake_word_detected` event (cadence survives half-duplex mute/unmute).
    - Per-stage latencies recorded in the SUMMARY for each successful turn (asr, llm, tts).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Full voice turn completes wake → ASR → LLM → TTS via a single turn_id trace, no timeouts.
- No audio-starvation voice_loop_error during live turns.
- Wake word still detected after TTS mute/unmute cycle — latency fix holds under load.
</verification>

<success_criteria>
The complete voice loop works end-to-end at restored ~50 Hz audio cadence, matching the 06-08/06-09 working state. The phase goal is met: "адам" reliably wakes Adam and a full dialogue turn completes without the burst-delivery pathology. Any residual defect is documented as a follow-up gap, not silently passed.
</success_criteria>

<output>
After completion, create `.planning/phases/41-audio-pipeline-latency-fix/41-04-SUMMARY.md` recording: the turn_ids and per-stage latencies of the verified turns, confirmation of no starvation errors, confirmation that wake detection survives the TTS mute cycle, and any gaps found (if the pipeline did not fully pass).
</output>
