---
phase: 35-live-integration-testing-ultimate-integration
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - .planning/phases/35-live-integration-testing-ultimate-integration/35-04-DEBUG-LOG.md
  - .planning/phases/35-live-integration-testing-ultimate-integration/35-GO-NO-GO.md
autonomous: false
requirements:
  - REQ-INT-VOICE-E2E
  - REQ-INT-FLORA-COEXIST
  - REQ-INT-SKILLS
  - REQ-INT-MEMORY
  - REQ-INT-CONFIG-LIVE
user_setup: []

must_haves:
  truths:
    - "Every defect from Waves 1-3 is triaged sequentially via /gsd-debug (one at a time)"
    - "Each fix is applied in the main dir (on ultimate-integration) and the failing turn is re-tested green"
    - "A go/no-go decision is produced citing each REQ-ID status"
    - "Push to origin/ultimate-integration happens ONLY on full green AND explicit user confirmation"
    - "Main-merge prerequisites (full green + ESP reflash per BRANCH cond 4) are documented"
  artifacts:
    - path: ".planning/phases/35-live-integration-testing-ultimate-integration/35-04-DEBUG-LOG.md"
      provides: "Per-defect debug record: symptom → root cause → fix → re-test result"
      min_lines: 10
    - path: ".planning/phases/35-live-integration-testing-ultimate-integration/35-GO-NO-GO.md"
      provides: "Final go/no-go decision with per-REQ status and push/merge gating"
      min_lines: 20
  key_links:
    - from: "Wave 1-3 defect list"
      to: "/gsd-debug"
      via: "sequential scientific-method debugging, persistent state"
      pattern: "gsd-debug"
    - from: "full green Waves 1-3"
      to: "git push origin ultimate-integration"
      via: "user-confirmed gate (D-04)"
      pattern: "push"
---

<objective>
Provide an explicit home for failures found in Waves 1-3: triage each defect sequentially through
/gsd-debug (scientific method, persistent state), apply the fix in the main dir on ultimate-integration,
re-test the exact failing turn until green, then produce a go/no-go decision and gate the irreversible
push to origin/ultimate-integration behind full green + explicit user confirmation.

Purpose: D-04 (fix-before-push, full green; each defect → /gsd-debug; push only after green Waves 1-3;
merge to main only after full green + ESP reflash). This plan runs PER-DEFECT during Waves 1-3 but is
captured as one plan so execution always has a place to route failures and record the decision.

Output: Per-defect debug log, final go/no-go decision, documented push gate + main-merge prerequisites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-live-integration-testing-ultimate-integration/35-CONTEXT.md
@.planning/phases/35-live-integration-testing-ultimate-integration/35-01-SUMMARY.md
@.planning/phases/35-live-integration-testing-ultimate-integration/35-02-SUMMARY.md
@.planning/phases/35-live-integration-testing-ultimate-integration/35-03-SUMMARY.md
@BRANCH.md
@CLAUDE.md
</context>

<live_state_facts>
- D-04 gate: push to origin/ultimate-integration ONLY after green Waves 1-3. Push is outward and
  irreversible → requires explicit user confirmation (do not push autonomously).
- Merge to main (BRANCH merge target) is NOT part of this phase — it additionally requires the ESP
  reflash under the flora firmware (BRANCH condition 4). Document this as a prerequisite, do not perform it.
- Debugging is sequential (one defect at a time) via /gsd-debug — scientific method, persistent state.
  Fixes land in the main dir which is on ultimate-integration; re-test the SAME failing turn.
- "Blocked on reflash" flora items (Wave 3) are NOT code defects — they go in the go/no-go as a
  known prerequisite for main-merge, not as a push blocker for origin/ultimate-integration.
- All localhost curl uses --noproxy '*'; commits/pushes via the project's git flow (Haiku commit-push
  sub-agent per CLAUDE.md) — but the actual `git push` to origin requires explicit user confirmation.
</live_state_facts>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Triage each Wave 1-3 defect via /gsd-debug and re-test</name>
  <read_first>
    - .planning/phases/35-live-integration-testing-ultimate-integration/35-01-EVIDENCE.md, 35-02-EVIDENCE.md, 35-03-EVIDENCE.md (defect lists)
    - .planning/phases/35-live-integration-testing-ultimate-integration/35-CONTEXT.md (D-04 debug loop)
    - CLAUDE.md (shared-infrastructure caution: Orchestrator.py / inference.py / flora.py changes affect the whole pipeline)
  </read_first>
  <decision>For each defect found in Waves 1-3, run /gsd-debug (sequentially), apply the fix in the main dir, and re-test the failing turn. If there are zero defects, skip to Task 2.</decision>
  <context>
    Gather every FAIL item from 35-01/02/03-EVIDENCE.md (broken trace stages, no ESP sound, barge-in
    not interrupting, «стоп» not cancelling, skill not short-circuiting, memory not writing, flora
    coexistence code defect). Exclude items marked "blocked on reflash" — those are not code defects.
    For each remaining defect, ONE AT A TIME:
      1. Run /gsd-debug with the symptom + the failing turn_id + the breaking stage from the trace.
      2. Apply the fix in the main dir (on ultimate-integration). If it touches Config.json, confirm
         it stays Config-First; if it touches Orchestrator.py/flora.py/inference.py, note the
         shared-infrastructure impact (CLAUDE.md trigger).
      3. Restart the affected service and re-run the EXACT failing turn (text turn for skills/memory;
         operator re-runs the voice/flora item for those). Confirm it is now green.
      4. Record symptom → root cause → fix → re-test result in 35-04-DEBUG-LOG.md.
    Defects are handled sequentially (persistent state, scientific method per D-04) — do not batch-fix.
  </context>
  <options>
    <option id="all-green">
      <name>All defects fixed and re-tested green (or zero defects)</name>
      <pros>Clears the path to the push gate (Task 2).</pros>
      <cons>None — this is the target state.</cons>
    </option>
    <option id="defect-remains">
      <name>A defect cannot be fixed within this phase</name>
      <pros>Honest no-go; avoids pushing broken integration.</pros>
      <cons>Push to origin/ultimate-integration is blocked until resolved.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Every non-"blocked-on-reflash" FAIL from Waves 1-3 has a 35-04-DEBUG-LOG.md entry: symptom → root cause → fix → re-test result
    - Each fixed defect's failing turn was re-tested and is now green (text turn re-run, or operator re-confirmation for voice/flora)
    - Defects were handled one at a time (the log shows sequential entries, not a single batch fix)
    - If any defect remains unfixed, it is explicitly recorded as a no-go blocker
  </acceptance_criteria>
  <resume-signal>Выберите: all-green (все дефекты починены / их не было) или defect-remains (остался блокер). Опишите кратко.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Produce the go/no-go decision</name>
  <read_first>
    - All five Wave evidence/summary files (35-01..35-03 EVIDENCE/SUMMARY, 35-04-DEBUG-LOG.md)
    - BRANCH.md (merge conditions 1-5; condition 4 = ESP reflash)
    - .planning/ROADMAP.md §Phase 35 (Requirements list)
  </read_first>
  <action>
    Write 35-GO-NO-GO.md with a per-REQ status table covering all five phase requirements:
    REQ-INT-CONFIG-LIVE (Wave 1), REQ-INT-VOICE-E2E (Wave 2), REQ-INT-FLORA-COEXIST /
    REQ-INT-SKILLS / REQ-INT-MEMORY (Wave 3). For each: PASS / FAIL / BLOCKED-ON-REFLASH, citing the
    evidence file + turn_id. State the overall decision:
      - GO for push to origin/ultimate-integration only if Waves 1-3 are fully green (flora may be
        BLOCKED-ON-REFLASH without blocking the push, since reflash is a main-merge prerequisite, not a
        push prerequisite — but say so explicitly).
      - NO-GO if any code defect remains unfixed (from Task 1 defect-remains).
    Document the main-merge prerequisites separately: full green AND ESP reflash under flora firmware
    (BRANCH condition 4) AND the other BRANCH conditions 1-3,5 status. Do NOT push and do NOT merge in
    this task — Task 3 gates the push behind user confirmation.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-live-integration-testing-ultimate-integration/35-GO-NO-GO.md && python3 -c "t=open('.planning/phases/35-live-integration-testing-ultimate-integration/35-GO-NO-GO.md',encoding='utf-8').read();reqs=['REQ-INT-CONFIG-LIVE','REQ-INT-VOICE-E2E','REQ-INT-FLORA-COEXIST','REQ-INT-SKILLS','REQ-INT-MEMORY'];missing=[r for r in reqs if r not in t];assert not missing, missing;assert 'GO' in t or 'NO-GO' in t;print('GO-NO-GO OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 35-GO-NO-GO.md has a per-REQ status row for ALL five phase requirements, each citing evidence + turn_id
    - An overall GO or NO-GO decision is stated, with flora BLOCKED-ON-REFLASH treated as a main-merge (not push) prerequisite
    - Main-merge prerequisites are documented separately: full green + ESP reflash (BRANCH cond 4) + BRANCH cond 1-3,5 status
    - No git push and no merge was performed in this task
  </acceptance_criteria>
  <done>Go/no-go decision written with per-REQ status and clearly separated push vs main-merge gating.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Gate the push to origin/ultimate-integration (user-confirmed)</name>
  <read_first>
    - .planning/phases/35-live-integration-testing-ultimate-integration/35-GO-NO-GO.md (the decision)
    - CLAUDE.md (commit-push flow via Haiku sub-agent; push outward = needs confirmation)
  </read_first>
  <what-built>
    The go/no-go decision is recorded in 35-GO-NO-GO.md. If the decision is GO, the ONLY remaining step
    is the irreversible push to origin/ultimate-integration, which per D-04 requires explicit user
    confirmation. The agent will NOT push autonomously.
  </what-built>
  <how-to-verify>
    Откройте 35-GO-NO-GO.md и проверьте решение:
    - Если решение NO-GO — ничего не пушим; подтвердите, что блокеры записаны и фаза остаётся открытой.
    - Если решение GO — подтвердите явно, что разрешаете `git push origin ultimate-integration`
      (необратимый шаг в общую ветку). Только после вашего «пушим» агент инициирует commit-push
      (через Haiku sub-agent, по флоу проекта). Мёрж в main НЕ выполняется — он требует reflash ESP
      (BRANCH условие 4) и отдельного решения.
  </how-to-verify>
  <acceptance_criteria>
    - If NO-GO: nothing was pushed; blockers recorded; phase left open
    - If GO: push to origin/ultimate-integration happened ONLY after explicit user confirmation, via the project commit-push flow
    - In all cases: NO merge to main was performed (it is gated on ESP reflash / BRANCH cond 4, out of scope)
  </acceptance_criteria>
  <resume-signal>Напишите «пушим» (разрешаю push на origin/ultimate-integration) или «не пушим» (NO-GO / отложить).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → origin (remote git) | Outward push to shared branch — gated behind explicit user confirmation (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06 | E (Elevation/irreversible action) | git push origin ultimate-integration | mitigate | Push is gated behind a human-action checkpoint requiring explicit confirmation; never autonomous (D-04). Merge to main further gated on BRANCH cond 4 (ESP reflash), out of scope. |
| T-35-07 | T (Tampering) | code fixes to shared infra (Orchestrator/flora/inference) | accept | Fixes are re-tested against the exact failing turn before any push; shared-infra impact is noted per CLAUDE.md trigger. |

This phase adds no new network attack surface. The only outward action is the git push, which is
human-gated.
</threat_model>

<verification>
- Every code defect from Waves 1-3 triaged via /gsd-debug, fixed, and re-tested green (or recorded as no-go).
- 35-GO-NO-GO.md states the decision with per-REQ status; flora-reflash items separated as main-merge prereqs.
- Push to origin happened only on GO + explicit user confirmation; no merge to main performed.
</verification>

<success_criteria>
- All five REQ-IDs have a documented status; go/no-go decision produced.
- Push gate respected (user-confirmed, never autonomous); main-merge prerequisites documented.
</success_criteria>

<output>
After completion, create `.planning/phases/35-live-integration-testing-ultimate-integration/35-04-SUMMARY.md`
</output>
