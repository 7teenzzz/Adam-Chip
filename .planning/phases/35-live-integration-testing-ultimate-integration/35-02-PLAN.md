---
phase: 35-live-integration-testing-ultimate-integration
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/35-live-integration-testing-ultimate-integration/35-02-VOICE-CHECKLIST.md
  - .planning/phases/35-live-integration-testing-ultimate-integration/35-02-EVIDENCE.md
autonomous: false
requirements:
  - REQ-INT-VOICE-E2E
user_setup: []

must_haves:
  truths:
    - "In exhibition mode the power-gate (MAXN + jetson_clocks) is satisfied at orchestrator start"
    - "Operator says «адам» + a command and the wake word activates the loop (oww→listening)"
    - "A live turn produces a trace oww→vad→asr→llm→tts→action for one turn_id"
    - "Operator confirms audible sound from the ESP speaker for that turn"
    - "Barge-in: operator speaking during TTS interrupts Adam and re-enters listening"
    - "Silence keyword «стоп» cancels the running turn and returns to standby"
  artifacts:
    - path: ".planning/phases/35-live-integration-testing-ultimate-integration/35-02-VOICE-CHECKLIST.md"
      provides: "Structured operator script with exact phrases, expected behavior, ok/fail boxes"
      min_lines: 30
    - path: ".planning/phases/35-live-integration-testing-ultimate-integration/35-02-EVIDENCE.md"
      provides: "Per-turn turn_id traces + operator sound confirmations"
      min_lines: 15
  key_links:
    - from: "OWW wake-word detection"
      to: "voice loop listening state"
      via: "wake_word_detected event → _set_voice_state listening"
      pattern: "wake_word_detected"
    - from: "TTS output"
      to: "ESP speaker (10.10.10.171)"
      via: "output_target=esp32_speaker, POST WAV"
      pattern: "esp32_speaker"
---

<objective>
Prove the integrated voice tract works end-to-end on real hardware in exhibition mode: real «адам»
wake word into the mic, real ASR→LLM→TTS, real sound out of the ESP speaker, plus barge-in and the
«стоп» silence keyword. This is the core acceptance signal of the phase and the direct restoration
check for the de-mojibaked wake word (on ult it previously read «Р°РґР°Рj» and could not be matched).

Purpose: REQ-INT-VOICE-E2E. The agent CANNOT speak into the mic or hear the ESP speaker — those
steps require the human operator. The agent prepares a precise checklist and collects the turn_id
trace + memory evidence for each operator-marked result.

Output: Operator voice checklist, per-turn traces, operator sound confirmations. Any fail feeds Wave 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-live-integration-testing-ultimate-integration/35-CONTEXT.md
@.planning/phases/35-live-integration-testing-ultimate-integration/35-01-SUMMARY.md
@BRANCH.md
@CLAUDE.md
</context>

<live_state_facts>
- Wave 1 left the main dir on ultimate-integration with the orchestrator restarted on integrated code.
- exhibition mode requires wake word «адам» before ASR activates AND enforces the power-gate
  (nvpmodel -m 0 + jetson_clocks), auto-checked at orchestrator start (project invariant). Run
  `scripts/adam_power_maxn.sh` before switching to exhibition.
- TTS output_target = esp32_speaker (no HDMI fallback) — sound comes from the ESP PCM5102A speaker.
- half_duplex_mute=true: mic is muted during TTS. Barge-in does NOT go through the main mic tract —
  it goes through a parallel OWW feed (`_local_barge_in_feed`), gated by wake_word.barge_in_debounce_hits.
- silence_keywords = [стоп, тихо, замолчи, хватит, молчи]; matched after wake-word stripping, so bare
  «стоп» in REPLY and «адам, стоп» both trigger → cancel LLM/TTS, return to standby.
- oww_score≈0.001 in standby is NORMAL idle floor (precedent: 0/1247 false positives) — not a defect.
- Evidence per D-03: turn_id trace via scripts/adam_pull_logs.py (oww→vad→asr→llm→tts→action) +
  operator verbal ok/fail for ESP sound. No audio recording.
- curl --noproxy '*' for all localhost; ESP Python HTTP via _NO_PROXY_OPENER (do not hand-roll urlopen).
</live_state_facts>

<tasks>

<task type="auto">
  <name>Task 1: Enter exhibition mode (power-gate) and author the operator voice checklist</name>
  <read_first>
    - scripts/adam_set_mode.sh, scripts/adam_power_maxn.sh (mode switch + power gate)
    - scripts/adam_pull_logs.py (turn_id trace stages: oww/vad/asr/llm/tts/action)
    - .planning/phases/35-live-integration-testing-ultimate-integration/35-CONTEXT.md (D-01, D-03, D-05)
    - System/Config.json (wake_words, silence_keywords, barge_in_debounce_hits)
  </read_first>
  <action>
    Set MAXN power first: `scripts/adam_power_maxn.sh`. Switch the agent to exhibition:
    `scripts/adam_set_mode.sh exhibition` then restart adam-orchestrator so the power-gate runs at
    start. Confirm the orchestrator came up in exhibition (the power-gate did NOT refuse start) via
    `/api/config` agent.mode == 'exhibition' and `/api/agent/status`. If the power-gate refuses
    start, record the failure and STOP — that is a Wave-4 defect (do not bypass the gate; it is a
    project invariant). Then author `35-02-VOICE-CHECKLIST.md`: a numbered operator script, each
    item with (a) exact phrase the operator must say, (b) expected observable behavior, (c) an
    ok/fail box, (d) the agent action on result. Cover at minimum:
      1. Wake + command: operator says «Адам, который час?» → expect Adam answers, sound from ESP speaker.
      2. Wake + open question: «Адам, расскажи о себе» → expect a spoken reply from ESP.
      3. Barge-in: «Адам, расскажи длинную историю» then, WHILE Adam is speaking, say «Адам» again →
         expect Adam stops mid-sentence and re-enters listening.
      4. Silence keyword: «Адам, ...» then say «стоп» → expect Adam cancels and goes quiet (standby).
    The checklist must instruct the operator to note the rough wall-clock time of each utterance so
    the agent can map it to the correct turn_id afterward. Do NOT attempt to speak into the mic
    yourself — that is the operator's job.
  </action>
  <verify>
    <automated>curl --noproxy '*' -fsS http://127.0.0.1:8080/api/config | python3 -c "import json,sys;d=json.load(sys.stdin);assert d['agent']['mode']=='exhibition', d['agent']['mode'];print('EXHIBITION OK')" && test -f .planning/phases/35-live-integration-testing-ultimate-integration/35-02-VOICE-CHECKLIST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `/api/config` agent.mode == 'exhibition' AND `/api/agent/status` returns 200 (power-gate did not refuse start)
    - 35-02-VOICE-CHECKLIST.md exists with ≥4 numbered items, each with exact phrase + expected behavior + ok/fail box
    - The checklist explicitly covers wake-word turn, barge-in, and «стоп» silence keyword
    - If power-gate refused: failure recorded as a Wave-4 defect and NO gate bypass was performed
  </acceptance_criteria>
  <done>Exhibition mode active with power-gate satisfied; operator checklist authored and ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the live voice checklist</name>
  <read_first>
    - .planning/phases/35-live-integration-testing-ultimate-integration/35-02-VOICE-CHECKLIST.md (the script you just authored)
  </read_first>
  <what-built>
    Exhibition-mode integrated orchestrator with «адам» wake gate, ESP-speaker TTS output, barge-in,
    and «стоп» silence keyword. A structured operator checklist is ready at 35-02-VOICE-CHECKLIST.md.
  </what-built>
  <how-to-verify>
    Стоя у установки, пройдите по чек-листу 35-02-VOICE-CHECKLIST.md по порядку:
    1. Скажите «Адам, который час?» — Адам должен ответить, звук должен идти ИЗ ESP-динамика. Отметьте ok/fail и слышен ли звук.
    2. Скажите «Адам, расскажи о себе» — ожидается голосовой ответ из ESP. Отметьте ok/fail.
    3. Скажите «Адам, расскажи длинную историю», и ПОКА Адам говорит — скажите «Адам» ещё раз. Адам должен прерваться и снова начать слушать. Отметьте ok/fail.
    4. Скажите «Адам, ...» затем «стоп» — Адам должен замолчать и уйти в standby. Отметьте ok/fail.
    Для каждой реплики запишите примерное время произнесения. Отметьте в чек-листе ok/fail и
    подтверждение звука из динамика устно (ok/нет).
  </how-to-verify>
  <acceptance_criteria>
    - Operator has marked ok/fail for all 4 checklist items
    - For each item the operator recorded whether sound was audible from the ESP speaker (ok/нет)
    - Approximate utterance times noted so the agent can map them to turn_ids
  </acceptance_criteria>
  <resume-signal>Напишите «готово» когда пройдёте все 4 пункта (или опишите, какие провалились).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Collect per-turn traces and assemble voice E2E evidence</name>
  <read_first>
    - scripts/adam_pull_logs.py (--last N, --stage; turn_id traces)
    - .planning/phases/35-live-integration-testing-ultimate-integration/35-02-VOICE-CHECKLIST.md (operator marks + times)
    - System/adam/memory.py (dialogue_turns increments per turn)
  </read_first>
  <action>
    For each operator turn, pull the trace via `scripts/adam_pull_logs.py --last N` (N large enough to
    cover all checklist turns) or per-turn via `/api/agent/turns?limit=N` + `/api/agent/events?turn_id=…`.
    Map each operator-noted utterance time to its turn_id. For each passing voice turn, confirm the
    trace contains the full chain oww→vad→asr→llm→tts→action (asr_result with the recognized text,
    llm_thinking, tts_started/tts_finished, adam_reply). For barge-in, confirm a second
    wake_word_detected during TTS and a voice_state transition back to listening. For «стоп», confirm
    the turn was cancelled (no completed adam_reply for that utterance / standby transition).
    Confirm dialogue_turns in memory.sqlite3 grew across the voice turns. Write 35-02-EVIDENCE.md
    pairing each checklist item → turn_id → trace summary → operator sound confirmation. Any item the
    operator marked fail (or where the trace is broken) → list explicitly as a defect for Wave 4 with
    its turn_id and the stage where the chain broke.
  </action>
  <verify>
    <automated>scripts/adam_pull_logs.py --last 4 >/dev/null 2>&1 && test -f .planning/phases/35-live-integration-testing-ultimate-integration/35-02-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 35-02-EVIDENCE.md maps each checklist item to a turn_id and a trace summary
    - For every PASSED wake turn, the trace shows oww→vad→asr→llm→tts→action (asr_result text + tts_started/finished + adam_reply present)
    - Barge-in evidence shows a second wake_word_detected during TTS + transition to listening
    - «стоп» evidence shows the turn cancelled / standby transition (no full adam_reply for that utterance)
    - Any operator-fail or broken-chain item is listed as a Wave-4 defect with turn_id + breaking stage
  </acceptance_criteria>
  <done>Voice E2E evidence assembled per turn_id; passes recorded, fails routed to Wave 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator voice → mic (ESP INMP441) | Acoustic input — existing tract, unchanged this phase |
| Jetson → ESP speaker (LAN) | Existing audio plane to 10.10.10.171 — unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03 | D (DoS) | exhibition power-gate | accept | Gate is a safeguard, not a vuln; if it refuses start that is the intended protection (logged as defect, not bypassed). |

No new network endpoints or inputs are introduced. The voice tract, ESP audio plane, and wake gate
all pre-exist. No new attack surface.
</threat_model>

<verification>
- Exhibition mode reached with power-gate satisfied.
- Operator confirmed wake + spoken reply from ESP for ≥1 turn; barge-in and «стоп» behaviors observed.
- turn_id traces show full oww→…→action chain for passing turns; fails routed to Wave 4.
</verification>

<success_criteria>
- REQ-INT-VOICE-E2E demonstrated live (wake → ASR → LLM → TTS → ESP sound), with barge-in and «стоп».
- Each turn has a turn_id trace + operator sound confirmation in 35-02-EVIDENCE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/35-live-integration-testing-ultimate-integration/35-02-SUMMARY.md`
</output>
