---
phase: 35-live-integration-testing-ultimate-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-live-integration-testing-ultimate-integration/35-01-EVIDENCE.md
autonomous: true
requirements:
  - REQ-INT-CONFIG-LIVE
user_setup: []

must_haves:
  truths:
    - "Main dir /home/i17jet/Agents/Adam-Chip is checked out on ultimate-integration (15d23ca)"
    - "Worktree /tmp/adam-ult is removed so the branch is held in exactly one place"
    - "adam-orchestrator (and TTS/ASR if needed) restarted and loading integrated code"
    - "Live /api/config shows flora block, skills block, wake_words=«адам», tts output_target=esp32_speaker"
    - "Maintenance-mode text /api/agent/turn returns a reply through LLM and adds dialogue_turns rows"
  artifacts:
    - path: ".planning/phases/35-live-integration-testing-ultimate-integration/35-01-EVIDENCE.md"
      provides: "Recorded command outputs proving bring-up + smoke green"
      min_lines: 20
  key_links:
    - from: "systemd adam-orchestrator.service"
      to: "/home/i17jet/Agents/Adam-Chip (main dir)"
      via: "WorkingDirectory loads code+Config only from main dir"
      pattern: "ultimate-integration"
    - from: "/api/agent/turn"
      to: "data/adam/memory.sqlite3 dialogue_turns"
      via: "memory.add_dialogue('viewer'|'adam', ...)"
      pattern: "dialogue_turns"
---

<objective>
Bring the integrated branch `ultimate-integration` live in the main repo dir, restart the
orchestrator so systemd loads the integrated code+config, and prove on real hardware that the
de-mojibaked config is valid and a text dialogue turn completes end-to-end (LLM → reply → memory).

Purpose: Separate "integrated code did not come up / config is broken" defects from
"wake word / sound" defects (D-01 two-stage rationale). This is the foundation wave — Waves 2-3
depend on the live orchestrator actually running the integrated code with flora+skills enabled.

Output: Branch switched in main dir, worktree removed, orchestrator restarted, config validated
live, maintenance text-turn smoke green. Evidence recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-live-integration-testing-ultimate-integration/35-CONTEXT.md
@.planning/STATE.md
@BRANCH.md
@CLAUDE.md
</context>

<live_state_facts>
<!-- Authoritative snapshot 2026-06-09, established during discuss — do NOT re-verify from zero. -->
- Network: eno1 UP, Jetson = 10.10.10.1/24, ESP 10.10.10.171 pings + :80/api/status OK.
- Services healthy: llama :8081, Silero TTS :8082, WhisperX ASR :8095, orchestrator :8080, logviewer :8083. Ollama absent.
- BLOCKER: the LIVE orchestrator runs from main dir which is on branch voice-loop-recovery →
  loaded config has flora:False, skills:False. The integrated branch lives in worktree /tmp/adam-ult
  (15d23ca, NOT pushed). systemd loads code+config ONLY from the main dir. This wave fixes that.
- Integrated Config.json (verified at plan time in /tmp/adam-ult): wake_words='адам',
  flora.enabled=True, skills.weather.enabled=True, skills.jokes.enabled=True, tts output_target='esp32_speaker'.
- No sqlite3 CLI binary on host — use `python3 -c "import sqlite3; ..."` for memory checks.
- events.jsonl is ~1.6 GB — prefer API (/api/agent/turns, /api/agent/events) and adam_pull_logs.py over raw file reads.
- All localhost/ESP curl MUST use `--noproxy '*'` (v2ray on :10808 hijacks localhost).
</live_state_facts>

<tasks>

<task type="auto">
  <name>Task 1: Switch main dir to ultimate-integration and remove worktree</name>
  <read_first>
    - BRANCH.md (merge conditions; current branch = voice-loop-recovery to return to after phase)
    - .planning/phases/35-live-integration-testing-ultimate-integration/35-CONTEXT.md (D-02 service source decision)
    - CLAUDE.md (Repository Cleanliness — what may be committed)
  </read_first>
  <action>
    Record the current branch first: `git -C /home/i17jet/Agents/Adam-Chip branch --show-current`
    (expected `voice-loop-recovery`) — write it into 35-01-EVIDENCE.md as the return branch.
    There is uncommitted work in the main dir (System/Config.json, System/Orchestrator.py per
    git status). Per D-02 the main dir must be clean before checkout. Inspect `git -C ... status`
    and `git -C ... diff --stat`. If the diff is only the voice-loop-recovery WIP already
    captured in commits, stash it: `git -C /home/i17jet/Agents/Adam-Chip stash push -u -m
    "phase35-pre-checkout voice-loop-recovery WIP"` and record the stash ref in EVIDENCE.
    Then remove the worktree so the branch is free: `git -C /home/i17jet/Agents/Adam-Chip
    worktree remove /tmp/adam-ult` (use `--force` only if it complains about the modified
    Config.json IN the worktree, and record why). Then check out the integrated branch in the
    main dir: `git -C /home/i17jet/Agents/Adam-Chip checkout ultimate-integration`.
    Do NOT push anything. Do NOT merge to main. This is a local checkout only.
  </action>
  <verify>
    <automated>git -C /home/i17jet/Agents/Adam-Chip branch --show-current | grep -qx ultimate-integration && git -C /home/i17jet/Agents/Adam-Chip worktree list | grep -qv /tmp/adam-ult && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/i17jet/Agents/Adam-Chip branch --show-current` outputs exactly `ultimate-integration`
    - `git -C /home/i17jet/Agents/Adam-Chip worktree list` does NOT list `/tmp/adam-ult`
    - `git -C /home/i17jet/Agents/Adam-Chip log --oneline -1` shows commit starting with `15d23ca`
    - 35-01-EVIDENCE.md records the return branch (`voice-loop-recovery`) and the stash ref (if created)
    - No `git push` and no `git merge` was executed (verify: `git -C ... reflog -5` shows only checkout/stash/worktree ops)
  </acceptance_criteria>
  <done>Main dir is on ultimate-integration at 15d23ca, worktree removed, return state recorded, nothing pushed.</done>
</task>

<task type="auto">
  <name>Task 2: Restart orchestrator on integrated code and run healthcheck</name>
  <read_first>
    - scripts/adam_healthcheck.sh, scripts/adam_service_status.sh (live-service probes)
    - README.md §Production Boot (service start order)
    - CLAUDE.md (NOPASSWD on adam-*.service; curl --noproxy '*' invariant)
  </read_first>
  <action>
    Restart the orchestrator so systemd reloads the now-integrated code+Config from the main dir:
    `sudo systemctl restart adam-orchestrator.service` (NOPASSWD is configured per project memory).
    TTS/ASR/LLM are already healthy and their code did not change in a way that requires restart;
    restart adam-tts-silero.service / adam-asr-whisperx.service ONLY if their /health probe fails
    after the orchestrator restart — record the decision in EVIDENCE either way. Wait for the
    orchestrator to come up (poll `/api/agent/status` until HTTP 200, ~10-20s — Gemma prefill is slow).
    Run `scripts/adam_service_status.sh` and `scripts/adam_healthcheck.sh`; capture their output into
    35-01-EVIDENCE.md. Confirm Ollama is still absent (`which ollama` empty).
  </action>
  <verify>
    <automated>curl --noproxy '*' -fsS http://127.0.0.1:8080/api/agent/status >/dev/null && which ollama; test $? -ne 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `curl --noproxy '*' -fsS http://127.0.0.1:8080/api/agent/status` returns HTTP 200 (exits 0)
    - `scripts/adam_service_status.sh` shows adam-orchestrator active/running; llama:8081, TTS:8082, ASR:8095 reachable
    - `which ollama` finds nothing (exit non-zero)
    - 35-01-EVIDENCE.md contains the healthcheck + service-status output and the TTS/ASR restart decision
  </acceptance_criteria>
  <done>Orchestrator restarted on integrated code, all dependent services healthy, Ollama absent, evidence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Validate live config + maintenance text-turn smoke</name>
  <read_first>
    - System/Config.json (integrated: flora, skills, wake_words=«адам», tts output_target)
    - System/adam/memory.py (dialogue_turns table — add_dialogue increments per turn)
    - .planning/phases/35-live-integration-testing-ultimate-integration/35-CONTEXT.md (D-03 evidence per turn)
  </read_first>
  <action>
    Confirm the orchestrator is in maintenance mode (wake gate bypassed, no power-gate) —
    `scripts/adam_set_mode.sh maintenance` if needed, then restart per Task 2 only if the mode
    actually changed. Validate the LIVE config the orchestrator loaded (NOT the file on disk — the
    running process): GET `/api/config` and assert the integrated blocks are present and correct.
    Then capture the current dialogue_turns row count from data/adam/memory.sqlite3 via python3
    sqlite3. Run a maintenance text turn through the full pipeline (bypasses mic/ASR):
    `curl --noproxy '*' -fsS -X POST http://127.0.0.1:8080/api/agent/turn -H 'Content-Type:
    application/json' -d '{"transcript":"Адам, ты меня слышишь?"}'`. Confirm the JSON has a
    non-empty `reply`. Re-read the dialogue_turns count and confirm it increased (one viewer +
    one adam row → +2). Pull the turn trace with `scripts/adam_pull_logs.py --last 1` and confirm
    it contains llm + tts stages. Write all outputs into 35-01-EVIDENCE.md.
  </action>
  <verify>
    <automated>curl --noproxy '*' -fsS http://127.0.0.1:8080/api/config | python3 -c "import json,sys;d=json.load(sys.stdin);a=d['services']['asr'];assert 'flora' in d, 'no flora block';assert 'skills' in d, 'no skills block';assert a['wake_words']=='адам', a['wake_words'];assert d['services']['tts']['output_target']=='esp32_speaker';print('CONFIG OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `CONFIG OK` and exits 0 (flora+skills blocks present, wake_words=='адам', tts esp32_speaker)
    - `/api/config` shows agent.mode == 'maintenance'
    - POST /api/agent/turn returns JSON with a non-empty `reply` string
    - `python3 -c "import sqlite3;c=sqlite3.connect('data/adam/memory.sqlite3');print(c.execute('SELECT count(*) FROM dialogue_turns').fetchone()[0])"` increases by ≥2 across the turn
    - `scripts/adam_pull_logs.py --last 1` trace for the turn contains `llm` and `tts` stage events
    - 35-01-EVIDENCE.md records: config assertion output, before/after dialogue_turns count, the reply text, the turn trace
  </acceptance_criteria>
  <done>Live integrated config validated, maintenance text-turn smoke green (reply + memory write + trace), evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/agent → localhost API | Local-only HTTP to 127.0.0.1:8080 — no new endpoints introduced this phase |
| Jetson → ESP (LAN) | Existing 10.10.10.171 control/audio plane — unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | T (Tampering) | git checkout in main dir | accept | Verification phase; checkout is local + reversible (return branch recorded in EVIDENCE before switch). No push without explicit user confirmation (D-04). |
| T-35-02 | I (Info Disclosure) | /api/config exposes config | accept | localhost-only, no new fields, no secrets added; same surface as prior phases. |

This phase introduces NO new network endpoints, inputs, or external surface — it operates on the
existing localhost orchestrator and the existing LAN ESP. No new attack surface.
</threat_model>

<verification>
- Branch live in main dir = ultimate-integration; worktree gone; nothing pushed.
- Orchestrator healthy on integrated code; dependent services up; Ollama absent.
- Live config has flora+skills+wake_words=«адам»+esp32_speaker.
- Maintenance text turn produces reply + dialogue_turns rows + llm/tts trace.
</verification>

<success_criteria>
- All three tasks' acceptance criteria met and recorded in 35-01-EVIDENCE.md.
- The system is ready for Wave 2 (live voice) — integrated code is what's running live.
</success_criteria>

<output>
After completion, create `.planning/phases/35-live-integration-testing-ultimate-integration/35-01-SUMMARY.md`
</output>
