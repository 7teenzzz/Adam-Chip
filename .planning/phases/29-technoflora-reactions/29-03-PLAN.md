---
phase: 29-technoflora-reactions
plan: 03
type: execute
wave: 2
depends_on: [29-01, 29-02]
files_modified:
  - System/adam/flora.py
  - System/adam/device.py
  - System/Orchestrator.py
  - tests/test_flora.py
autonomous: true
requirements: [FLORA-03, FLORA-06]
user_setup: []

must_haves:
  truths:
    - "FloraController subscribes to event_log and maps real pipeline events to flora presets"
    - "wake_word_detected → accent; voice_state_change(to=listening) → attentive; voice_state_change(to=standby) → breathe; llm_thinking_started → think_pulse"
    - "The attentive (listening) state sends vibro_enabled=false (vibro silent during ASR, D-11)"
    - "FloraController starts in Orchestrator lifespan and stops cleanly (unsubscribes, cancels task)"
    - "ESP HTTP for flora state goes through MCUClient (_NO_PROXY_OPENER), never a fresh client"
  artifacts:
    - path: "System/adam/flora.py"
      provides: "FloraController: event consumer + state mapping + start/stop lifecycle"
      contains: "class FloraController"
    - path: "System/adam/device.py"
      provides: "MCUClient.set_flora_state — thin POST /api/flora/state via _NO_PROXY_OPENER"
      contains: "set_flora_state"
    - path: "System/Orchestrator.py"
      provides: "flora_controller construction + lifespan start/stop wiring"
      contains: "flora_controller"
  key_links:
    - from: "FloraController._consume"
      to: "event_log.subscribe() queue"
      via: "asyncio.Queue get loop, unsubscribe in finally"
      pattern: "event_log.subscribe"
    - from: "FloraController._set_state"
      to: "MCUClient.set_flora_state → /api/flora/state"
      via: "_NO_PROXY_OPENER POST"
      pattern: "set_flora_state"
    - from: "Orchestrator lifespan"
      to: "flora_controller.start() / .stop()"
      via: "next to mic_reader.start()/stop()"
      pattern: "flora_controller.start"
---

<objective>
Build the Jetson event layer (FLORA-03) and enforce vibro policy (FLORA-06).
`System/adam/flora.py` `FloraController` subscribes to `event_log` (the real
pub-sub Queue, NOT a callback bus), maps VERIFIED pipeline event names to flora
presets, and POSTs `/api/flora/state` via a new thin `MCUClient.set_flora_state`
(mandatory `_NO_PROXY_OPENER`). The `attentive` (listening) state sends
`vibro_enabled=false` so motors stay silent during ASR (D-11/FLORA-06).
FloraController is constructed alongside the other Orchestrator singletons and
started/stopped in the lifespan next to `mic_reader`.

The RMS speech-sync streamer (FLORA-04) is plan 04 — this plan stubs `tts_started`/
`tts_finished` handling to a generic attentive-style pulse so the ambient layer is
complete and plan 04 only fills the RMS path.

Purpose: the Jetson-owns-pipeline-state half of the hybrid (D-03/D-04).
Output: flora.py, MCUClient.set_flora_state, lifespan wiring, FLORA-03/06 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-technoflora-reactions/29-RESEARCH.md
@.planning/phases/29-technoflora-reactions/29-PATTERNS.md
@System/adam/CLAUDE.md

<interfaces>
VERIFIED real event names + payloads (Orchestrator.py):
- `wake_word_detected` (line 828) — детекция
- `voice_state_change` (line 561) payload `{from, to, reason}`, states boot_warmup|standby|listening|reply
- `llm_thinking_started` (line 2398) — раздумье
- `tts_started` (lines 2793, 2992) — ответ boundary start
- `tts_finished` (lines 2986, 2997) — ответ boundary end
There is NO distinct "answer" voice_state; tts_started/tts_finished ARE the answer boundary.

From System/adam/events.py (VERIFIED):
- `event_log.subscribe(max_queue=200) -> asyncio.Queue` (line 80)
- `event_log.unsubscribe(queue)` (line 87)
- `event_log.append(type, payload, *, turn_id=None)` (line 32)

From System/adam/device.py (VERIFIED):
- `MCUClient.set_channels(updates: list[dict]) -> DeviceResult` (line 69, clamps + _NO_PROXY_OPENER)
- `MCUClient.request(method, path, payload) -> DeviceResult` (line 46) / `_request` thread-wrapped
- `_NO_PROXY_OPENER` (line 14) — mandatory for all ESP HTTP

From System/Orchestrator.py (VERIFIED):
- singletons constructed at module scope ~lines 1296-1313 (mic_reader, voice_loop, scene_worker); `mcu` at line 75, `event_log` at line 66
- lifespan start `await mic_reader.start()` (line 1788); stop `await mic_reader.stop()` (line 1807, finally block)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCUClient.set_flora_state (device.py)</name>
  <files>System/adam/device.py</files>
  <read_first>
    - System/adam/device.py:33-78 (MCUClient __init__, set_channels, set_scene — the method shape to mirror)
    - System/adam/device.py:46-47 (request/_request via asyncio.to_thread) and the _request body lower in the file (how POST payload + _NO_PROXY_OPENER are used)
    - 29-PATTERNS.md (flora.py section — set_flora_state guidance: reuse self._request "POST" "/api/flora/state")
  </read_first>
  <action>
    Add `async def set_flora_state(self, state: str, params: dict[str, Any] | None = None) -> DeviceResult`
    to MCUClient, mirroring `set_scene`. Build payload `{"state": state, **(params or {})}` — keep keys flat
    (firmware parser is flat-key, 29-RESEARCH §Firmware HTTP Endpoint). Clamp any duty params to value_min..value_max
    and any channel-ish params to channel_min..channel_max before sending (defence-in-depth, mirrors set_channels).
    Route through `asyncio.to_thread(self._request, "POST", "/api/flora/state", payload)` so it reuses
    `_NO_PROXY_OPENER`. Do NOT roll a new HTTP client (CLAUDE.md v2ray socket-leak gotcha).
  </action>
  <acceptance_criteria>
    - device.py contains `async def set_flora_state` posting to `/api/flora/state`
    - set_flora_state routes through self._request (no new urllib/httpx client introduced)
    - duty/channel params clamped before send
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=System python -c "import inspect; from adam.device import MCUClient; assert 'set_flora_state' in dir(MCUClient); src=inspect.getsource(MCUClient.set_flora_state); assert '/api/flora/state' in src and '_request' in src"</automated>
  </verify>
  <done>MCUClient.set_flora_state posts to /api/flora/state via _NO_PROXY_OPENER path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FloraController event consumer + state mapping (flora.py)</name>
  <files>System/adam/flora.py, tests/test_flora.py</files>
  <behavior>
    - test_event_mapping: a FloraController fed synthetic events maps:
      {"type":"wake_word_detected"} → set_flora_state called with "accent";
      {"type":"voice_state_change","payload":{"to":"listening"}} → "attentive";
      {"type":"voice_state_change","payload":{"to":"standby"}} → "breathe";
      {"type":"llm_thinking_started"} → "think_pulse".
      Use a fake MCUClient capturing (state, params) calls — no network.
    - test_vibro_silent_listening: when state == "attentive" (listening), the params passed to
      set_flora_state carry vibro_enabled=False (D-11).
  </behavior>
  <read_first>
    - System/adam/api_runtime.py:521-547 (canonical subscribe()→queue consume loop — copy structure)
    - System/adam/mic_reader.py:199-243 (long-lived task start()/stop() lifecycle — copy structure)
    - System/adam/events.py:80-89 (subscribe/unsubscribe semantics)
    - System/adam/config.py:302 (settings.section pattern)
    - 29-RESEARCH.md §Pattern 4 (event→state mapping, VERIFIED names) + §Vibro Policy
    - 29-PATTERNS.md (flora.py section — composed analogs, mapping block, lifecycle block)
    - tests/test_flora.py (existing scaffold from plan 02 — fill test_event_mapping + test_vibro_silent_listening, drop their skip markers)
  </read_first>
  <action>
    Create `class FloraController` taking (settings_section: dict from settings.section("flora"),
    mcu_client: MCUClient, event_log: EventLog). `start()`: `self._queue = event_log.subscribe()` +
    `self._task = asyncio.create_task(self._consume(), name="flora_consumer")` (mic_reader.start pattern).
    `stop()`: cancel consumer task (and any RMS streamer task — plan 04 fills that), await it swallowing
    CancelledError, then `event_log.unsubscribe(self._queue)`.
    `_consume()`: `while True: event = await self._queue.get(); await self._handle(event)`,
    with `event_log.unsubscribe` in finally (api_runtime pattern).
    `_handle(event)`: dispatch on `event.get("type")` per VERIFIED mapping (interfaces block):
    wake_word_detected→accent; voice_state_change to listening→attentive (vibro_enabled=False, D-11),
    to standby→breathe; llm_thinking_started→think_pulse;
    tts_started→ `await self._on_answer_start(event)` (STUB this plan: generic attentive-style pulse,
    plan 04 replaces with RMS streamer); tts_finished→ `await self._on_answer_end()`.
    `_set_state(state, **params)`: read per-state defaults from the flora config section, build params
    (base_duty, peak_duty, period_ms, crossfade_ms, vibro_enabled, vibro_duty), call
    `mcu_client.set_flora_state(state, params)`. Force vibro_enabled=False whenever state=="attentive"
    (belt-and-suspenders mirror of firmware, D-11). Convert pct params (base_duty_pct etc.) to 0-4095 duties
    using the flora config; gamma is applied firmware-side, send raw duties.
    Read all numbers from the flora config section (Config-First) — no hardcoded magic numbers.
    Optionally `event_log.append("flora_state_change", {"state": state})` for diagnostics.
    Then fill test_event_mapping + test_vibro_silent_listening in tests/test_flora.py using a fake
    MCUClient that records calls; remove their skip markers.
  </action>
  <acceptance_criteria>
    - flora.py contains `class FloraController` with `start`, `stop`, `_consume`, `_handle`, `_set_state`
    - flora.py uses `event_log.subscribe()` and `event_log.unsubscribe` (queue pattern, not callbacks)
    - flora.py reads numbers from the flora config section (no hardcoded duty/period literals in mapping logic)
    - `pytest tests/test_flora.py::test_event_mapping -x` exits 0
    - `pytest tests/test_flora.py::test_vibro_silent_listening -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=System pytest tests/test_flora.py::test_event_mapping tests/test_flora.py::test_vibro_silent_listening -x</automated>
  </verify>
  <done>FloraController maps all ambient events to presets; attentive forces vibro off; FLORA-03/06 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire flora_controller into Orchestrator lifespan</name>
  <files>System/Orchestrator.py</files>
  <read_first>
    - System/Orchestrator.py:66 (event_log), :75 (mcu = MCUClient(...)), :1296-1313 (singleton construction block — add flora_controller here)
    - System/Orchestrator.py:1784-1788 (lifespan start, mic_reader.start()), :1802-1814 (finally block, mic_reader.stop())
    - System/adam/flora.py (FloraController constructor signature from Task 2)
    - 29-PATTERNS.md (Orchestrator.py section — start/stop placement, construction with settings.section("flora") + shared mcu + event_log)
  </read_first>
  <action>
    Import FloraController in Orchestrator.py. Construct `flora_controller = FloraController(
    settings.section("flora"), mcu, event_log)` in the module-level singleton block alongside
    mic_reader/scene_worker (~line 1313). In lifespan, add `await flora_controller.start()` next to
    `await mic_reader.start()` (line ~1788). In the finally block, add `await flora_controller.stop()`
    next to `await mic_reader.stop()` (line ~1807). flora only consumes events — no new emissions required.
  </action>
  <acceptance_criteria>
    - Orchestrator.py imports FloraController and constructs `flora_controller`
    - Orchestrator.py calls `flora_controller.start()` in lifespan entry and `flora_controller.stop()` in finally
    - `PYTHONPATH=System python -c "import ast; ast.parse(open('System/Orchestrator.py').read())"` exits 0 (syntax valid)
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=System python -c "src=open('System/Orchestrator.py').read(); assert 'flora_controller.start()' in src and 'flora_controller.stop()' in src and 'FloraController(' in src; import ast; ast.parse(src)"</automated>
  </verify>
  <done>flora_controller constructed + started/stopped in lifespan next to mic_reader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jetson event bus → FloraController | Trusted in-process events; no external input |
| FloraController → ESP HTTP (:80) | Jetson-controlled POST; clamped duties cross to firmware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Tampering | flora duties sent to ESP | mitigate | MCUClient.set_flora_state clamps duty/channel params before POST; firmware re-clamps (plan 01) — defence-in-depth |
| T-29-08 | Denial of Service | flora_consumer queue backpressure | accept | event_log.subscribe bounded queue (max_queue=200) already drops on overflow; flora consumer is a single fast handler; residual accepted |
| T-29-09 | Repudiation | flora state changes unlogged | mitigate | Optional `flora_state_change` diagnostic event via event_log.append (audit trail in events.jsonl) |
</threat_model>

<verification>
- `pytest tests/test_flora.py -x` green (test_flora_config + test_event_mapping + test_vibro_silent_listening; test_rms_envelope still skipped for plan 04).
- Orchestrator imports + wires flora_controller (Task 3 verify).
</verification>

<success_criteria>
- FloraController consumes events and maps all 5 ambient transitions to presets via MCUClient.set_flora_state.
- Vibro forced off in attentive on the Jetson tier (FLORA-06).
- flora_controller lifecycle wired into Orchestrator lifespan.
</success_criteria>

<output>
After completion, create `.planning/phases/29-technoflora-reactions/29-03-SUMMARY.md`
</output>
