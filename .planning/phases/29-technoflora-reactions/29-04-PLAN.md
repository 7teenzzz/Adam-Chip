---
phase: 29-technoflora-reactions
plan: 04
type: execute
wave: 3
depends_on: [29-03]
files_modified:
  - System/adam/flora.py
  - tests/test_flora.py
autonomous: true
requirements: [FLORA-04]
user_setup: []

must_haves:
  truths:
    - "An RMS envelope is computed from TTS WAV bytes using stdlib wave+audioop (no numpy)"
    - "The envelope is normalized 0..1 and mapped to base_duty_pct(25)..peak_duty_pct(90) per D-08"
    - "During the answer state, FloraController streams brightness frames to light channels 0-10 in lockstep with playback, offset by flora.speech.hdmi_latency_offset_ms"
    - "On tts_finished or barge-in (voice_state_change→listening|standby, or new wake_word_detected) the RMS streamer is cancelled immediately and hands back to attentive/breathe"
    - "Random sparks boost a random channel subset to ~peak on envelope peaks (D-08), cluster-friendly (no center)"
  artifacts:
    - path: "System/adam/flora.py"
      provides: "RMS envelope computation + frame streamer task replacing the plan-03 answer stub"
      contains: "_rms_envelope"
    - path: "tests/test_flora.py"
      provides: "test_rms_envelope filled (level count + range from known WAV)"
      contains: "def test_rms_envelope"
  key_links:
    - from: "FloraController._on_answer_start"
      to: "_rms_stream task"
      via: "perf_counter t0 timer + hdmi_latency_offset_ms, set_channels per envelope sample"
      pattern: "set_channels"
    - from: "_rms_envelope"
      to: "audioop.rms over wave windows"
      via: "per frame_interval_ms window → normalized level"
      pattern: "audioop.rms"
---

<objective>
Implement RMS speech sync (FLORA-04): the answer-state path where Jetson drives
light brightness in lockstep with TTS playback. Compute a downsampled RMS
envelope from the TTS WAV (stdlib `wave`+`audioop`, no numpy), normalize to
base..peak duty (D-08), and stream brightness frames to light channels 0-10 over
HTTP via `MCUClient.set_channels` on a wall-clock timer started at playback
dispatch, offset by `flora.speech.hdmi_latency_offset_ms` (HDMI buffer lag, D-07).
Random sparks on peaks add texture (D-08). The streamer cancels immediately on
`tts_finished` or barge-in (D-09).

This replaces the generic answer-state stub from plan 03 with the real RMS path.
Constraint (RESEARCH Open Q2): precise sync requires the streaming-consumer +
jetson_hdmi path that exposes WAV bytes; the `/speak` fallback degrades to a
generic pulse — document this.

Purpose: the cinematic voice-reactive light that makes the installation feel alive.
Output: RMS envelope + frame streamer in flora.py, FLORA-04 test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-technoflora-reactions/29-RESEARCH.md
@.planning/phases/29-technoflora-reactions/29-PATTERNS.md
@System/adam/CLAUDE.md

<interfaces>
From System/adam/inference.py (VERIFIED):
- `wave`, `audioop` imported (lines 4, 11); `wave.open` used at line 500
- `tts._get_wav_bytes_sync(text) -> bytes` (line 264) — raw WAV, no playback (RMS source)
- `tts._play_wav_bytes_local_sync(wav)` (line 280) — BLOCKING aplay/paplay (wrapped in asyncio.to_thread by consumer)
- `tts.interrupt_playback()` (line 406) — barge-in kill of aplay Popen

From System/adam/device.py (VERIFIED):
- `MCUClient.set_channels(updates: list[dict]) -> DeviceResult` (line 69) — batch POST ch updates, clamps + _NO_PROXY_OPENER

flora config (plan 02): speech.frame_interval_ms, speech.hdmi_latency_offset_ms,
speech.base_duty_pct, speech.peak_duty_pct, speech.spark_probability, light_channels [0..10].

Barge-in / answer-end signals: tts_finished event; voice_state_change→listening|standby; new wake_word_detected.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WAV→RMS envelope computation</name>
  <files>System/adam/flora.py, tests/test_flora.py</files>
  <behavior>
    - test_rms_envelope: feed a known synthesized WAV (the _make_sine_wav helper from plan 02's scaffold,
      or synthesize a ramp/sine of known duration). Assert:
      - the returned envelope is a list of floats in [0.0, 1.0]
      - the number of samples ≈ wav_duration_ms / frame_interval_ms (within ±1)
      - a louder WAV yields higher mean level than a quieter WAV (monotonic w.r.t. amplitude)
  </behavior>
  <read_first>
    - System/adam/inference.py:1-15 (wave/audioop imports), :264-272 (_get_wav_bytes_sync), :495-505 (wave.open + audioop usage idiom)
    - System/adam/mic_reader.py:~500 (audioop.rms idiom over PCM windows)
    - 29-RESEARCH.md §RMS Speech Sync (steps 1-2: window by frame_interval_ms, audioop.rms, normalize 0..1, map base..peak)
    - 29-PATTERNS.md (flora.py section — RMS envelope from WAV block)
    - System/adam/flora.py (FloraController from plan 03 — add envelope helper here)
  </read_first>
  <action>
    Add `_rms_envelope(self, wav_bytes: bytes) -> list[float]` to flora.py (or a module-level helper):
    `wave.open(BytesIO(wav_bytes))`, read nframes; window the PCM into chunks of
    `frame_interval_ms` worth of samples (from flora config, ~80 ms → ~12.5 fps); per window
    `audioop.rms(window, sampwidth)`; collect levels; normalize by the max level (or full-scale)
    to 0..1. No numpy. Handle empty/short WAV gracefully (return []).
    Add `_envelope_to_duties(self, levels) -> list[int]`: map each 0..1 level to a duty between
    base_duty (from base_duty_pct, e.g. 25% of 4095) and peak_duty (peak_duty_pct, 90% of 4095) per D-08.
    Fill test_rms_envelope in tests/test_flora.py (drop its skip marker) per the behavior block,
    importing flora lazily inside the test.
  </action>
  <acceptance_criteria>
    - flora.py contains `_rms_envelope` using `audioop.rms` and `wave.open` (no numpy import)
    - flora.py maps levels to base..peak duties from the flora config
    - `pytest tests/test_flora.py::test_rms_envelope -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=System pytest tests/test_flora.py::test_rms_envelope -x</automated>
  </verify>
  <done>WAV→envelope→duties pipeline implemented and tested with a known WAV.</done>
</task>

<task type="auto">
  <name>Task 2: RMS frame streamer + barge-in cancel</name>
  <files>System/adam/flora.py</files>
  <read_first>
    - System/adam/flora.py (FloraController._on_answer_start stub + _consume/_handle from plan 03; _envelope helpers from Task 1)
    - System/adam/inference.py:280-300 (_play_wav_bytes_local_sync blocking), :406 (interrupt_playback)
    - System/Orchestrator.py:2785-2900 (streaming TTS consumer — where WAV bytes + asyncio.to_thread(_play...) are dispatched; the answer-state hook point and where tts_started fires)
    - 29-RESEARCH.md §RMS Speech Sync (steps 3-5: t0=perf_counter at dispatch, +hdmi offset, await sleep per sample, set_channels, sparks; barge-in cancel) + §Pitfall 5 (HDMI offset) + Open Q2 (path constraint)
    - 29-PATTERNS.md (flora.py section — RMS frame timer block)
  </read_first>
  <action>
    Replace the plan-03 answer-state stub: on `tts_started`, obtain the WAV bytes for the reply
    (from the event payload if present, else fall back to a generic attentive-style pulse and DOCUMENT that
    `/speak` / non-streaming has no exposed WAV — RESEARCH Open Q2). Compute envelope+duties (Task 1).
    `_start_rms_stream`: create an asyncio task `self._rms_task = asyncio.create_task(self._rms_stream(duties), name="flora_rms")`.
    `_rms_stream(duties)`: `t0 = perf_counter()`; for sample i at time `i*frame_interval_ms`,
    `await asyncio.sleep` until `t0 + i*interval/1000 + hdmi_latency_offset_ms/1000`, then
    `await mcu_client.set_channels([{channel: ch, value: duty} for ch in light_channels])`.
    On envelope peaks, with `spark_probability` boost a RANDOM subset of light channels to ~peak for one frame
    (D-08, cluster-friendly, no spatial center — D-01). Vibro (ch 11-14) follows the light rhythm subtly per D-11
    (drive from same phase; vibro intensity from config; NOT silent here — only attentive is silent).
    `_stop_rms_stream` (called on tts_finished): cancel `_rms_task`, swallow CancelledError, hand back to
    attentive or breathe.
    Barge-in (D-09): in `_handle`, if a `voice_state_change` to listening/standby OR a new `wake_word_detected`
    arrives while `_rms_task` is live, cancel it immediately (light snaps to attentive). Ensure `stop()`
    (plan 03) also cancels `_rms_task`.
    Frame rate ceiling ~15 fps (HTTP round-trip + ESP socket limit, RESEARCH A1) — frame_interval_ms from config
    keeps this ≤ ~12.5 fps default; do not exceed.
  </action>
  <acceptance_criteria>
    - flora.py `_start_rms_stream` creates an asyncio task and `_stop_rms_stream` cancels it
    - flora.py streams via `mcu_client.set_channels` to the flora light_channels (0-10) only
    - flora.py applies `hdmi_latency_offset_ms` to the frame schedule
    - flora.py cancels `_rms_task` on voice_state_change→listening/standby and on wake_word_detected (barge-in)
    - `PYTHONPATH=System python -c "import inspect; from adam.flora import FloraController; s=inspect.getsource(FloraController); assert 'set_channels' in s and 'hdmi_latency_offset' in s.replace('_ms','')"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=System pytest tests/test_flora.py -x</automated>
  </verify>
  <done>RMS streamer drives light 0-10 in lockstep with playback; cancels instantly on barge-in; full test_flora.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FloraController → ESP HTTP (:80) | High-rate (~12 fps) frame POSTs cross to the ESP control server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10 | Denial of Service | ESP :80 socket pool under RMS frame rate | mitigate | frame_interval_ms config caps frame rate ≤ ~12.5 fps (default 80 ms); short-lived connections close promptly; LWIP_MAX_SOCKETS=16 headroom; RMS task cancelled at tts_finished so bursts are bounded to reply duration |
| T-29-11 | Tampering | RMS duties to light channels | mitigate | set_channels clamps duty 0-4095 and channel 0-15; only light_channels (0-10) driven; vibro untouched by RMS path |
| T-29-12 | Denial of Service | runaway RMS task on missed tts_finished | mitigate | Barge-in cancel on voice_state_change/wake_word_detected + stop() cancels _rms_task; envelope length is bounded by WAV duration |
</threat_model>

<verification>
- `pytest tests/test_flora.py -x` fully green (all 4 named tests, no skips remaining).
- Manual (29-VALIDATION, on-HW): speak a TTS reply, observe light tracks voice envelope; tune hdmi_latency_offset_ms until aligned; barge-in snaps light to listening.
</verification>

<success_criteria>
- WAV→RMS envelope→duty pipeline (stdlib wave+audioop) maps voice to light 0-10 at base..peak (D-08).
- Frames stream in lockstep with playback (one timer from dispatch + HDMI offset, D-07).
- Sparks on peaks (D-08); barge-in cancels instantly (D-09).
- test_rms_envelope green; full flora test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/29-technoflora-reactions/29-04-SUMMARY.md`
</output>
