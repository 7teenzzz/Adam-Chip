---
phase: 42-webui-reorganization
plan: 05
type: execute
wave: 3
depends_on: [01]
files_modified:
  - System/WebUI/static/js/panels/system/audioAndVideo.js
  - System/WebUI/static/js/panels/audioInput.js
autonomous: true
requirements: [WEBUI-R02]
must_haves:
  truths:
    - "Страница #/system/audio объединяет калибровку микрофона/wake-word/VAD/EQ/пресеты (из audioInput) с видео/камера/scene-полями (из settings)"
    - "Карточка громкости больше не использует card-full (нормальная ширина)"
    - "Дублирующее поле wake-word threshold присутствует ровно один раз"
    - "Дублирующая карточка Камера объединена с Видеопоток в одну"
  artifacts:
    - path: "System/WebUI/static/js/panels/system/audioAndVideo.js"
      provides: "Объединённая панель Аудио и видео (3.1)"
      min_lines: 120
      contains: "field-grid"
  key_links:
    - from: "system/audioAndVideo.js"
      to: "/api/config PATCH"
      via: "saveConfigField для media.audio/media.video/wake_word"
      pattern: "/api/config"
---

<objective>
Создать страницу Аудио и видео (#/system/audio, D-07): объединить контент audioInput.js (калибровка микрофона, чувствительность wake-word, VAD, EQ, пресеты — все 5 карточек) с аудио/видео-карточками из settings.js (OWW, тайминги пайплайна, видеопоток, камера, микрофон/VAD, scene-воркер, silence calibration). Исправить баг card-full на карточке громкости (D-15/WEBUI-R07), удалить дубль wake-word threshold, объединить дублирующиеся карточки Видеопоток+Камера.

Purpose: D-07 + WEBUI-R07 — единая страница аудио/видео вместо разбросанного контента. Карточки мигрируют из удаляемого settings.js (RESEARCH mapping → 3.1).
Output: panels/system/audioAndVideo.js + точечный фикс audioInput.js (card-full).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-webui-reorganization/42-PATTERNS.md
@.planning/phases/42-webui-reorganization/42-RESEARCH.md
@System/WebUI/static/js/panels/audioInput.js
@System/WebUI/static/js/panels/settings.js

<interfaces>
<!-- settings.js карточки → 3.1 (RESEARCH mapping): -->
<!--   OWW · Wake word (config/wake_word: threshold, debounce_hits, vad_threshold + buildWakeWordExtras canvas) -->
<!--   Тайминги голосового пайплайна (config/services.asr) -->
<!--   Видеопоток (config/media.video) + Камера (config/media.video — ДУБЛЬ → объединить в одну) -->
<!--   Микрофон и VAD (config/media.audio) -->
<!--   Воркер описания сцены (config/media: scene_worker_enabled/scene_interval_sec/...) -->
<!--   Silence calibration (config/services.asr — buildSilenceCalibExtras) -->
<!-- + все 5 карточек audioInput.js -->

<!-- audioInput.js card-full баг (строка ~820): Карточка 1 "Громкость входа" использует card-full → убрать. -->
<!--   Карточки 2-5 (EQ canvas, OWW meter, Monitor, Presets) — card-full ЛЕГИТИМНО, оставить. -->
<!-- Дубль wake-word threshold: оставить ровно один раз (в OWW/калибровке audioInput строки ~400-450). -->

<!-- saveConfigField / renderFieldRow / getNested / setNested — verbatim из settings.js (42-PATTERNS.md раздел 3) -->
<!-- disposables[] + SSE _dispose паттерн — audioInput.js строки 793-896 (EQ/OWW/Monitor виджеты имеют _dispose) -->
<!-- mount→teardown контракт обязателен (есть SSE — критично для отсутствия утечек) -->
<!-- Путь импорта из panels/system/ → ../../api.js -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Точечный фикс audioInput.js — убрать card-full с карточки громкости</name>
  <read_first>
    - System/WebUI/static/js/panels/audioInput.js (строка ~820 — карточка "Громкость входа"; строки 840/851/864/876 — карточки 2-5 которые ЛЕГИТИМНО card-full)
    - .planning/phases/42-webui-reorganization/42-PATTERNS.md (раздел 6 — bug location)
  </read_first>
  <files>System/WebUI/static/js/panels/audioInput.js</files>
  <action>
    Найти карточку "Громкость входа" (media.audio.input_gain, ~строка 820) с классом "card card-full" → заменить класс на "card" (убрать только card-full). Карточки 2-5 (EQ canvas, OWW meter, Monitor, Presets) — НЕ трогать, остаются card-full. Это минимальный фикс; полная миграция контента в Task 2. audioInput.js остаётся рабочим (старый маршрут сохранён в ROUTES до Wave 4).
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/audioInput.js && grep -q 'Громкость входа' System/WebUI/static/js/panels/audioInput.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Карточка "Громкость входа" использует class "card" (без card-full)
    - Карточки 2-5 (EQ/OWW meter/Monitor/Presets) сохраняют card-full
    - `node --check` проходит
  </acceptance_criteria>
  <done>Карточка громкости имеет нормальную ширину; остальные card-full не затронуты.</done>
</task>

<task type="auto">
  <name>Task 2: panels/system/audioAndVideo.js — объединённая панель Аудио и видео</name>
  <read_first>
    - System/WebUI/static/js/panels/audioInput.js (все 5 карточек, buildVolumeSection, EQ/OWW/Monitor виджеты с _dispose, disposables-паттерн, mount/teardown)
    - System/WebUI/static/js/panels/settings.js (SCHEMA-группы аудио/видео, renderFieldRow/saveConfigField, buildWakeWordExtras, buildSilenceCalibExtras)
    - .planning/phases/42-webui-reorganization/42-RESEARCH.md (раздел "→ 3.1 Аудио и видео" — точный список карточек и полей)
  </read_first>
  <files>System/WebUI/static/js/panels/system/audioAndVideo.js</files>
  <action>
    Создать panels/system/audioAndVideo.js. el() verbatim. Импорт { api, toast, subscribeEvents } из ../../api.js. Скопировать helpers (renderFieldRow/saveConfigField/getNested/setNested) verbatim из settings.js.
    1. Локальный SCHEMA для аудио/видео-карточек (из RESEARCH 3.1):
       - OWW · Wake word (config/wake_word): threshold, debounce_hits, vad_threshold + buildWakeWordExtras() canvas.
       - Тайминги голосового пайплайна (config/services.asr): listening_silence_timeout_sec, reply_silence_timeout_sec, post_tts_discard_window_ms, silence_after_speech_ms.
       - Видео (config/media.video): ОБЪЕДИНИТЬ Видеопоток + Камера в ОДНУ карточку (primary, gstreamer_pipeline, preview_enabled, camera_capture_interval_sec, video_device, camera_width, camera_height, camera_quality). НЕ создавать две карточки.
       - Микрофон и VAD (config/media.audio): mic_source, input_device, sample_rate, min_speech_ms, max_segment_ms, webrtc_vad_aggressiveness.
       - Воркер описания сцены (config/media): scene_worker_enabled, scene_interval_sec, scene_stale_after_sec, scene_context_count.
    2. Перенести 5 карточек audioInput.js (Громкость БЕЗ card-full; EQ/OWW meter/Monitor/Presets С card-full). Виджеты с _dispose регистрировать в disposables[].
    3. ДЕДУП: wake-word threshold присутствует ровно ОДИН раз (в OWW-карточке/калибровке), не дублировать в других группах.
    4. Silence calibration (buildSilenceCalibExtras) — одна карточка.
    5. mount(target) с disposables[]: рендерить карточки в .card-grid; собрать _dispose от EQ/OWW/Monitor/SSE; teardown дренирует disposables.
    6. Порядок карточек — на усмотрение (Claude's Discretion D-02): видео → микрофон → wake-word → калибровка → EQ → пресеты.
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/system/audioAndVideo.js && grep -q 'field-grid' System/WebUI/static/js/panels/system/audioAndVideo.js && grep -q 'media.video' System/WebUI/static/js/panels/system/audioAndVideo.js && grep -q 'media.audio' System/WebUI/static/js/panels/system/audioAndVideo.js && grep -q 'wake_word' System/WebUI/static/js/panels/system/audioAndVideo.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - audioAndVideo.js экспортирует mount(target) возвращающий teardown
    - Содержит видео-поля (media.video) объединённые в ОДНУ карточку, микрофон/VAD (media.audio), wake_word, scene-воркер
    - Все 5 карточек audioInput перенесены; Громкость без card-full
    - wake-word threshold встречается ровно один раз
    - Виджеты с _dispose зарегистрированы в disposables[] (нет утечки SSE)
    - `node --check` проходит
  </acceptance_criteria>
  <done>Аудио и видео — единая страница; видео+камера объединены; громкость нормальной ширины; threshold не дублируется; SSE-виджеты очищаются на teardown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → /api/config | Аудио/видео-параметры (VAD/gain/pipeline) пишутся в Config.json, влияют на пайплайн распознавания |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09 | Denial of Service | SSE-виджеты (EQ/OWW meter) без teardown | mitigate | disposables[] собирает _dispose всех SSE-виджетов; teardown дренирует — предотвращает накопление дублирующих listener'ов (Pitfall #1). |
| T-42-10 | Tampering | media.audio gain/threshold | accept | Параметры hot-reloadable, обратимы; диапазоны валидируются schema на backend. Оператор в изолированной сети. |
</threat_model>

<verification>
- #/system/audio открывается без ошибок консоли; все карточки рендерятся
- Карточка громкости — обычная ширина (визуально), остальные card-full корректны
- wake-word threshold виден один раз
- Видео и камера — одна карточка
- Сохранение поля (напр. webrtc_vad_aggressiveness): PATCH /api/config успешен (Network)
- mount→unmount: EventStream-соединения закрываются (нет роста listener'ов)
</verification>

<success_criteria>
- Аудио и видео = audioInput (5 карточек) + settings audio/video карточки (D-07)
- card-full фикс громкости (WEBUI-R07)
- дубль threshold и дубль Камера устранены
</success_criteria>

<output>
After completion, create `.planning/phases/42-webui-reorganization/42-05-SUMMARY.md`
</output>
