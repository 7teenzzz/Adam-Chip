---
phase: 42-webui-reorganization
plan: 06
type: execute
wave: 3
depends_on: [01]
files_modified:
  - System/WebUI/static/js/panels/system/servicesModels.js
autonomous: true
requirements: [WEBUI-R02, WEBUI-R04]
must_haves:
  truths:
    - "Страница #/system/services объединяет управление сервисами (services.js) и поля моделей (models.js + settings.js LLM/TTS/ASR/VLM карточки)"
    - "Мёртвая карточка VILA1.5-3b (buildVlmCard, /api/live_vlm/*) удалена"
    - "Сервис Cosmos VLM (adam-vlm.service, :8051) представлен через SERVICE_META"
    - "Невалидные поля удалены: keep_alive (LLM), riva (ASR provider), reply_absolute_deadline_sec (ASR), дубли vad_onset/vad_offset; VLM hint :8084 → :8051"
  artifacts:
    - path: "System/WebUI/static/js/panels/system/servicesModels.js"
      provides: "Объединённая панель Сервисы и модели (3.2)"
      min_lines: 150
      contains: "adam-vlm.service"
  key_links:
    - from: "system/servicesModels.js"
      to: "/api/services/{name}/{verb}"
      via: "start/stop/restart сервисов"
      pattern: "/api/services/"
    - from: "system/servicesModels.js"
      to: "/api/config + /api/tuning"
      via: "поля моделей LLM/TTS/ASR/VLM"
      pattern: "/api/(config|tuning)"
---

<objective>
Создать страницу Сервисы и модели (#/system/services, D-08): слить services.js (управление systemd-сервисами) и models.js + LLM/TTS/ASR/VLM-карточки из settings.js. Удалить мёртвую Docker-карточку VILA1.5-3b, невалидные поля (keep_alive, riva, reply_absolute_deadline_sec), дубли vad_onset/vad_offset. Добавить сервис Cosmos VLM (adam-vlm.service) через SERVICE_META. Исправить stale VLM hint :8084 → :8051.

Purpose: D-08 + WEBUI-R04 — единая страница сервисов и моделей без мёртвого/невалидного контента.
Output: panels/system/servicesModels.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-webui-reorganization/42-PATTERNS.md
@.planning/phases/42-webui-reorganization/42-RESEARCH.md
@System/WebUI/static/js/panels/services.js
@System/WebUI/static/js/panels/models.js
@System/WebUI/static/js/panels/settings.js

<interfaces>
<!-- SERVICE_META (services.js строки 20-24) — расширить vlm: -->
<!--   llm: adam-llm.service :8081, tts: adam-tts-silero.service :8082, -->
<!--   asr: adam-asr-whisperx.service :8095, vlm: adam-vlm.service :8051 (НОВЫЙ) -->
<!-- УДАЛИТЬ buildVlmCard() целиком (мёртвый Docker VILA1.5-3b, /api/live_vlm/*) — Pitfall #7 -->
<!-- buildServiceCard(name, meta) (services.js строки 98-173) — verbatim для всех сервисов -->
<!-- setInterval(refreshAll, 5000) teardown (services.js строки 198-201) -->

<!-- settings.js карточки → 3.2 (RESEARCH mapping): -->
<!--   Агент (config/agent), LLM·инфраструктура (config/services.llm), LLM·runtime (tuning/llm), -->
<!--   Сборка промта (tuning/prompt), Голос runtime (tuning/voice), TTS·Silero (config/services.tts), -->
<!--   TTS·Филлер (config/services.tts), ASR·WhisperX (config/services.asr), VLM (config/services.vlm) -->

<!-- ОЧИСТКА полей (Pitfalls 3/6/8): -->
<!--   LLM: убрать keep_alive (no-op llama.cpp) -->
<!--   ASR provider select: убрать опцию riva (только whisperx) -->
<!--   ASR: убрать reply_absolute_deadline_sec (невалидно, Phase 8) -->
<!--   ASR: дедуп vad_onset/vad_offset (в settings.js встречаются дважды — оставить первое вхождение с полным hint) -->
<!--   VLM: hint URL :8084 → :8051 -->

<!-- saveConfigField/saveTuningField/renderFieldRow/getNested — verbatim из settings.js (42-PATTERNS.md раздел 3) -->
<!-- dot()/buildServiceCard — services.js (42-PATTERNS.md раздел 7) -->
<!-- Путь импорта из panels/system/ → ../../api.js -->
<!-- mount→teardown обязателен (есть setInterval refresh) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Управление сервисами — buildServiceCard для llm/tts/asr/vlm, удалить мёртвый VILA</name>
  <read_first>
    - System/WebUI/static/js/panels/services.js (SERVICE_META, buildServiceCard, dot(), refreshAll, setInterval teardown, мёртвый buildVlmCard)
    - .planning/phases/42-webui-reorganization/42-PATTERNS.md (раздел 7 — SERVICE_META extend, buildServiceCard verbatim, удалить buildVlmCard)
  </read_first>
  <files>System/WebUI/static/js/panels/system/servicesModels.js</files>
  <action>
    Создать panels/system/servicesModels.js. el() verbatim. Импорт { api, toast } из ../../api.js. Скопировать dot() и buildServiceCard(name, meta) verbatim из services.js.
    1. SERVICE_META с 4 сервисами: llm (adam-llm.service, :8081), tts (adam-tts-silero.service, :8082), asr (adam-asr-whisperx.service, :8095), vlm (adam-vlm.service, :8051). VLM использует ТОТ ЖЕ buildServiceCard, что и остальные (не Docker).
    2. НЕ переносить buildVlmCard() (мёртвый Docker /api/live_vlm/* для VILA1.5-3b — Pitfall #7). Полностью опустить.
    3. Рендерить 4 service-карточки. refreshAll каждые 5000ms через setInterval; teardown: clearInterval. Зарегистрировать clearInterval в disposables.
    4. Этот task закладывает каркас mount() с disposables; поля моделей добавляются в Task 2 в тот же файл.
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/system/servicesModels.js && grep -q 'adam-vlm.service' System/WebUI/static/js/panels/system/servicesModels.js && grep -q 'adam-llm.service' System/WebUI/static/js/panels/system/servicesModels.js && node -e "const s=require('fs').readFileSync('System/WebUI/static/js/panels/system/servicesModels.js','utf8'); if(/live_vlm/.test(s)){console.error('dead VILA card present');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - servicesModels.js содержит SERVICE_META с 4 сервисами включая vlm → adam-vlm.service :8051
    - Все 4 сервиса используют buildServiceCard (нет отдельного Docker buildVlmCard)
    - Нет упоминаний /api/live_vlm (мёртвый VILA удалён)
    - setInterval refresh имеет clearInterval в teardown
    - `node --check` проходит
  </acceptance_criteria>
  <done>Управление сервисами: llm/tts/asr/vlm через единый buildServiceCard; мёртвый VILA удалён; refresh останавливается на teardown.</done>
</task>

<task type="auto">
  <name>Task 2: Поля моделей LLM/TTS/ASR/VLM + очистка невалидных полей</name>
  <read_first>
    - System/WebUI/static/js/panels/models.js (карточки моделей, невалидные поля keep_alive/riva)
    - System/WebUI/static/js/panels/settings.js (SCHEMA-группы LLM/TTS/ASR/VLM/agent/prompt/voice, дубли vad_onset/vad_offset, reply_absolute_deadline_sec)
    - .planning/phases/42-webui-reorganization/42-RESEARCH.md (раздел "→ 3.2 Сервисы и модели" — точный список полей и очистки)
  </read_first>
  <files>System/WebUI/static/js/panels/system/servicesModels.js</files>
  <action>
    В том же файле добавить локальный SCHEMA для карточек моделей (из RESEARCH 3.2). Скопировать helpers saveConfigField/saveTuningField/renderFieldRow/getNested/setNested verbatim из settings.js. Карточки:
    - Агент (config/agent): mode, language, name.
    - LLM · инфраструктура (config/services.llm): base_url, model, num_ctx, timeout_sec. УБРАТЬ keep_alive.
    - LLM · runtime (tuning/llm): temperature, max_tokens, response_word_target.
    - Сборка промта (tuning/prompt): history_turns, include_scene, include_sensors.
    - Голос runtime (tuning/voice): speaker, speed_multiplier, volume.
    - TTS · Silero (config/services.tts): base_url, output_device, speaker, timeout_sec.
    - TTS · Филлер (config/services.tts): filler_enabled, filler_phrase, filler_delay_ms, filler_probability.
    - ASR · WhisperX (config/services.asr): model, reply_window_sec, reply_window_expired_action, vad_onset, vad_offset, logprob_threshold. УБРАТЬ reply_absolute_deadline_sec; ДЕДУП vad_onset/vad_offset (одно вхождение); provider select БЕЗ опции riva (только whisperx).
    - VLM · описание сцены (config/services.vlm): base_url, model, max_new_tokens, timeout_sec, prompt (textarea, card-full). Hint URL :8084 → :8051.
    Рендерить карточки моделей в .card-grid (после service-карточек или отдельной секцией). Сохранение config-полей через saveConfigField, tuning-полей через saveTuningField. Зарегистрировать _dispose инпутов в disposables.
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/system/servicesModels.js && grep -q '8051' System/WebUI/static/js/panels/system/servicesModels.js && grep -q 'services.llm\|services.tts\|services.asr\|services.vlm' System/WebUI/static/js/panels/system/servicesModels.js && node -e "const s=require('fs').readFileSync('System/WebUI/static/js/panels/system/servicesModels.js','utf8'); for(const bad of ['keep_alive','reply_absolute_deadline_sec','8084']){ if(s.includes(bad)){console.error('invalid field present:',bad);process.exit(1)} } if(/[\"']riva[\"']/.test(s)){console.error('riva option present');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - SCHEMA содержит карточки agent/LLM-инфра/LLM-runtime/prompt/voice/TTS/филлер/ASR/VLM
    - keep_alive, reply_absolute_deadline_sec, hint :8084, опция riva ОТСУТСТВУЮТ
    - VLM hint содержит :8051
    - vad_onset/vad_offset встречаются по одному разу
    - Сохранение config через saveConfigField, tuning через saveTuningField
    - `node --check` проходит
  </acceptance_criteria>
  <done>Поля моделей мигрированы и очищены; VLM указывает на :8051; невалидные поля удалены.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → /api/services/{name}/{verb} | Управление systemd-юнитами (start/stop/restart) с привилегиями NOPASSWD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-11 | Elevation of Privilege | service start/stop | accept | Эндпоинт /api/services/* существует, авторизация не меняется этим планом; UI оператора в изолированной сети Jetson. NOPASSWD ограничен adam-*.service. |
| T-42-12 | Denial of Service | setInterval refresh без clearInterval | mitigate | clearInterval в teardown предотвращает накопление polling-таймеров при перемонтировании панели. |
</threat_model>

<verification>
- #/system/services открывается без ошибок; 4 service-карточки + карточки моделей
- Cosmos VLM-карточка показывает adam-vlm.service статус
- Поиск в DevTools: нет /api/live_vlm запросов
- Сохранение VLM base_url: PATCH /api/config успешен
- mount→unmount: setInterval остановлен (нет фонового polling)
</verification>

<success_criteria>
- Сервисы+Модели объединены (D-08)
- мёртвый VILA, keep_alive, riva, reply_absolute_deadline_sec, дубли vad удалены (WEBUI-R04)
- Cosmos VLM добавлен; hint :8051
</success_criteria>

<output>
After completion, create `.planning/phases/42-webui-reorganization/42-06-SUMMARY.md`
</output>
