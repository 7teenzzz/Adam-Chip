---
phase: 42-webui-reorganization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - System/WebUI/static/js/router.js
  - System/WebUI/static/js/main.js
  - System/WebUI/index.html
autonomous: true
requirements: [WEBUI-R01]
must_haves:
  truths:
    - "Хеш #/agent/persona навигирует на панель Личность без ошибок в консоли"
    - "Боковое меню показывает 3-уровневую иерархию: Чат / Настройки→Агент→{Личность,Инструкции,Память} / Система→{...} / Технофлора / Диагностика→{...}"
    - "Раскрытие/сворачивание подгрупп персистится между перезагрузками страницы"
    - "marked.parse доступна как глобальная функция в браузере"
  artifacts:
    - path: "System/WebUI/static/js/router.js"
      provides: "parseHash без split — поддержка вложенных ключей маршрутов; новый ROUTES с nested-ключами + сохранёнными dead-route записями (удаляются в Wave 4)"
      contains: "agent/persona"
    - path: "System/WebUI/static/js/main.js"
      provides: "3-уровневый NAV_STRUCTURE + buildNav() с поддержкой subgroup"
      contains: "subgroup"
    - path: "System/WebUI/index.html"
      provides: "marked.js CDN script tag + marked.setOptions"
      contains: "marked.min.js"
  key_links:
    - from: "router.js parseHash()"
      to: "ROUTES[\"agent/persona\"]"
      via: "hash.replace(/^#\\/?/, \"\") без split"
      pattern: "replace\\(/\\^#"
    - from: "main.js buildNav()"
      to: "navGroup_ localStorage keys"
      via: "subgroup collapse persistence"
      pattern: "navGroup_"
---

<objective>
Заложить фундамент 3-уровневой навигации: исправить router.js parseHash() для поддержки вложенных хеш-маршрутов (#/agent/persona), расширить NAV_STRUCTURE и buildNav() в main.js до трёх уровней, подключить marked.js (CDN) в index.html для будущих markdown-страниц.

Purpose: Все последующие волны (новые страницы Агент/Система/Диагностика) зависят от рабочего вложенного роутинга и нового nav-компонента. Без этого ни одна 3-уровневая ссылка не откроется.
Output: router.js с nested-маршрутами, main.js с 3-уровневым меню, index.html с marked.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-webui-reorganization/42-PATTERNS.md
@.planning/phases/42-webui-reorganization/42-UI-SPEC.md

<interfaces>
<!-- Текущее ROUTES (router.js строки 3-21): плоские ключи + alias-маршруты -->
<!-- parseHash() строка 29: const hash = window.location.hash.replace(/^#\/?/, "").split(/[/?]/)[0]; -->
<!--   → split на [/?] берёт ТОЛЬКО первый сегмент → #/agent/persona даёт "agent" (нет в ROUTES) → DEFAULT_ROUTE -->
<!-- activate() строка 40: await import(`./panels/${cfg.file}.js`) — cfg.file = "agent/persona" даёт import("./panels/agent/persona.js") — работает если подпапки существуют -->

<!-- Текущий NAV_STRUCTURE (main.js строки 75-94): 2 уровня. navLink(key,label,indent) строки 96-106. buildNav() строки 108+. -->
<!-- Collapse pattern: storageKey = `navGroup_${item.id}`; isOpen = localStorage.getItem(storageKey) !== "false" -->

<!-- index.html: НЕТ markdown-библиотеки (подтверждено grep). marked.js CDN нужен. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Исправить router.js parseHash() + новый ROUTES с nested-ключами</name>
  <read_first>
    - System/WebUI/static/js/router.js (текущее состояние — увидеть parseHash, ROUTES, activate)
    - .planning/phases/42-webui-reorganization/42-PATTERNS.md (раздел 1 router.js — точный новый ROUTES)
  </read_first>
  <files>System/WebUI/static/js/router.js</files>
  <action>
    1. Изменить `parseHash()` (строка 29): заменить `const hash = window.location.hash.replace(/^#\/?/, "").split(/[/?]/)[0];` на вариант БЕЗ split первого сегмента — отрезать только query-часть: `const hash = window.location.hash.replace(/^#\/?/, "").split("?")[0];`. Теперь `#/agent/persona` даёт ключ `"agent/persona"`, `#/chat` даёт `"chat"`.
    2. Расширить ROUTES новыми nested-ключами (значения file совпадают с ключом — подпапки panels/agent, panels/system, panels/diagnostics):
       - "agent/persona" → { file: "agent/persona", label: "Личность" }
       - "agent/instructions" → { file: "agent/instructions", label: "Инструкции" }
       - "agent/memory" → { file: "agent/memory", label: "Память" }
       - "system/audio" → { file: "system/audioAndVideo", label: "Аудио и видео" }
       - "system/services" → { file: "system/servicesModels", label: "Сервисы и модели" }
       - "system/esp32" → { file: "system/esp32", label: "Подсистема ESP32" }
       - "diagnostics/metrics" → { file: "diagnostics/metricsLogs", label: "Метрики и логи" }
       - "diagnostics/system" → { file: "diagnostics/systemHealth", label: "Система" }
       - "diagnostics/esp32" → { file: "diagnostics/esp32Health", label: "Подсистема" }
       Сохранить: chat, flora.
    3. ВАЖНО (D-14, миграция контента в Wave 4): НЕ удалять пока старые ключи settings/audioInput/models/tuning/services/persona/metrics/prompts/subsystem/scene/voice/camera/logs — они нужны как fallback пока новые страницы не наполнены. Удаление произойдёт в Wave 4 (план 42-08). Оставить их в ROUTES без изменений.
    4. Исправить дублирующийся ключ `prompts` (объявлен дважды — строки 12 и 20): оставить одну запись.
  </action>
  <verify>
    <automated>grep -q 'split("?")' System/WebUI/static/js/router.js && grep -q '"agent/persona"' System/WebUI/static/js/router.js && node -e "const s=require('fs').readFileSync('System/WebUI/static/js/router.js','utf8'); if(/\.split\(\/\[\/\?\]\/\)/.test(s)){console.error('parseHash still splits on [/?]');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - router.js parseHash() НЕ содержит `.split(/[/?]/)` — заменён на `.split("?")[0]`
    - ROUTES содержит ключи "agent/persona", "agent/instructions", "agent/memory", "system/audio", "system/services", "system/esp32", "diagnostics/metrics", "diagnostics/system", "diagnostics/esp32"
    - Старые ключи settings, tuning, prompts, scene, subsystem всё ещё присутствуют (удаление отложено в Wave 4)
    - Ключ prompts объявлен ровно один раз
  </acceptance_criteria>
  <done>parseHash возвращает полный nested-ключ; ROUTES имеет новые + старые маршруты; #/agent/persona разрешается в ROUTES["agent/persona"].</done>
</task>

<task type="auto">
  <name>Task 2: 3-уровневый NAV_STRUCTURE + buildNav() в main.js</name>
  <read_first>
    - System/WebUI/static/js/main.js (строки 75-160 — NAV_STRUCTURE, navLink, buildNav, collapse pattern)
    - .planning/phases/42-webui-reorganization/42-PATTERNS.md (раздел 2 main.js — новый NAV_STRUCTURE shape, subgroup storage key)
    - .planning/phases/42-webui-reorganization/42-UI-SPEC.md (раздел Component Patterns 1 — визуальная спека nav: padding-left 24px L2, 40px L3, стрелки ▸/▾)
  </read_first>
  <files>System/WebUI/static/js/main.js</files>
  <action>
    Заменить NAV_STRUCTURE на 3-уровневую структуру (точная иерархия из D-02 / UI-SPEC):
    - { key: "chat", label: "Чат" } — прямая ссылка верхнего уровня
    - { group: "Настройки", id: "settings", children: [ { subgroup: "Агент", id: "agent", children: [ {key:"agent/persona",label:"Личность"}, {key:"agent/instructions",label:"Инструкции"}, {key:"agent/memory",label:"Память"} ] } ] }
    - { group: "Система", id: "system", children: [ {key:"system/audio",label:"Аудио и видео"}, {key:"system/services",label:"Сервисы и модели"}, {key:"system/esp32",label:"Подсистема ESP32"} ] }
    - { key: "flora", label: "Технофлора" } — прямая ссылка верхнего уровня
    - { group: "Диагностика", id: "diagnostics", children: [ {key:"diagnostics/metrics",label:"Метрики и логи"}, {key:"diagnostics/system",label:"Система"}, {key:"diagnostics/esp32",label:"Подсистема"} ] }

    Расширить buildNav() для рендера типа `subgroup` внутри group.children:
    - Внутри цикла по group.children: если child имеет поле `subgroup` — рендерить вложенный collapsible-заголовок (кнопка class="nav-link" с inline `style="padding-left:24px"`, стрелка ▸/▾ цвет var(--dim)) + вложенный body-контейнер.
    - localStorage-ключ для subgroup: `navGroup_${item.id}_${child.id}` (например navGroup_settings_agent). isOpen = localStorage.getItem(key) !== "false".
    - L3-ссылки внутри subgroup: использовать navLink(key, label) но с inline `style="padding-left:40px"` (расширить navLink доп. параметром ИЛИ обернуть и переопределить padding на возвращённом узле).
    - L2-ссылки (прямые children группы без subgroup, напр. Система): navLink(child.key, child.label, true) (existing indent=24px).
    Сохранить навешивание `data-route` чтобы router.activate() корректно подсвечивал .active на leaf-ссылке.
  </action>
  <verify>
    <automated>grep -q 'subgroup' System/WebUI/static/js/main.js && grep -q 'navGroup_' System/WebUI/static/js/main.js && grep -q 'agent/persona' System/WebUI/static/js/main.js && node --check System/WebUI/static/js/main.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - NAV_STRUCTURE содержит group "Настройки" с subgroup "Агент" с 3 children (agent/persona, agent/instructions, agent/memory)
    - NAV_STRUCTURE содержит group "Система" (3 children) и group "Диагностика" (3 children)
    - "chat" и "flora" — прямые ссылки верхнего уровня (без group)
    - buildNav() обрабатывает item с полем `subgroup`, использует localStorage-ключ формата `navGroup_${parentId}_${subId}`
    - L3-ссылки имеют padding-left:40px, L2-ссылки padding-left:24px
    - `node --check System/WebUI/static/js/main.js` проходит без синтаксических ошибок
  </acceptance_criteria>
  <done>Боковое меню рендерит 3 уровня; раскрытие подгруппы Агент персистится; каждая leaf-ссылка имеет href #/{key} и data-route.</done>
</task>

<task type="auto">
  <name>Task 3: Подключить marked.js (CDN) в index.html</name>
  <read_first>
    - System/WebUI/index.html (текущие script-теги — найти точку вставки перед main.js)
    - .planning/phases/42-webui-reorganization/42-UI-SPEC.md (раздел Component Patterns 2 — Marked.js integration: setOptions breaks/gfm)
  </read_first>
  <files>System/WebUI/index.html</files>
  <action>
    1. Добавить `<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>` ПЕРЕД тегом подключения main.js (чтобы marked был доступен до загрузки панелей).
    2. Добавить однократную конфигурацию после загрузки marked: inline `<script>` с `marked.setOptions({ breaks: true, gfm: true });` (обернуть в проверку `if (window.marked)` на случай оффлайн/блокировки CDN — тогда панели Инструкции/блок-редакторы покажут raw-текст вместо рендера, но не упадут).
  </action>
  <verify>
    <automated>grep -q 'marked.min.js' System/WebUI/index.html && grep -q 'setOptions' System/WebUI/index.html && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - index.html содержит `<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js">`
    - marked.setOptions({ breaks: true, gfm: true }) вызывается с guard `if (window.marked)`
    - marked-script расположен ДО подключения main.js
  </acceptance_criteria>
  <done>window.marked.parse доступна в браузере; конфигурация breaks/gfm применена однократно.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → браузер | marked.min.js загружается с jsdelivr.net — внешний скрипт исполняется в контексте оператора |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | marked.js CDN | accept | UI оператора в изолированной сети Jetson; CDN-скрипт MIT, без runtime network calls (UI-SPEC Registry Safety review 2026-06-13). Оффлайн-guard `if (window.marked)` предотвращает падение при блокировке. |
| T-42-02 | Information Disclosure | nested hash routes | accept | Маршруты только клиентские, не несут секретов; backend-авторизация не меняется этим планом. |
</threat_model>

<verification>
- `#/agent/persona` в браузере: открывается панель (пока 404-панель если файл ещё не создан — это нормально в Wave 1; главное — роутер не падает в DEFAULT_ROUTE)
- Боковое меню показывает все 5 секций верхнего уровня
- DevTools Console: 0 ошибок при навигации по существующим маршрутам (chat, flora)
- `node --check` проходит для router.js и main.js
</verification>

<success_criteria>
- parseHash() поддерживает nested-ключи (split только по "?")
- NAV_STRUCTURE 3-уровневый, buildNav() рендерит subgroup с персистом
- marked.js подключён с оффлайн-guard
- Старые маршруты сохранены (удаление в Wave 4)
</success_criteria>

<output>
After completion, create `.planning/phases/42-webui-reorganization/42-01-SUMMARY.md`
</output>
