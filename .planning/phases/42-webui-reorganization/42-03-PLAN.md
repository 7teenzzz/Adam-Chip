---
phase: 42-webui-reorganization
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - System/WebUI/static/js/panels/agent/instructions.js
  - System/WebUI/static/js/panels/agent/memory.js
autonomous: true
requirements: [WEBUI-R03, WEBUI-R06]
must_haves:
  truths:
    - "Страница #/agent/instructions показывает System.md / Lore.md / Abilities.md как rendered markdown, кнопка «Редактировать» переключает в textarea, «Сохранить» возвращает в render"
    - "Страница #/agent/memory имеет две суб-вкладки: Базовая (пулы Echoes/Jokes/Chinese + настройки) и Дополненная (память + placeholder реестра)"
    - "Сохранение persona-файла идёт через /api/persona PUT {path, content}"
    - "Настройки памяти сохраняются в /api/tuning (episodic/weights/semantic/recent_injection/consolidator) и /api/config (echoes/chinese pool settings)"
  artifacts:
    - path: "System/WebUI/static/js/panels/agent/instructions.js"
      provides: "mount() панель Инструкции: markdown render+edit toggle для 3 persona-файлов"
      min_lines: 80
      contains: "marked"
    - path: "System/WebUI/static/js/panels/agent/memory.js"
      provides: "mount() панель Память: суб-вкладки Базовая/Дополненная, пулы + tuning-настройки памяти"
      min_lines: 120
      contains: "Базовая"
  key_links:
    - from: "agent/instructions.js"
      to: "/api/persona PUT {path, content}"
      via: "save markdown file"
      pattern: "path:.*content:"
    - from: "agent/memory.js"
      to: "/api/tuning + /api/persona GET"
      via: "memory settings + pool display"
      pattern: "/api/(tuning|persona)"
---

<objective>
Создать две новые страницы секции Агент:
- Инструкции (#/agent/instructions) — System.md / Lore.md / Abilities.md с markdown render+edit toggle (D-05). Identity.md НЕ здесь (он на Личности).
- Память (#/agent/memory) — суб-вкладки «Базовая» (пулы Echoes/Jokes/Chinese + настройки пулов) и «Дополненная» (настройки системы памяти из tuning + placeholder реестра посетителей) (D-06).

Purpose: D-05/D-06 — выделить редактирование инструкций и память в собственные страницы. Настройки памяти мигрируют из удаляемого tuning.js.
Output: panels/agent/instructions.js + panels/agent/memory.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-webui-reorganization/42-PATTERNS.md
@.planning/phases/42-webui-reorganization/42-UI-SPEC.md
@System/WebUI/static/js/panels/persona.js
@System/WebUI/static/js/panels/settings.js
@System/WebUI/static/js/panels/tuning.js

<interfaces>
<!-- /api/persona GET → { base_prompt, files: [{name, path, content}] } -->
<!-- /api/persona PUT { path: file.path, content: ta.value } — path НЕ file -->
<!-- /api/tuning GET/PUT — backing iAdam.json -->
<!-- /api/config PATCH { section, patch } -->

<!-- Persona-файлы для Инструкций: System.md, Lore.md, Abilities.md (НЕ Identity.md) -->
<!-- Пулы для Базовой памяти (через /api/persona GET, фильтр по path): -->
<!--   Agent-Adam-Chip/About/Echoes.md, Jokes.md, Chinese_lines.md -->

<!-- tuning.js SPEC-группы для миграции в Дополненную память: -->
<!--   memory.episodic (5 полей), memory.episodic.weights (6), memory.semantic (2), -->
<!--   memory.recent_injection (4), memory.consolidator (8, full-width) -->
<!-- pool settings для Базовой (источник config/tuning): echoes (tuning/echoes), chinese (tuning/chinese) -->

<!-- saveTuningField / saveConfigField / renderFieldRow / getNested / setNested — verbatim из settings.js (42-PATTERNS.md раздел 3) -->
<!-- marked.parse доступна глобально (план 42-01); guard if(window.marked) -->
<!-- buildMarkdownEditor pattern — 42-PATTERNS.md раздел 5 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: panels/agent/instructions.js — markdown render+edit для 3 persona-файлов</name>
  <read_first>
    - System/WebUI/static/js/panels/persona.js (el(), /api/persona GET/PUT format, file card pattern)
    - .planning/phases/42-webui-reorganization/42-PATTERNS.md (раздел 5 — buildMarkdownEditor toggle, No-Analog note для instructions.js)
    - .planning/phases/42-webui-reorganization/42-UI-SPEC.md (раздел Component Patterns 2 — markdown render+edit toggle, .md-render стили, кнопки Редактировать/Сохранить/Отмена)
  </read_first>
  <files>System/WebUI/static/js/panels/agent/instructions.js</files>
  <action>
    Создать panels/agent/instructions.js. el() скопировать verbatim. Импорт { api, toast } из ../../api.js.
    1. mount(target): GET /api/persona, отфильтровать files где path заканчивается на System.md / Lore.md / Abilities.md (НЕ Identity.md).
    2. Для каждого файла — buildMarkdownEditor(file, onSave) (паттерн 42-PATTERNS.md раздел 5):
       - .card с .card-header (title = file.name) + кнопка "Редактировать" (.btn, ghost-стиль).
       - View: div.md-render с innerHTML = marked.parse(content) (guard: если !window.marked → показать content в <pre class="textarea">).
       - Edit: textarea (.textarea.field-wide, min-height:240px), кнопки "Сохранить" (.btn-primary) + "Отмена" (.btn-ghost), auto-focus textarea при входе в edit.
       - Toggle isEditing: view↔edit меняет видимость и набор кнопок.
       - onSave: PUT /api/persona { path: file.path, content: textarea.value } → успех: toast + вернуть в view + обновить локальную копию content; ошибка: toast "Ошибка сохранения: {message}", остаться в edit.
    3. Loading-state до загрузки: card-body "Загрузка…".
    4. mount возвращает teardown (панель без SSE — disposables/пустой teardown).
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/agent/instructions.js && grep -q 'marked' System/WebUI/static/js/panels/agent/instructions.js && grep -q 'path:.*content\|content:.*path' System/WebUI/static/js/panels/agent/instructions.js && grep -q 'System.md\|Lore.md\|Abilities.md' System/WebUI/static/js/panels/agent/instructions.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - instructions.js экспортирует mount(target) возвращающий teardown
    - Фильтрует persona-файлы по System.md/Lore.md/Abilities.md, НЕ включает Identity.md
    - Каждый файл имеет render-view (marked.parse) и edit-textarea с toggle
    - Save использует PUT /api/persona { path, content }
    - Guard if(window.marked) присутствует (оффлайн fallback на raw)
    - `node --check` проходит
  </acceptance_criteria>
  <done>Инструкции показывают 3 файла как markdown; редактирование переключает в textarea; сохранение возвращает в render-view.</done>
</task>

<task type="auto">
  <name>Task 2: panels/agent/memory.js — суб-вкладки Базовая/Дополненная</name>
  <read_first>
    - System/WebUI/static/js/panels/settings.js (renderFieldRow, saveConfigField, saveTuningField, getNested, setNested, SCHEMA card loop)
    - System/WebUI/static/js/panels/tuning.js (SPEC-группы memory.episodic/weights/semantic/recent_injection/consolidator — точные поля)
    - System/WebUI/static/js/panels/persona.js (/api/persona GET для пулов)
    - .planning/phases/42-webui-reorganization/42-UI-SPEC.md (раздел Component Patterns 6 — sub-tab pattern; Empty State Copy для пустого пула и реестра)
  </read_first>
  <files>System/WebUI/static/js/panels/agent/memory.js</files>
  <action>
    Создать panels/agent/memory.js. el() verbatim. Импорт { api, toast }. Скопировать helpers renderFieldRow/saveConfigField/saveTuningField/getNested/setNested verbatim из settings.js (42-PATTERNS.md раздел 3).
    1. Tab-bar (UI-SPEC раздел 6): div.row с border-bottom, две кнопки-вкладки "Базовая" / "Дополненная". Активная: border-bottom 2px var(--accent), color var(--accent). Переключение = show/hide content-div (display none/block), без персиста (открывается на "Базовая").
    2. Вкладка «Базовая»:
       - GET /api/persona, отфильтровать пулы по path (Echoes.md / Jokes.md / Chinese_lines.md). Для каждого — read-only .card с превью контента (если пул пуст → empty-state "Пул пуст" / "Файл пула не содержит записей.").
       - Карточки настроек пулов через SCHEMA-паттерн: echoes (tuning/echoes), chinese (tuning/chinese) — поля cooldown/threshold/match (использовать существующие имена полей из tuning). Сохранение через saveTuningField.
    3. Вкладка «Дополненная»:
       - Карточки настроек системы памяти (SCHEMA + saveTuningField): memory.episodic (5 полей), memory.episodic.weights (6), memory.semantic (2), memory.recent_injection (4), memory.consolidator (8, .card-full). Точные поля — из tuning.js SPEC-групп.
       - Placeholder реестра посетителей: .card "Реестр посетителей" с empty-state "Данные недоступны" / "Реестр посетителей будет доступен после Phase 37." (НЕ реализовывать данные — D-06 deferred Phase 37).
    4. mount() с disposables[] (для _dispose полей-инпутов) + teardown.
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/agent/memory.js && grep -q 'Базовая' System/WebUI/static/js/panels/agent/memory.js && grep -q 'Дополненная' System/WebUI/static/js/panels/agent/memory.js && grep -q 'consolidator' System/WebUI/static/js/panels/agent/memory.js && grep -q 'Phase 37' System/WebUI/static/js/panels/agent/memory.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - memory.js экспортирует mount(target) возвращающий teardown
    - Две суб-вкладки Базовая/Дополненная переключаются по клику (display none/block)
    - Базовая показывает пулы Echoes/Jokes/Chinese (read-only) + настройки echoes/chinese
    - Дополненная содержит memory.episodic/weights/semantic/recent_injection/consolidator (tuning) + placeholder реестра с текстом "Phase 37"
    - Сохранение настроек через saveTuningField (PUT /api/tuning)
    - `node --check` проходит
  </acceptance_criteria>
  <done>Память функциональна: пулы отображаются, настройки памяти сохраняются в tuning, реестр — placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → /api/persona PUT | Контент System.md/Lore.md/Abilities.md идёт в системный промпт LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Tampering | markdown render innerHTML | accept | Контент persona-файлов доверенный (редактируется только оператором); marked.js экранирует HTML по умолчанию. Sanitize не требуется в изолированной операторской сети. |
| T-42-06 | Denial of Service | пустой пул / отсутствующий файл | mitigate | Empty-state обрабатывает пустые/отсутствующие пулы без падения панели. |
</threat_model>

<verification>
- #/agent/instructions: 3 файла рендерятся как markdown; edit→save→render работает
- #/agent/memory: вкладки переключаются; настройки consolidator сохраняются (Network подтверждает PUT /api/tuning)
- Реестр посетителей показывает placeholder-текст
- mount/unmount без утечек
</verification>

<success_criteria>
- Инструкции: markdown render+edit для System/Lore/Abilities (D-05)
- Память: Базовая + Дополненная с пулами и tuning-настройками (D-06)
- Identity.md отсутствует в Инструкциях
</success_criteria>

<output>
After completion, create `.planning/phases/42-webui-reorganization/42-03-SUMMARY.md`
</output>
