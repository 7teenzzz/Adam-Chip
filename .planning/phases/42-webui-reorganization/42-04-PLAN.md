---
phase: 42-webui-reorganization
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - System/WebUI/static/js/panels/flora.js
  - System/Orchestrator.py
autonomous: true
requirements: [WEBUI-R07]
must_haves:
  truths:
    - "Страница #/flora показывает 5 состояний (breathe/accent/attentive/think_pulse/wake_bloom) как одну компактную таблицу вместо 5 карточек"
    - "Колонка Пик имеет слайдер с max=71 (hard cap)"
    - "Колонка Скорость показывает значение + sub-label с реальным именем поля (period_ms/flash_ms/wave_period_ms)"
    - "Кнопка «Показать сейчас» сохраняет состояние в Flora.json и триггерит POST /api/flora/state"
    - "Сохранение состояний пишет в Flora.json (FloraStore), а не в мёртвую секцию flora в Config.json"
  artifacts:
    - path: "System/WebUI/static/js/panels/flora.js"
      provides: "Таблица состояний флоры (D-11) заменяет 5 карточек; peak_pct max=71"
      contains: "flora-table"
    - path: "System/Orchestrator.py"
      provides: "Эндпоинт записи flora states в Flora.json (FloraStore.apply_patch states)"
      contains: "states"
  key_links:
    - from: "flora.js таблица"
      to: "POST /api/flora/config (states write)"
      via: "FloraStore.apply_patch({states})"
      pattern: "flora/config|flora/states"
    - from: "flora.js «Показать сейчас»"
      to: "POST /api/flora/state"
      via: "trigger state на ESP32"
      pattern: "flora/state"
---

<objective>
Заменить 5 карточек состояний в flora.js на одну компактную таблицу (D-11) с колонками: Состояние | База | Пик (≤71%) | Скорость (с sub-label реального имени поля) | Вибро | Показать сейчас. Исправить hard cap peak_pct (max=71). Добавить корректный backend-путь записи состояний флоры в Flora.json (текущий flora.js пишет состояния в мёртвую секцию flora Config.json — это баг, FloraStore читает из Flora.json).

Purpose: D-11 — компактная таблица вместо карточек. Pitfall #4 (RESEARCH): flora-конфиг теперь в Flora.json через FloraStore, но эндпоинта записи states нет — только user_presets/sequences/emotion_map. Нужно добавить.
Output: flora.js (таблица состояний), Orchestrator.py (эндпоинт записи states).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-webui-reorganization/42-PATTERNS.md
@.planning/phases/42-webui-reorganization/42-UI-SPEC.md
@System/WebUI/static/js/panels/flora.js
@System/adam/flora.py

<interfaces>
<!-- ВАЖНО (Pitfall #4 подтверждён чтением кода): -->
<!--   Текущий flora.js сохраняет состояния через api.patch("/api/config", {section:"flora", patch:{states:{...}}}) -->
<!--   НО Settings.apply_patch пишет в Config.json (DEFAULT_CONFIG_PATH), а FloraStore читает из System/Flora.json. -->
<!--   Секция flora в Config.json — МЁРТВАЯ. Значит текущий save состояний НЕ доходит до hardware. -->
<!--   FloraStore.apply_patch(patch, replace_keys) + .save() пишет в Flora.json (Orchestrator.py строки 3225-3226 пример для user_presets). -->
<!--   НЕТ эндпоинта для записи states → нужно добавить. -->

<!-- STATE_SPEED (flora.js строки 149-155, keep verbatim): -->
<!--   breathe: {key:"period_ms", min:1000, max:10000}, accent:{key:"period_ms", min:300, max:3000}, -->
<!--   attentive:{key:"wave_period_ms", min:100, max:2000}, think_pulse:{key:"flash_ms", min:100, max:3000}, -->
<!--   wake_bloom:{key:"period_ms", min:1000, max:8000} -->
<!-- STATE_LABELS / STATE_KEYS (flora.js строки 158-164): breathe/accent/attentive/think_pulse/wake_bloom -->
<!-- makeSlider() (строки 22-74), makeToggle() (строки 77-105) — keep verbatim -->
<!-- «Показать сейчас» паттерн — flora.js строки 284-300 -->

<!-- Исключения состояний (Pitfall #9): -->
<!--   attentive: нет base_pct (показать —), vibro disabled -->
<!--   think_pulse: vibro = "double_pulse" (badge 2×, не checkbox) -->

<!-- HARD CAP: peak_pct slider max ДОЛЖЕН быть 71, не 100 (текущий buildStateCard строка ~199 ошибочно max:100). -->
<!--   NB: фактический Flora.json max_duty_pct=100, peak_pct состояний=100. D-11 LOCKED требует слайдер max=71. -->
<!--   Слайдер ограничивает РЕДАКТИРОВАНИЕ; при загрузке значения >71 — clamp отображения к 71 (input value не превышает max). -->

<!-- get_flora_store() + .current_dict() — текущая конфигурация флоры -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — эндпоинт записи flora states в Flora.json</name>
  <read_first>
    - System/Orchestrator.py (строки 3170-3270 — flora эндпоинты, пример user_presets: get_flora_store().apply_patch + .save())
    - System/adam/flora.py (строки 177-210 — FloraStore.apply_patch signature + save; _build_params для max_duty_pct)
  </read_first>
  <files>System/Orchestrator.py</files>
  <action>
    Добавить эндпоинт записи состояний флоры в Flora.json (рядом с /api/flora/config GET, после строки 3180):
    - `@app.post("/api/flora/config")` async def flora_config_write(payload). Body: { states: { stateKey: {base_pct, peak_pct, <speedKey>, vibro}, ... } }.
    - Реализация: `get_flora_store().apply_patch({"states": payload["states"]})` (deep-merge — НЕ replace, чтобы не затереть состояния curious/calm которых нет в UI), затем `get_flora_store().save()`.
    - Вернуть { ok: True, saved_to: str(path) }. FloraConfig.model_validate в apply_patch отклонит невалидные данные (raises ValidationError → 400).
    - Обернуть в try/except ValidationError → HTTPException(400, detail=str(exc)).
    - NB: пересоздать flora_controller-конфиг не требуется — _build_params читает get_flora_store().current_dict() заново (hot-reload, flora.py строка 531+).
  </action>
  <verify>
    <automated>grep -q 'POST.*"/api/flora/config"\|post("/api/flora/config")' System/Orchestrator.py && grep -q 'apply_patch({"states"' System/Orchestrator.py && python3 -c "import ast; ast.parse(open('System/Orchestrator.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Orchestrator.py содержит POST /api/flora/config принимающий { states }
    - Реализация вызывает get_flora_store().apply_patch({"states": ...}) (deep-merge, не replace) + .save()
    - ValidationError маппится в HTTPException 400
    - `python3 -c "ast.parse(...)"` проходит без SyntaxError
  </acceptance_criteria>
  <done>Запись состояний флоры идёт в Flora.json через FloraStore; невалидные данные отклоняются с 400.</done>
</task>

<task type="auto">
  <name>Task 2: flora.js — заменить 5 карточек на компактную таблицу состояний</name>
  <read_first>
    - System/WebUI/static/js/panels/flora.js (STATE_SPEED, STATE_LABELS, STATE_KEYS, makeSlider, makeToggle, текущий buildStateCard, «Показать сейчас» паттерн, mount/teardown)
    - .planning/phases/42-webui-reorganization/42-PATTERNS.md (раздел 4 — table row structure, peak max=71, speed sub-label)
    - .planning/phases/42-webui-reorganization/42-UI-SPEC.md (раздел Component Patterns 3 — flora-table CSS, колонки, attentive/think_pulse исключения, кнопки)
  </read_first>
  <files>System/WebUI/static/js/panels/flora.js</files>
  <action>
    Заменить рендеринг 5 карточек состояний на одну таблицу. Остальное (пресеты, секвенции, emotion_map) — НЕ трогать (D-12: keyframe-редактор deferred Phase 36B; шаговый редактор секвенций оставить как есть).
    1. Сохранить STATE_SPEED / STATE_LABELS / STATE_KEYS / makeSlider / makeToggle verbatim.
    2. Построить `<table class="flora-table">` внутри .card.card-full (стили flora-table вставить inline в flora.js через <style> или добавить в components.css — выбрать по UI-SPEC раздел 3 CSS-блок). Колонки:
       - Состояние: STATE_LABELS[key]
       - База: для breathe/accent/think_pulse/wake_bloom — слайдер base_pct (max=100); для attentive — `<span style="color:var(--dim)">—</span>` (нет base_pct).
       - Пик (≤71%): слайдер peak_pct с **max=71** (НЕ 100). Если загруженное значение >71 — input примет max (71).
       - Скорость: слайдер makeSlider(STATE_SPEED[key]) (min/max из STATE_SPEED) + sub-label `<div class="speed-label">${STATE_SPEED[key].key}</div>` (period_ms/flash_ms/wave_period_ms).
       - Вибро: для attentive — disabled checkbox; для think_pulse — read-only badge "2×" (значение "double_pulse"); для остальных — makeToggle bool.
       - Показать сейчас: `<button class="btn btn-icon btn-primary" title="Показать сейчас">▶</button>` (36×36).
    3. Сбор getValues для строки → patch { base_pct?, peak_pct, [speedKey], vibro }. vibro think_pulse сохранять как "double_pulse" если включён, иначе false.
    4. «Показать сейчас» (паттерн строки 284-300, но новый save-путь): сначала POST /api/flora/config { states: { [stateKey]: rowPatch } } → при успехе POST /api/flora/state { state: stateKey } → toast. Ошибка PATCH → toast "Ошибка сохранения конфига флоры: {message}", НЕ триггерить state.
    5. Кнопка «Сохранить все состояния» (.btn-primary) под таблицей: POST /api/flora/config { states: { все строки } } → toast.
    6. Сохранить контракт mount→teardown с disposables.
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/flora.js && grep -q 'flora-table' System/WebUI/static/js/panels/flora.js && grep -q 'max:.*71\|max="71"\|max: 71' System/WebUI/static/js/panels/flora.js && grep -q '/api/flora/config' System/WebUI/static/js/panels/flora.js && grep -q 'speed-label' System/WebUI/static/js/panels/flora.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - flora.js рендерит одну `<table class="flora-table">` с 5 строками состояний вместо 5 отдельных карточек
    - peak_pct слайдер имеет max=71 (не 100)
    - Колонка Скорость показывает sub-label с реальным именем поля (.speed-label с STATE_SPEED[key].key)
    - attentive: База показывает "—", Вибро disabled; think_pulse: Вибро как badge "2×"
    - «Показать сейчас» и «Сохранить все состояния» используют POST /api/flora/config (НЕ /api/config section flora)
    - Пресеты/секвенции/emotion_map не затронуты
    - `node --check` проходит
  </acceptance_criteria>
  <done>Состояния флоры — компактная таблица; peak ограничен 71%; сохранение доходит до Flora.json; «Показать сейчас» триггерит ESP32.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → /api/flora/config | Значения duty/period пишутся в Flora.json и идут на PCA9685 PWM (моторика/свет) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | Tampering | flora peak_pct | mitigate | Слайдер max=71 + backend _effective_max_duty (max_duty_pct × hardware_duty_ceiling_pct) клампит duty на железе независимо от UI-значения. Двойная защита. |
| T-42-08 | Elevation of Privilege | /api/flora/config write | mitigate | FloraConfig.model_validate отклоняет невалидные структуры; deep-merge (не replace) предотвращает затирание не-UI состояний. |
</threat_model>

<verification>
- #/flora: таблица из 5 строк, остальные блоки (пресеты/секвенции) на месте
- peak-слайдер не даёт задать >71%
- «Показать сейчас» для breathe: чтение Flora.json подтверждает запись states.breathe; ESP32 получает /api/flora/state (DevTools Network)
- Состояния curious_a/calm_a в Flora.json не затёрты после сохранения (deep-merge)
- mount/unmount без утечек
</verification>

<success_criteria>
- 5 карточек → 1 таблица (D-11)
- peak hard cap 71% в слайдере
- speed sub-label с реальным именем поля
- save-путь = Flora.json через POST /api/flora/config (исправлен Pitfall #4)
</success_criteria>

<output>
After completion, create `.planning/phases/42-webui-reorganization/42-04-SUMMARY.md`
</output>
