---
phase: 42-webui-reorganization
plan: 07
type: execute
wave: 3
depends_on: [01]
files_modified:
  - System/WebUI/static/js/panels/system/esp32.js
  - System/WebUI/static/js/panels/diagnostics/metricsLogs.js
  - System/WebUI/static/js/panels/diagnostics/systemHealth.js
  - System/WebUI/static/js/panels/diagnostics/esp32Health.js
  - System/WebUI/static/js/panels/metrics.js
autonomous: true
requirements: [WEBUI-R02, WEBUI-R06]
must_haves:
  truths:
    - "Страница #/system/esp32 имеет 3 суб-секции: Общие настройки (адреса+mcu/safety/sounds), Моторный слой (ручное PWM), Сенсорный слой (камера/мик/датчики)"
    - "Страница #/diagnostics/metrics показывает метрики + логи; в неё перенесён inlineList(recent_episodic) из prompts.js"
    - "Страница #/diagnostics/system — placeholder + статус питания/сервисов из subsystem.js"
    - "Страница #/diagnostics/esp32 показывает health-grid модулей ESP32 из subsystem.js"
  artifacts:
    - path: "System/WebUI/static/js/panels/system/esp32.js"
      provides: "ESP32 Общие/Моторный/Сенсорный (3.3.1/3.3.2/3.3.3)"
      min_lines: 120
      contains: "Моторный"
    - path: "System/WebUI/static/js/panels/diagnostics/esp32Health.js"
      provides: "Health-grid модулей ESP32 (5.3)"
      min_lines: 40
      contains: "moduleGrid"
    - path: "System/WebUI/static/js/panels/metrics.js"
      provides: "inlineList(recent_episodic) перенесён из prompts.js"
      contains: "recent_episodic"
  key_links:
    - from: "system/esp32.js Моторный слой"
      to: "ESP32 PWM API"
      via: "ручная команда канала"
      pattern: "mcu|pwm|channel"
    - from: "diagnostics/esp32Health.js"
      to: "/api/ui/status"
      via: "moduleGrid health данные"
      pattern: "/api/ui/status|moduleGrid"
---

<objective>
Создать страницы Подсистема ESP32 (#/system/esp32, D-09) и три страницы Диагностики (5.1/5.2/5.3, D-13), разделив subsystem.js. Перенести уникальный inlineList(recent_episodic) из prompts.js в metrics.js (D-14, перед удалением prompts.js в Wave 4). metricsLogs.js — обёртка существующего metrics+logs контента для нового маршрута.

Purpose: D-09 + D-13 + миграция WEBUI-R06. subsystem.js разделяется: адреса/конфиг → ESP32 Общие; health-grid → Диагностика 5.3; power/сервисы → Диагностика 5.2.
Output: system/esp32.js, diagnostics/{metricsLogs,systemHealth,esp32Health}.js, metrics.js (приём inlineList).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-webui-reorganization/42-PATTERNS.md
@.planning/phases/42-webui-reorganization/42-RESEARCH.md
@.planning/phases/42-webui-reorganization/42-UI-SPEC.md
@System/WebUI/static/js/panels/subsystem.js
@System/WebUI/static/js/panels/metrics.js
@System/WebUI/static/js/panels/prompts.js

<interfaces>
<!-- subsystem.js функции (42-PATTERNS.md раздел 9): -->
<!--   moduleGrid(modules) строки 37-58 — health-grid → diagnostics/esp32Health.js (5.3) -->
<!--   addrRow(label, value) строки 61-75 — адреса → system/esp32.js Общие (3.3.1) -->
<!--   configSection(title, obj) строки 91-103 — read-only конфиг → system/esp32.js Общие -->
<!--   power/сервисы секция → diagnostics/systemHealth.js (5.2) -->
<!--   MODULE_ORDER/MODULE_LABELS — для health-grid -->
<!-- /api/ui/status GET — модули ESP32 grid + адреса -->

<!-- settings.js карточки → 3.3 (RESEARCH): Модуль ESP32 (config/mcu), Безопасность и моторика (config/safety), Системные звуки (config/sounds) -->

<!-- prompts.js inlineList() (строки ~82, 42-PATTERNS.md раздел 8): -->
<!--   function inlineList(items){ if(!items||!items.length) return null; return el("div",{style:"..."},items.join(" · ")); } -->
<!--   Вставить в metrics.js promptTurnRow() ПОСЛЕ injectionsBadges div, вызов inlineList(item.recent_episodic) -->

<!-- Моторный слой 3.3.2 — НОВЫЙ контент: ручное PWM-управление каналами PCA9685. -->
<!--   ВАЖНО: уточнить существующий эндпоинт прямого управления каналом. Если нет — простой POST к mcu base_url. -->
<!--   Config.json mcu.channels: min 0, max 15, value_min 0, value_max 4095. allowed_scenes. -->

<!-- Empty-state копирайт (UI-SPEC): диагностика placeholder "Раздел в разработке"/"Содержимое появится в следующей фазе." -->
<!-- Путь импорта из panels/system|diagnostics/ → ../../api.js -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: metrics.js — принять inlineList(recent_episodic) из prompts.js</name>
  <read_first>
    - System/WebUI/static/js/panels/prompts.js (inlineList() ~строка 82 — точная реализация)
    - System/WebUI/static/js/panels/metrics.js (promptTurnRow() строки 95-124 — injectionsBadges, куда вставить)
    - .planning/phases/42-webui-reorganization/42-PATTERNS.md (раздел 8 — точка вставки)
  </read_first>
  <files>System/WebUI/static/js/panels/metrics.js</files>
  <action>
    1. Скопировать функцию inlineList(items) verbatim из prompts.js в metrics.js (на уровне модуля).
    2. В promptTurnRow() вставить вызов `inlineList(item.recent_episodic)` ПОСЛЕ div с injectionsBadges (внутри card-body). inlineList возвращает null при пустом списке — el() корректно игнорирует null-child.
    Это единственное уникальное содержимое prompts.js — после миграции prompts.js станет чистым дублем (удаляется в Wave 4).
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/metrics.js && grep -q 'function inlineList' System/WebUI/static/js/panels/metrics.js && grep -q 'recent_episodic' System/WebUI/static/js/panels/metrics.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - metrics.js содержит function inlineList(items)
    - promptTurnRow() вызывает inlineList(item.recent_episodic) после injectionsBadges
    - `node --check` проходит
  </acceptance_criteria>
  <done>Уникальный контент prompts.js перенесён в metrics.js; prompts.js готов к удалению в Wave 4.</done>
</task>

<task type="auto">
  <name>Task 2: system/esp32.js — Общие / Моторный / Сенсорный (3.3.1/3.3.2/3.3.3)</name>
  <read_first>
    - System/WebUI/static/js/panels/subsystem.js (addrRow, configSection, /api/ui/status, mount/teardown без SSE)
    - System/WebUI/static/js/panels/settings.js (renderFieldRow/saveConfigField для mcu/safety/sounds)
    - System/Config.json (mcu.channels, mcu.base_url, mcu.speaker_url, safety, sounds — точные поля)
    - .planning/phases/42-webui-reorganization/42-UI-SPEC.md (раздел 6 sub-tab pattern — переиспользовать для 3 суб-секций)
  </read_first>
  <files>System/WebUI/static/js/panels/system/esp32.js</files>
  <action>
    Создать panels/system/esp32.js. el() verbatim. Импорт { api, toast } из ../../api.js. Скопировать addrRow/configSection из subsystem.js + saveConfigField/renderFieldRow из settings.js.
    Использовать sub-tab паттерн (UI-SPEC раздел 6) для трёх суб-секций:
    1. Общие настройки (3.3.1): GET /api/ui/status → addrRow для base_url/speaker_url/camera_url; SCHEMA-карточки mcu (config/mcu: base_url, speaker_url, timeout_sec, idle_scene, flora_timeout_sec), safety (config/safety: half_duplex_mute, motor_*), sounds (config/sounds: enabled, success_path, local_output_device). Сохранение через saveConfigField.
    2. Моторный слой (3.3.2): ручное PWM-управление. Ряды каналов PCA9685 (mcu.channels: 0-15). Каждый — слайдер value 0-4095 + кнопка «Применить» → POST к ESP32 каналу. УТОЧНИТЬ эндпоинт: проверить grep по api_runtime.py/Orchestrator.py на наличие channel/pwm-эндпоинта; если есть — использовать; если нет — добавить НЕ требуется в этом плане (показать поля + кнопку, использовать ближайший существующий mcu-эндпоинт; при отсутствии — disable кнопку с подсказкой "эндпоинт ручного PWM появится позже"). НЕ выходить за рамки фронтенда без подтверждённого эндпоинта.
    3. Сенсорный слой (3.3.3): read-only статусы ESP32 камеры, микрофона, TEMT6000 (свет), PIR из /api/ui/status (moduleGrid-подобный вывод сенсорных модулей) + ESP32 mic секция (если есть в audioInput — ссылка/краткий статус).
    mount() с disposables + teardown.
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/system/esp32.js && grep -q 'Общие' System/WebUI/static/js/panels/system/esp32.js && grep -q 'Моторный' System/WebUI/static/js/panels/system/esp32.js && grep -q 'Сенсорный' System/WebUI/static/js/panels/system/esp32.js && grep -q 'config/mcu\|"mcu"\|mcu' System/WebUI/static/js/panels/system/esp32.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - esp32.js имеет 3 суб-секции: Общие настройки, Моторный слой, Сенсорный слой
    - Общие: адреса (addrRow) + mcu/safety/sounds карточки с saveConfigField
    - Моторный: PWM-каналы 0-15, value 0-4095; кнопка применения (или disabled с подсказкой если эндпоинт не подтверждён)
    - Сенсорный: read-only статусы камеры/мик/свет/PIR
    - mount возвращает teardown
    - `node --check` проходит
  </acceptance_criteria>
  <done>ESP32-страница с тремя суб-секциями; общие настройки сохраняются; моторный слой не выдумывает несуществующий эндпоинт.</done>
</task>

<task type="auto">
  <name>Task 3: Диагностика — metricsLogs.js (5.1), systemHealth.js (5.2), esp32Health.js (5.3)</name>
  <read_first>
    - System/WebUI/static/js/panels/subsystem.js (moduleGrid, MODULE_ORDER/MODULE_LABELS, power/сервисы секция)
    - System/WebUI/static/js/panels/metrics.js (mount — для re-use в metricsLogs)
    - System/WebUI/static/js/panels/logs.js (Log Viewer proxy контент)
    - .planning/phases/42-webui-reorganization/42-UI-SPEC.md (Empty State Copy — диагностика placeholder)
  </read_first>
  <files>System/WebUI/static/js/panels/diagnostics/metricsLogs.js, System/WebUI/static/js/panels/diagnostics/systemHealth.js, System/WebUI/static/js/panels/diagnostics/esp32Health.js</files>
  <action>
    1. metricsLogs.js (5.1): тонкая обёртка — импортировать и смонтировать существующий metrics.js контент (метрики 3 вкладки) + ссылку/proxy на Log Viewer (как в logs.js). Простейший вариант: re-export mount из metrics.js плюс блок логов. el() verbatim, импорт из ../../api.js. mount→teardown прокинуть.
    2. systemHealth.js (5.2): power/nvpmodel/jetson_clocks статус + systemd-сервисы (из subsystem.js power/сервисы секции). Если полный диагностический контент отсутствует — показать существующее (power-секция) + placeholder-карточку "Раздел в разработке" / "Содержимое появится в следующей фазе." (D-13 deferred — Phase 42 только перемещает существующее).
    3. esp32Health.js (5.3): moduleGrid(modules) из subsystem.js — health-grid модулей (mic/cam/pcm5102/pca9685/temt600/pir). GET /api/ui/status, рендер MODULE_ORDER через moduleGrid. Скопировать moduleGrid/dot/MODULE_ORDER/MODULE_LABELS verbatim.
    Все три: mount(target) с teardown (без SSE → пустой teardown или disposables).
  </action>
  <verify>
    <automated>for f in metricsLogs systemHealth esp32Health; do node --check System/WebUI/static/js/panels/diagnostics/$f.js || exit 1; done && grep -q 'moduleGrid' System/WebUI/static/js/panels/diagnostics/esp32Health.js && grep -q 'разработке\|следующей фазе\|nvpmodel\|jetson_clocks\|power' System/WebUI/static/js/panels/diagnostics/systemHealth.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Три файла в panels/diagnostics/ существуют, каждый экспортирует mount(target) с teardown
    - metricsLogs.js монтирует метрики + логи
    - systemHealth.js показывает power-статус или placeholder "Раздел в разработке"
    - esp32Health.js использует moduleGrid для health-grid модулей ESP32
    - все `node --check` проходят
  </acceptance_criteria>
  <done>Три диагностические страницы созданы; health-grid перенесён; placeholder для незавершённого контента.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → ESP32 PWM (Моторный слой) | Прямое управление каналами PCA9685 — физическая моторика установки |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-13 | Tampering | ручное PWM value | mitigate | value ограничен 0-4095 (mcu.channels.value_max), канал 0-15; backend safety (motor_max_duration_ms, cooldown) клампит. Если эндпоинт не подтверждён — кнопка disabled (не отправляет ничего). |
| T-42-14 | Information Disclosure | /api/ui/status health | accept | Health-данные не содержат секретов; UI оператора в изолированной сети. |
</threat_model>

<verification>
- #/system/esp32: 3 суб-секции переключаются; адреса и mcu/safety/sounds видны
- #/diagnostics/metrics: метрики+логи; recent_episodic отображается в promptTurnRow
- #/diagnostics/esp32: health-grid модулей рендерится
- #/diagnostics/system: power-статус или placeholder
- Все панели mount/unmount без ошибок консоли
</verification>

<success_criteria>
- ESP32 3.3.1/3.3.2/3.3.3 (D-09)
- Диагностика 5.1/5.2/5.3 nav-структура + перемещённый существующий контент (D-13)
- inlineList перенесён в metrics.js (WEBUI-R06, готово к удалению prompts.js)
</success_criteria>

<output>
After completion, create `.planning/phases/42-webui-reorganization/42-07-SUMMARY.md`
</output>
