---
phase: 42-webui-reorganization
plan: 08
type: execute
wave: 4
depends_on: [02, 03, 04, 05, 06, 07]
files_modified:
  - System/WebUI/static/js/router.js
  - System/WebUI/static/js/main.js
  - System/WebUI/static/js/panels/scene.js
  - System/WebUI/static/js/panels/tuning.js
  - System/WebUI/static/js/panels/prompts.js
  - System/WebUI/static/js/panels/settings.js
autonomous: true
requirements: [WEBUI-R01]
must_haves:
  truths:
    - "Файлы scene.js, tuning.js, prompts.js удалены"
    - "Маршруты scene/tuning/prompts/settings/audioInput/models/services/persona/subsystem удалены из ROUTES"
    - "Навигация к старым хешам (#/tuning, #/prompts, #/scene, #/settings) перенаправляет на DEFAULT_ROUTE без ошибок консоли"
    - "settings.js удалён или заменён redirect-stub (контент полностью мигрирован в 3.1/3.2/3.3)"
  artifacts:
    - path: "System/WebUI/static/js/router.js"
      provides: "ROUTES без мёртвых маршрутов; только новая 3-уровневая структура + chat/flora + backward-compat alias по необходимости"
      contains: "agent/persona"
  key_links:
    - from: "router.js parseHash()"
      to: "DEFAULT_ROUTE"
      via: "неизвестный хеш → fallback"
      pattern: "DEFAULT_ROUTE"
---

<objective>
Финальная очистка мёртвого кода (D-14, WEBUI-R01) ПОСЛЕ того как весь уникальный контент мигрирован и верифицирован на новых страницах (планы 02-07). Удалить файлы scene.js/tuning.js/prompts.js, удалить мёртвые маршруты из ROUTES, удалить или заменить redirect-stub'ом settings.js (контент полностью разнесён по 3.1/3.2/3.3). Финальная чистка NAV_STRUCTURE.

Purpose: WEBUI-R01 — удаление дублирующих/мёртвых страниц. Выполняется ПОСЛЕДНИМ, чтобы откат был возможен если новая страница окажется неполной.
Output: router.js/main.js без мёртвых записей; 3 удалённых файла; settings.js удалён/stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-webui-reorganization/42-PATTERNS.md
@.planning/phases/42-webui-reorganization/42-CONTEXT.md
@System/WebUI/static/js/router.js
@System/WebUI/static/js/main.js

<interfaces>
<!-- D-14 удаляемые файлы: scene.js (stub), tuning.js (контент мигрирован в 02/03), prompts.js (inlineList мигрирован в 07) -->
<!-- D-03 settings.js: полностью разобран → 3.1 (план 05) / 3.2 (план 06) / 3.3 (план 07). Нет standalone Конфигурация. -->

<!-- Старые маршруты в ROUTES (router.js) для удаления: -->
<!--   settings, audioInput, models, tuning, services, persona, metrics(?), prompts, subsystem, scene -->
<!--   ВНИМАНИЕ: metrics — старый metrics.js всё ещё нужен (его монтирует diagnostics/metricsLogs). НЕ удалять файл metrics.js. -->
<!--   audioInput.js — фикс card-full применён (план 05), контент мигрирован; файл можно удалить ИЛИ оставить (маршрут удалить). -->
<!--   persona.js (старый) — заменён agent/persona.js; старый файл удалить ИЛИ оставить, маршрут persona удалить. -->
<!--   subsystem.js — контент разнесён (план 07); маршрут subsystem удалить. -->

<!-- ПРОВЕРКА перед удалением маршрута: каждый старый маршрут должен иметь работающий новый аналог (планы 02-07 завершены). -->

<!-- parseHash() уже возвращает DEFAULT_ROUTE для неизвестных ключей (router.js строка 30) — старые хеши безопасно фоллбэчат на chat. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить мёртвые файлы panels + settings.js</name>
  <read_first>
    - System/WebUI/static/js/panels/scene.js (подтвердить что это stub без уникального контента)
    - System/WebUI/static/js/panels/tuning.js (подтвердить миграцию — makeEmotionCard deferred Phase 39, остальное в 02/03)
    - System/WebUI/static/js/panels/prompts.js (подтвердить inlineList уже в metrics.js)
    - .planning/phases/42-webui-reorganization/42-RESEARCH.md (Dead Page Content Inventory — что мигрировано)
  </read_first>
  <files>System/WebUI/static/js/panels/scene.js, System/WebUI/static/js/panels/tuning.js, System/WebUI/static/js/panels/prompts.js, System/WebUI/static/js/panels/settings.js</files>
  <action>
    ПРЕДУСЛОВИЕ: убедиться (grep), что весь уникальный контент мигрирован:
    - tuning.js AIIM/identity → проверить наличие в panels/agent/persona.js (extractSection, base_weights); memory.* → в panels/agent/memory.js (consolidator).
    - prompts.js inlineList → проверить в metrics.js (function inlineList + recent_episodic).
    - settings.js карточки → проверить распределение по system/audioAndVideo.js, system/servicesModels.js, system/esp32.js, agent/memory.js.
    Если предусловие выполнено — удалить файлы: scene.js, tuning.js, prompts.js, settings.js (через `rm`).
    ИСКЛЮЧЕНИЕ: makeEmotionCard из tuning.js deferred Phase 39 — он НЕ мигрирован и НЕ нужен в Phase 42 (см. CONTEXT deferred). Удаление tuning.js допустимо: emotion-карточка восстановится в Phase 39 из git-истории.
    НЕ удалять: metrics.js (нужен diagnostics/metricsLogs), logs.js, chat.js, flora.js, camera.js, voice.js.
    audioInput.js / persona.js (старые) / models.js / services.js / subsystem.js — оставить файлы (контент мигрирован, но удаление файлов не обязательно; маршруты удаляются в Task 2). Это снижает риск отката.
  </action>
  <verify>
    <automated>test ! -f System/WebUI/static/js/panels/scene.js && test ! -f System/WebUI/static/js/panels/tuning.js && test ! -f System/WebUI/static/js/panels/prompts.js && test ! -f System/WebUI/static/js/panels/settings.js && test -f System/WebUI/static/js/panels/metrics.js && grep -q 'function inlineList' System/WebUI/static/js/panels/metrics.js && grep -q 'base_weights' System/WebUI/static/js/panels/agent/persona.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - scene.js, tuning.js, prompts.js, settings.js НЕ существуют
    - metrics.js существует и содержит inlineList (миграция подтверждена)
    - agent/persona.js содержит base_weights (AIIM миграция подтверждена)
    - chat.js, flora.js, logs.js не удалены
  </acceptance_criteria>
  <done>Мёртвые файлы удалены; миграция уникального контента подтверждена grep'ом перед удалением.</done>
</task>

<task type="auto">
  <name>Task 2: Очистить ROUTES в router.js + финализировать NAV_STRUCTURE</name>
  <read_first>
    - System/WebUI/static/js/router.js (текущий ROUTES после плана 01 — старые + новые ключи)
    - System/WebUI/static/js/main.js (NAV_STRUCTURE — убедиться что не ссылается на удалённые маршруты)
  </read_first>
  <files>System/WebUI/static/js/router.js, System/WebUI/static/js/main.js</files>
  <action>
    1. В router.js ROUTES удалить мёртвые ключи: settings, tuning, prompts, scene, subsystem, models, services, persona, audioInput. Оставить: chat, flora, все nested-ключи (agent/*, system/*, diagnostics/*).
    2. Backward-compat: для diagnostics/metrics — старый ключ metrics всё ещё может прийти из закладок. Опционально оставить alias `metrics` → file diagnostics/metricsLogs ИЛИ просто положиться на DEFAULT_ROUTE fallback (parseHash вернёт chat для неизвестного ключа — безопасно). Выбрать: оставить минимальный alias только если есть риск сломать внешние ссылки; иначе чистый fallback.
    3. Подтвердить: parseHash() для неизвестного хеша (#/tuning) возвращает DEFAULT_ROUTE (chat) — НЕ падает.
    4. В main.js проверить, что NAV_STRUCTURE не содержит ссылок на удалённые маршруты (после плана 01 он уже 3-уровневый и чистый — только подтвердить).
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/router.js && node --check System/WebUI/static/js/main.js && node -e "const s=require('fs').readFileSync('System/WebUI/static/js/router.js','utf8'); for(const dead of ['settings:','tuning:','prompts:','scene:','subsystem:','audioInput:']){ if(new RegExp('\\\\b'+dead.replace(':','')+'\\\\s*:').test(s) && !s.includes('\"'+dead.replace(':','')+'\"')){ } } if(/\bscene\s*:/.test(s)||/\btuning\s*:/.test(s)||/\bprompts\s*:/.test(s)){console.error('dead route still in ROUTES');process.exit(1)} if(!s.includes('agent/persona')){console.error('new routes missing');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - ROUTES не содержит ключей scene, tuning, prompts, settings, subsystem, audioInput (мёртвые удалены)
    - ROUTES содержит chat, flora, agent/persona, agent/instructions, agent/memory, system/audio, system/services, system/esp32, diagnostics/metrics, diagnostics/system, diagnostics/esp32
    - parseHash() возвращает DEFAULT_ROUTE для неизвестного хеша
    - NAV_STRUCTURE не ссылается на удалённые маршруты
    - `node --check` проходит для обоих файлов
  </acceptance_criteria>
  <done>ROUTES очищен от мёртвых маршрутов; старые хеши безопасно фоллбэчат; навигация полностью на новой 3-уровневой структуре.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| старый хеш-маршрут → router | Закладки оператора на #/tuning, #/settings могут существовать |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-15 | Denial of Service | удалённый маршрут → битая навигация | mitigate | parseHash() возвращает DEFAULT_ROUTE (chat) для неизвестного ключа — старые закладки не вызывают ошибку, плавно фоллбэчат. |
| T-42-16 | Tampering | преждевременное удаление до миграции | mitigate | Wave 4 зависит от 02-07 (depends_on); Task 1 verify grep'ом подтверждает миграцию AIIM/inlineList ПЕРЕД удалением файлов. |
</threat_model>

<verification>
- Файлы scene/tuning/prompts/settings отсутствуют
- ROUTES не содержит мёртвых ключей; содержит все новые
- Навигация #/tuning → chat (DEFAULT_ROUTE), 0 ошибок консоли
- Все новые страницы (agent/*, system/*, diagnostics/*, flora) открываются
- Полный обход меню без console errors и без 404-панелей
</verification>

<success_criteria>
- 3 мёртвых файла + settings.js удалены (D-14, WEBUI-R01)
- ROUTES очищен, NAV финализирован
- старые хеши безопасно фоллбэчат
</success_criteria>

<output>
After completion, create `.planning/phases/42-webui-reorganization/42-08-SUMMARY.md`
</output>
