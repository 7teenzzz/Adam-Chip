---
phase: 42-webui-reorganization
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - System/WebUI/static/js/panels/agent/persona.js
autonomous: true
requirements: [WEBUI-R03, WEBUI-R05, WEBUI-R06]
must_haves:
  truths:
    - "Страница #/agent/persona показывает пресет-селектор, матрицу AIIM (12 аспектов 4×3) и два блок-редактора (Интенции / Голос)"
    - "Слайдеры AIIM редактируют веса/уровни; кнопка «Сохранить матрицу» пишет в /api/tuning PUT"
    - "Клик по пресету применяет его через POST /api/tuning/preset/{name} и перерисовывает слайдеры"
    - "Блок-редактор Интенции/Голос редактирует только свою секцию Identity.md и сохраняет полный файл через /api/persona PUT {path, content}"
  artifacts:
    - path: "System/WebUI/static/js/panels/agent/persona.js"
      provides: "mount() панель Личность: preset selector + AIIM matrix + Identity.md section editors"
      min_lines: 200
      contains: "extractSection"
  key_links:
    - from: "agent/persona.js"
      to: "/api/tuning"
      via: "GET веса + PUT матрица"
      pattern: "/api/tuning"
    - from: "agent/persona.js"
      to: "/api/persona PUT {path, content}"
      via: "reconstructFile(Identity.md, section, newContent)"
      pattern: "path:.*content:"
    - from: "agent/persona.js"
      to: "/api/tuning/preset/"
      via: "POST применение пресета"
      pattern: "tuning/preset/"
---

<objective>
Создать новую страницу Личность (#/agent/persona), заменяющую старый persona.js. Содержит: пресет-селектор личности, матрицу AIIM (12 аспектов с весами и уровнями), и два сфокусированных блок-редактора секций Identity.md — «Интенции» и «Голос». Identity.md НЕ показывается как textarea целиком (D-04).

Purpose: D-04 — реорганизация редактирования личности из плоских textarea в структурированную матрицу + сфокусированные секции. Контент AIIM мигрирует из удаляемого tuning.js.
Output: panels/agent/persona.js (новый файл в подпапке agent/).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-webui-reorganization/42-PATTERNS.md
@.planning/phases/42-webui-reorganization/42-UI-SPEC.md
@System/WebUI/static/js/panels/persona.js
@System/WebUI/static/js/panels/tuning.js

<interfaces>
<!-- /api/persona GET → { base_prompt, files: [{name, path, content}] } -->
<!-- /api/persona PUT body: { path: file.path, content: ta.value } — КРИТИЧНО path НЕ file -->
<!-- /api/tuning GET → полный объект включая identity.base_weights[code] (se=0.92, co=0.88, ...) -->
<!-- /api/tuning PUT → пишет полный объект обратно -->
<!-- /api/tuning/preset/{name} POST → применяет пресет (без body) -->

<!-- AIIM_ASPECTS (12): code = wi|lo|im|ho|co|em|be|sp|se|pe|me|at; каждый { code, name, plan, level, ac, or, desc } -->
<!-- PLAN_META keys: P|S|I|B|T (цвета inline: P #a78bfa, S #60a5fa, I var(--accent), B #f97316, T var(--cyan)) -->
<!-- LEVEL_LABELS: 5 меток для уровней 0..4 -->
<!-- Источник: tuning.js — makeAiimWeightsCard(data, dirtyState), makePresetCard(), AIIM_ASPECTS, PLAN_META, LEVEL_LABELS -->

<!-- Identity.md секции (Agent-Adam-Chip/About/Identity.md): -->
<!--   "## Интенции" (строка 71, контент 72-90) — РЕДАКТИРУЕМО -->
<!--   "## Голос" (строка 97, контент 98-106) — РЕДАКТИРУЕМО -->
<!--   reconstructFile должен сохранять остальной файл verbatim (код-фенсы в ## Промт-инъекция НЕ трогать) -->

<!-- extractSection / reconstructFile — точные реализации в 42-PATTERNS.md раздел 5 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AIIM section extractor + preset selector + matrix scaffold</name>
  <read_first>
    - System/WebUI/static/js/panels/tuning.js (makeAiimWeightsCard, makePresetCard, AIIM_ASPECTS, PLAN_META, LEVEL_LABELS — точные имена и структура)
    - System/WebUI/static/js/panels/persona.js (el(), api GET/PUT pattern, /api/persona PUT format)
    - .planning/phases/42-webui-reorganization/42-PATTERNS.md (раздел 5 — extractSection/reconstructFile verbatim)
    - .planning/phases/42-webui-reorganization/42-UI-SPEC.md (раздел Component Patterns 4 — AIIM matrix layout, preset selector, plan colors)
  </read_first>
  <files>System/WebUI/static/js/panels/agent/persona.js</files>
  <behavior>
    - extractSection(md, "Интенции") возвращает контент строк между "## Интенции" и следующим "## " (trim)
    - extractSection возвращает null если секция не найдена
    - reconstructFile(original, "Голос", newText) сохраняет before-секцию и after-секцию verbatim, заменяя только тело секции; код-фенсы в других секциях не искажаются
    - reconstructFile при endIdx === -1 (последняя секция) корректно дописывает в конец
  </behavior>
  <action>
    Создать файл panels/agent/persona.js. Скопировать el() DOM builder verbatim из persona.js (НЕ импортировать). Импортировать { api } и при необходимости toast из ../../api.js (учесть путь: файл в panels/agent/ → ../../api.js).
    1. Вставить extractSection() и reconstructFile() verbatim из 42-PATTERNS.md раздел 5.
    2. Скопировать AIIM_ASPECTS (12 объектов), PLAN_META (P/S/I/B/T цвета), LEVEL_LABELS (5) из tuning.js. Цвета планов — inline style, без новых CSS-переменных (UI-SPEC).
    3. makePresetCard(): .card "Пресет личности" с рядом badge-кнопок (одна на пресет из /api/tuning identity-пресетов; если список пресетов берётся из tuning-данных — взять оттуда). Клик неактивного пресета → POST /api/tuning/preset/{name} → перезагрузить tuning → перерисовать матрицу + обновить активный badge. Loading: все badge opacity:0.5 во время POST. Ошибка: toast "Не удалось применить пресет: {message}".
    4. Объявить mount(target) с disposables[] и teardown (контракт обязателен). Внутри renderAll() — GET /api/tuning и GET /api/persona, затем сборка карточек (матрица и блок-редакторы — Task 2).
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/agent/persona.js && grep -q 'extractSection' System/WebUI/static/js/panels/agent/persona.js && grep -q 'reconstructFile' System/WebUI/static/js/panels/agent/persona.js && grep -q 'tuning/preset/' System/WebUI/static/js/panels/agent/persona.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Файл panels/agent/persona.js существует, экспортирует mount(target) возвращающий teardown
    - el() определена локально (не импортирована)
    - extractSection и reconstructFile присутствуют с реализацией из PATTERNS.md
    - AIIM_ASPECTS содержит 12 аспектов с кодами wi/lo/im/ho/co/em/be/sp/se/pe/me/at
    - PLAN_META использует inline-цвета (нет новых CSS-переменных)
    - Преset-клик вызывает POST /api/tuning/preset/{name}
    - `node --check` проходит
  </acceptance_criteria>
  <done>Скелет панели с extractor, preset selector и GET-загрузкой данных готов; матрица и блок-редакторы наполняются в Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: AIIM matrix (12 слайдеров) + блок-редакторы Интенции/Голос</name>
  <read_first>
    - .planning/phases/42-webui-reorganization/42-UI-SPEC.md (раздел Component Patterns 4 — точная вёрстка ячейки аспекта: код mono dim, имя, plan-dot; weight row range 0-1 step 0.01; level row range 0-4 step 1; Identity block editors)
    - System/WebUI/static/js/panels/tuning.js (makeAiimWeightsCard — как читаются data.identity.base_weights[code] и уровни)
  </read_first>
  <files>System/WebUI/static/js/panels/agent/persona.js</files>
  <action>
    1. makeAiimMatrixCard(tuningData): .card.card-full, внутри .grid-3 (4 ряда × 3 колонки) — 12 ячеек. Каждая ячейка (border 1px var(--line), radius var(--radius-s), padding 8px 12px, bg var(--bg-2)):
       - Шапка: код аспекта (mono dim 11px) + имя (text 13px weight 500) + plan-dot (inline-цвет PLAN_META, margin-left:auto).
       - Weight row: input range min=0 max=1 step=0.01 value=tuningData.identity.base_weights[code], accent-color var(--accent), + live-значение (mono 11px accent, обновляется на input).
       - Level row: input range min=0 max=4 step=1, + метка LEVEL_LABELS[value] (11px muted, обновляется на input).
       Собирать get-функции значений для последующего сохранения.
    2. Кнопка «Сохранить матрицу» (.btn-primary) под сеткой: собирает все веса+уровни в patch identity.base_weights и соответствующее поле уровней, PUT /api/tuning (полный объект — setNested как в settings.js saveTuningField). Loading: текст "Сохраняется…", возврат "Сохранить матрицу". Ошибка: toast "Ошибка сохранения матрицы: {message}".
    3. Два блок-редактора (Интенции, Голос) под матрицей — каждый .card с toggle render/edit:
       - View-режим: marked.parse(extractSection(identityContent, name)) в div (если window.marked нет — показать raw в <pre>).
       - "Редактировать" → textarea (.textarea.field-wide, min-height:120px) с extractSection-контентом; кнопки "Сохранить" (.btn-primary) + "Отмена" (.btn-ghost).
       - Сохранение: reconstructFile(fullIdentityContent, name, textarea.value) → PUT /api/persona { path: 'Agent-Adam-Chip/About/Identity.md', content: reconstructed } → toast + возврат в view. Обновить локальную копию fullIdentityContent после успеха.
    4. Зарегистрировать любые _dispose в disposables[].
  </action>
  <verify>
    <automated>node --check System/WebUI/static/js/panels/agent/persona.js && grep -q 'base_weights' System/WebUI/static/js/panels/agent/persona.js && grep -q 'Сохранить матрицу' System/WebUI/static/js/panels/agent/persona.js && grep -q "Agent-Adam-Chip/About/Identity.md" System/WebUI/static/js/panels/agent/persona.js && grep -q 'min-height:120px\|min-height: 120px' System/WebUI/static/js/panels/agent/persona.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Матрица рендерит 12 ячеек в .grid-3 с weight (0-1) и level (0-4) слайдерами
    - Live-значения весов обновляются на событие input
    - Кнопка «Сохранить матрицу» делает PUT /api/tuning с identity.base_weights
    - Блок-редакторы Интенции и Голос используют extractSection для загрузки и reconstructFile для сохранения
    - PUT /api/persona использует body { path: 'Agent-Adam-Chip/About/Identity.md', content: ... } (path не file)
    - Textarea блок-редактора имеет min-height:120px
    - `node --check` проходит
  </acceptance_criteria>
  <done>Личность полностью функциональна: матрица сохраняется в /api/tuning, секции Identity.md сохраняются через reconstructFile без искажения остального файла.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → /api/persona PUT | Контент Identity.md записывается в системный промпт LLM — попадает в контекст Adam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Tampering | reconstructFile | mitigate | reconstructFile сохраняет before/after секции verbatim; редактируется только тело одной секции — остальной файл (включая AIIM код-фенсы) не искажается. Behavior-тест проверяет сохранность. |
| T-42-04 | Information Disclosure | Identity.md контент | accept | UI оператора в изолированной сети; редактирование персоны — штатная функция оператора. |
</threat_model>

<verification>
- #/agent/persona открывается без ошибок консоли
- AIIM-слайдеры двигаются, live-значения обновляются
- Сохранение матрицы: PATCH-ответ /api/tuning успешен (DevTools Network)
- Редактирование секции Голос → сохранение → чтение Identity.md подтверждает: изменилась ТОЛЬКО секция Голос, остальное verbatim
- mount/unmount без утечки SSE (панель без SSE — teardown пустой/disposables)
</verification>

<success_criteria>
- Страница Личность = preset selector + AIIM matrix + 2 блок-редактора (D-04)
- Identity.md НЕ показан как полный textarea
- Сохранение в /api/tuning и /api/persona работает корректно
</success_criteria>

<output>
After completion, create `.planning/phases/42-webui-reorganization/42-02-SUMMARY.md`
</output>
