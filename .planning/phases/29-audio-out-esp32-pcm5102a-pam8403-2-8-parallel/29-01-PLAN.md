---
phase: 29
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - System/Config.schema.json
  - System/Config.json
  - System/adam/tuning.py
autonomous: true
requirements:
  - AUDIO-OUT-01  # Software volume cap опущен до 1.0 на всех трёх уровнях (schema / Config.json / pydantic)
  - AUDIO-OUT-02  # Стартовый tuning.voice.volume = 0.5 (консервативный первый ramp)

must_haves:
  truths:
    - "Если orchestrator перезапускается во время Wave 1 (пайка), tuning.voice.volume не превышает 0.5 — динамики защищены даже если 5V уже подаётся"
    - "UI tuning slider (читает Config.schema.json `maximum`) больше не показывает 2.0 как валидный потолок"
    - "Pydantic-модель VoiceTuning отклоняет volume > 1.0 с ValidationError при загрузке через Settings.load()"
    - "Описание ключа volume в schema упоминает hardware chain (PCM5102A → делитель 1:6 → PAM8403 → 4Ω 1W rating) — оператор понимает, почему cap опущен"
  artifacts:
    - path: "System/Config.schema.json"
      provides: "JSON-Schema cap для tuning.voice.volume.maximum=1.0 + расширенное description"
      contains: '"maximum": 1.0'
    - path: "System/Config.json"
      provides: "Runtime-значение tuning.voice.volume=0.5"
      contains: '"volume": 0.5'
    - path: "System/adam/tuning.py"
      provides: "Pydantic-clamp volume в VoiceTuning"
      contains: "le=1.0"
  key_links:
    - from: "System/Config.schema.json (volume.maximum=1.0)"
      to: "System/adam/tuning.py (VoiceTuning.volume Field le=1.0)"
      via: "оба уровня должны быть синхронны — schema валидирует через UI/API, pydantic — через Python-загрузку"
      pattern: "le=1.0"
    - from: "System/Config.json (volume=0.5)"
      to: "Orchestrator._current_volume() (читается per-chunk)"
      via: "tuning_store.current().voice.volume hot-reload"
      pattern: "_current_volume"
---

<objective>
Wave 0 / Pre-flight safety. До любых паяльных работ — опустить software-cap громкости на всех трёх уровнях (Config.schema.json, Config.json, pydantic). Это гейт безопасности: если оператор перезапустит orchestrator во время Wave 1, динамики 1W не сгорят даже если 5V уже подано.

Purpose: RESEARCH.md §"Wave Structure Critique" Issue 1 — Wave 2 (cap) из CONTEXT.md перенесён в Wave 0 как pre-flight. Nyquist mantra "fail safe before failing fast". До этого Wave hardware-этап рискует получить runaway volume на старой схеме (1.1) если случится рестарт сервиса.
Output: Три синхронных правки + один integration-check, что Settings.load() не падает на новой схеме и pydantic-валидация enforced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-CONTEXT.md
@.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-RESEARCH.md
@System/Config.json
@System/Config.schema.json
@System/adam/tuning.py

<interfaces>
<!-- Текущее состояние (до Wave 0): -->
<!-- System/Config.schema.json:947 -->
<!--   "volume": { "type": "number", "minimum": 0, "maximum": 2.0, "default": 0.5, -->
<!--     "description": "Output volume gain applied before the WAV reaches ALSA / ESP32." } -->
<!-- System/Config.json (tuning.voice.volume): "volume": 1.1 -->
<!-- System/adam/tuning.py:145-148 -->
<!--   class VoiceTuning(BaseModel): -->
<!--       speaker: str = "eugene" -->
<!--       speed_multiplier: float = Field(1.0, ge=0.5, le=2.0) -->
<!--       volume: float = Field(1.0, ge=0, le=2.0) -->

Целевое состояние (после Wave 0):
- schema: "maximum": 1.0, description упоминает PCM5102A/делитель 1:6/PAM8403/4Ω/1W rating
- Config.json: "volume": 0.5
- tuning.py: volume: float = Field(0.5, ge=0, le=1.0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config.schema.json — понизить tuning.voice.volume.maximum 2.0 → 1.0 с расширенным description</name>
  <files>System/Config.schema.json</files>
  <action>
    Открыть System/Config.schema.json, найти строку 947 (key `volume` под `properties.tuning.properties.voice.properties.volume`). Применить две правки в одном объекте:

    1. `"maximum": 2.0` → `"maximum": 1.0`.
    2. Расширить `description` так, чтобы он явно объяснял, почему cap опущен. Новый текст (на английском — это schema, code conventions = English):

       "Output volume gain applied before the WAV reaches ALSA / ESP32. Hard-capped at 1.0 because the ESP32 audio chain (PCM5102A line-out 2.1 Vrms → resistive divider 1:6 → PAM8403 class-D × 16 gain → 2 parallel 8Ω speakers per channel = 4Ω load) drives 1W-rated drivers; volume > 1.0 risks thermal failure on long vowels. Hardware-level mitigation (divider) is paired with this software cap as defense-in-depth. See .planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-CONTEXT.md."

    Сохранить файл. JSON syntax должен остаться валидным (не сломать запятые / quoting).

    `default` оставить как есть (0.5 уже корректно). `minimum` оставить как есть (0).
  </action>
  <verify>
    <automated>jq '.properties.tuning.properties.voice.properties.volume' System/Config.schema.json | tee /tmp/29-01-volume-schema.txt; jq -e '.properties.tuning.properties.voice.properties.volume.maximum == 1.0' System/Config.schema.json && echo OK_MAX || echo FAIL_MAX; jq -r '.properties.tuning.properties.voice.properties.volume.description' System/Config.schema.json | grep -E "PCM5102A|PAM8403|1W" || echo FAIL_DESC</automated>
  </verify>
  <done>
    - `jq` извлекает объект volume с `"maximum": 1.0`
    - description содержит хотя бы один из токенов «PCM5102A» / «PAM8403» / «1W»
    - JSON-файл по-прежнему парсится (jq не падает)
  </done>
</task>

<task type="auto">
  <name>Task 2: System/adam/tuning.py — pydantic VoiceTuning.volume Field le=2.0 → le=1.0</name>
  <files>System/adam/tuning.py</files>
  <action>
    Открыть System/adam/tuning.py, найти класс `VoiceTuning` (строка 145):

    ```
    class VoiceTuning(BaseModel):
        speaker: str = "eugene"
        speed_multiplier: float = Field(1.0, ge=0.5, le=2.0)
        volume: float = Field(1.0, ge=0, le=2.0)
    ```

    Изменить ТОЛЬКО строку volume на:

    ```
    volume: float = Field(0.5, ge=0, le=1.0)
    ```

    Обоснование изменения дефолта 1.0 → 0.5: pydantic-default используется только когда Config.json не содержит ключ; Phase 29 ставит явно 0.5 в Config.json, но если кто-то случайно удалит ключ, безопасный fallback = 0.5, не 1.0. Это согласуется с consensus-инвариантом «софт-cap на 1.0 + старт на 0.5».

    `speaker`, `speed_multiplier` НЕ трогать.

    После правки убедиться, что `from System.adam.tuning import VoiceTuning` импортируется без ошибок и `VoiceTuning(volume=1.5)` теперь поднимает ValidationError.
  </action>
  <verify>
    <automated>cd /home/i17jet/Agents/Adam-Chip && PYTHONPATH=System ./.venv/bin/python -c "from adam.tuning import VoiceTuning; v = VoiceTuning(); print('default volume=', v.volume); print('default speed_multiplier=', v.speed_multiplier); assert v.volume == 0.5, f'expected 0.5, got {v.volume}'; from pydantic import ValidationError; raised = False; \nimport sys\ntry:\n    VoiceTuning(volume=1.5)\nexcept ValidationError:\n    raised = True\nassert raised, 'pydantic should reject volume=1.5'; print('OK: volume=1.5 rejected')"</automated>
  </verify>
  <done>
    - `VoiceTuning()` default = 0.5
    - `VoiceTuning(volume=1.5)` raises pydantic ValidationError
    - `VoiceTuning(volume=1.0)` accepted (boundary inclusive)
    - `VoiceTuning(volume=0.5)` accepted
  </done>
</task>

<task type="auto">
  <name>Task 3: Config.json — tuning.voice.volume 1.1 → 0.5 + integration check</name>
  <files>System/Config.json</files>
  <action>
    Открыть System/Config.json, найти `tuning.voice.volume` (текущее значение 1.1). Изменить на 0.5:

    ```
    "voice": {
      "speaker": "eugene",
      "speed_multiplier": 1.1,
      "volume": 0.5
    }
    ```

    `speaker`, `speed_multiplier` НЕ менять.

    После правки прогнать integration-check: вся загрузка конфига через `Settings.load()` должна пройти без warnings/errors, и применённое значение должно быть 0.5. Это гарантирует, что:
    - JSON синтаксически валиден
    - Pydantic-валидация принимает 0.5 (≤ 1.0)
    - Hot-reload механика `tuning_store` стартует в безопасном состоянии для следующего рестарта оркестратора

    Никаких других ключей в Config.json не трогать.
  </action>
  <verify>
    <automated>cd /home/i17jet/Agents/Adam-Chip && jq -e '.tuning.voice.volume == 0.5' System/Config.json && echo OK_CFG || echo FAIL_CFG; PYTHONPATH=System ./.venv/bin/python -c "from adam.config import Settings; s = Settings.load(); voice = s.section('tuning')['voice']; print('loaded voice=', voice); assert voice['volume'] == 0.5, f'Settings.load returned volume={voice[\"volume\"]}'; from adam.tuning import VoiceTuning; v = VoiceTuning(**voice); print('pydantic accepted volume=', v.volume)"</automated>
  </verify>
  <done>
    - `jq` подтверждает `tuning.voice.volume == 0.5` в Config.json
    - `Settings.load()` отрабатывает без exceptions
    - Pydantic VoiceTuning принимает загруженное значение
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Config.json | Оператор/UI может через `/api/config` или прямой edit задать `tuning.voice.volume`. Без cap → hardware damage на динамиках. |
| Operator → tuning.py | Code path: загрузка Config.json в pydantic-модель — последняя линия защиты против невалидного JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | UI slider / API PATCH `/api/config` устанавливает volume > 1.0 | mitigate | Schema `maximum: 1.0` блокирует через runtime API валидацию; pydantic `le=1.0` блокирует через Python загрузку. Defense-in-depth: два уровня. |
| T-29-02 | Tampering | Прямой edit Config.json с volume > 1.0 → manual restart | mitigate | Pydantic при `Settings.load()` поднимет ValidationError; orchestrator не стартует с невалидным конфигом — оператор сразу увидит ошибку. |
| T-29-03 | Repudiation | Изменение volume без аудита | accept | События `config_updated` уже логируются в events.jsonl (out of scope для Phase 29). |
</threat_model>

<verification>
- Schema maximum опущен до 1.0
- Description упоминает hardware chain
- Pydantic enforced: volume > 1.0 → ValidationError
- Config.json volume = 0.5
- `Settings.load()` загружает без exceptions
- Никаких других секций Config.json/schema не изменено (diff = только volume + description)
</verification>

<success_criteria>
- Все три уровня (schema, pydantic, Config.json) дают согласованное поведение
- Любой попытка установить volume > 1.0 через UI/API/прямой edit падает с понятной ошибкой
- Orchestrator всё ещё запускается (текущий target=jetson_hdmi, volume=0.5 — Адам станет тише на HDMI, это ожидаемо и pre-flight цена)
</success_criteria>

<output>
After completion, create `.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-01-SUMMARY.md`
</output>
