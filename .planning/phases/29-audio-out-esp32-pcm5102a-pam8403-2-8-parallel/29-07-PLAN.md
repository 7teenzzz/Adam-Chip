---
phase: 29
plan: 07
type: execute
wave: 6
depends_on:
  - 29-05
  - 29-06
files_modified:
  - .planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-VERIFICATION.md
  - docs/RUNBOOK_JETSON_EXHIBITION.md  # финальное обновление с реальным discard_window если изменился
autonomous: false
requirements:
  - AUDIO-OUT-13  # Все 10 acceptance criteria из 29-CONTEXT.md <verify> зафиксированы Pass/Fail в 29-VERIFICATION.md
  - AUDIO-OUT-14  # Phase 29 закоммичена в ветке V-S09.1-Audio_out через /commit-push (Haiku sub-agent)

must_haves:
  truths:
    - "Каждое из 10 acceptance criteria (CONTEXT.md <verify>) явно отмечено PASS или FAIL в 29-VERIFICATION.md с указанием доказательства"
    - "Если Plan 05 поменял post_tts_discard_window_ms — runbook обновлён финальным числом"
    - "Phase 29 закоммичена в ветке V-S09.1-Audio_out с понятным сообщением (через /commit-push)"
    - "29-SUMMARY.md (Wave 6 финальный артефакт) сгенерирован через /gsd-extract-learnings"
  artifacts:
    - path: ".planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-VERIFICATION.md"
      provides: "Compliance checklist для всех 10 criteria из CONTEXT.md"
      contains: "Pass/Fail per criterion"
  key_links:
    - from: "29-VERIFICATION.md"
      to: "29-CONTEXT.md <verify> checklist"
      via: "сравнение criterion-by-criterion"
      pattern: "[x] / [ ]"
    - from: "/commit-push phase-29 audio-out"
      to: "Haiku sub-agent git commit + push"
      via: "git add -p (точечные файлы, не всё) → commit с сообщением → push"
      pattern: "git commit"
---

<objective>
Wave 6 / Verify + Commit. Финальная фаза: прогнать все 10 acceptance criteria из `29-CONTEXT.md <verify>` через явный чеклист в `29-VERIFICATION.md`, обновить runbook финальным числом `post_tts_discard_window_ms` если оно поменялось в Wave 4, и зафиксировать всё в git через `/commit-push phase-29 audio-out`.

Purpose: Phase должна заканчиваться измеримым signal'ом «done» — без VERIFICATION.md и git-фиксации future agents не смогут понять, в каком состоянии оставлен audio-маршрут. Также после этой фазы — `/gsd-extract-learnings 29` для генерации SUMMARY (это запускается отдельной командой пользователя, не входит в PLAN).
Output: 29-VERIFICATION.md + git commits в ветке V-S09.1-Audio_out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-CONTEXT.md
@.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-RESEARCH.md
@.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-01-SUMMARY.md
@.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-02-SUMMARY.md
@.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-03-SUMMARY.md
@.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-04-SUMMARY.md
@.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-05-SUMMARY.md
@.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-06-SUMMARY.md

<interfaces>
<!-- 10 acceptance criteria из 29-CONTEXT.md <verify>: -->

<!-- Wave 1: -->
<!-- C1: омметр показывает 4 Ω на каждой паре +OUT_x / −OUT_x; нет КЗ минусов на GND -->
<!-- C2: PAM8403 питается без видимого спайка при включении моторов -->

<!-- Wave 3: -->
<!-- C3: curl POST :81/speaker @test_440hz.wav → HTTP 200, чистый синус -->

<!-- Wave 4: -->
<!-- C4: output_target=esp32_speaker + live /api/agent/turn → голос разборчивый -->

<!-- Wave 5: -->
<!-- C5: tuning.voice.volume=1.0 — нет клиппинга на длинных гласных -->
<!-- C6: динамики не нагреваются после 30 мин -->

<!-- Wave 6: -->
<!-- C7: 10 последних tts_finished в events.jsonl все target=esp32_speaker ok=true -->
<!-- C8: 0 asr_result в окне [tts_start, tts_end + post_tts_discard_window_ms] -->

<!-- Wave 7 (изначальное число CONTEXT.md): -->
<!-- C9: docs/RUNBOOK обновлён с секцией Аудио-маршрут -->
<!-- C10: Config.schema.json voice.volume.maximum = 1.0 + описание -->

<!-- Маппинг к новой нумерации waves (после RESEARCH critique): -->
<!-- C1, C2 → Plan 02 (Wave 1) -->
<!-- C3 → Plan 03 (Wave 2) -->
<!-- C4 → Plan 04 (Wave 3) -->
<!-- C5, C6 → Plan 04 (Wave 3 ramp) + Plan 05 (Wave 4 30-min) -->
<!-- C7 → Plan 04 (Wave 3 augmentation) + Plan 05 (Wave 4 30 min verify) -->
<!-- C8 → Plan 05 (Wave 4) -->
<!-- C9 → Plan 06 (Wave 5) -->
<!-- C10 → Plan 01 (Wave 0) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать 29-VERIFICATION.md с явным чеклистом всех 10 criteria + доказательствами из SUMMARY-файлов</name>
  <files>.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-VERIFICATION.md</files>
  <action>
    Сгенерировать `29-VERIFICATION.md` со структурой ниже. Для каждого criterion прочитать соответствующий SUMMARY и заполнить колонки «Evidence» (где зафиксировано) и «Result» (PASS/FAIL/N/A).

    Шаблон:

    ```markdown
    # Phase 29 — Verification

    **Date:** <today>
    **Branch:** V-S09.1-Audio_out
    **Source criteria:** 29-CONTEXT.md `<verify>` block (10 criteria)

    ---

    ## Acceptance Criteria Checklist

    | # | Criterion (verbatim from CONTEXT) | Wave / Plan | Evidence | Result |
    |---|------------------------------------|-------------|----------|--------|
    | C1 | Wave 1: омметр показывает 4 Ω на каждой паре +OUT_x / −OUT_x; нет КЗ минусов на GND | Wave 1 / Plan 02 | 29-02-SUMMARY.md §Омметровый журнал | PASS / FAIL |
    | C2 | Wave 1: PAM8403 питается без видимого спайка при включении моторов | Wave 1 / Plan 02 | 29-02-SUMMARY.md §Power-on test | PASS / FAIL |
    | C3 | Wave 3: curl POST :81/speaker @test_440hz.wav → HTTP 200, чистый синус без хрипа | Wave 2 / Plan 03 | 29-03-SUMMARY.md §Positive loopback | PASS / FAIL |
    | C4 | Wave 4: output_target=esp32_speaker + live /api/agent/turn → голос разборчивый | Wave 3 / Plan 04 Task 2 | 29-04-SUMMARY.md §Target flip + first live turn | PASS / FAIL |
    | C5 | Wave 5: tuning.voice.volume=1.0 — нет клиппинга на длинных гласных | Wave 3 / Plan 04 Task 3 | 29-04-SUMMARY.md §Ramp table | PASS / FAIL (если ramp остановлен раньше, фиксировать финальный safe volume) |
    | C6 | Wave 5: динамики не нагреваются после 30 мин непрерывного диалога | Wave 4 / Plan 05 Task 1 | 29-05-SUMMARY.md §Thermal touch results | PASS / FAIL |
    | C7 | Wave 6: 10 последних tts_finished в events.jsonl все target=esp32_speaker ok=true | Wave 3 augmentation + Wave 4 verify / Plan 04 Task 1 + Plan 05 Task 2 | 29-04-SUMMARY.md §Event augmentation + 29-05-SUMMARY.md §Self-echo analysis | PASS / FAIL |
    | C8 | Wave 6: 0 asr_result в окне [tts_start, tts_end + post_tts_discard_window_ms] | Wave 4 / Plan 05 Task 2 | 29-05-SUMMARY.md §Self-echo analysis (violations count) | PASS / FAIL |
    | C9 | Wave 7: docs/RUNBOOK_JETSON_EXHIBITION.md обновлён, секция «Аудио-маршрут» с failover | Wave 5 / Plan 06 | 29-06-SUMMARY.md + grep "## Audio Output Path" docs/RUNBOOK | PASS / FAIL |
    | C10 | Config.schema.json `tuning.voice.volume.maximum = 1.0`, описание обновлено | Wave 0 / Plan 01 | 29-01-SUMMARY.md + `jq '.properties.tuning.properties.voice.properties.volume.maximum' System/Config.schema.json` | PASS / FAIL |

    ## Verification Commands (для воспроизведения)

    ```bash
    # C10
    jq -e '.properties.tuning.properties.voice.properties.volume.maximum == 1.0' System/Config.schema.json
    jq -r '.properties.tuning.properties.voice.properties.volume.description' System/Config.schema.json | grep -E "PCM5102A|PAM8403|1W"

    # C7 (augmentation работает)
    tail -100 data/adam/events.jsonl | jq -c 'select(.event=="tts_finished") | .payload' | tail -10 | grep -c '"target":"esp32_speaker"'
    # ожидание: 10 (или количество turn-ов в окне)

    # C8 (no violations)
    DISCARD=$(jq '.services.asr.post_tts_discard_window_ms' System/Config.json)
    # запустить snippet из RESEARCH.md §Validation Architecture C8

    # C4 (output_target подтверждён)
    PYTHONPATH=System ./.venv/bin/python -c "from adam.config import Settings; print(Settings.load().section('services')['tts']['output_target'])"
    # ожидание: esp32_speaker

    # C9 (RUNBOOK секция)
    grep -n "## Audio Output Path" docs/RUNBOOK_JETSON_EXHIBITION.md
    ```

    ## Final state snapshot

    ```bash
    jq '{target: .services.tts.output_target, volume: .tuning.voice.volume, discard_window_ms: .services.asr.post_tts_discard_window_ms}' System/Config.json
    ```

    Записать output этой команды в SUMMARY.

    ## Open issues / future work

    - Barge-in для esp32_speaker target: deferred (требует firmware-фазы с `POST :81/api/speaker/stop`).
    - UI tuning slider refresh после смены ceiling: deferred.
    - Если C5/C6 ramp остановлен раньше volume=1.0 — это OK для V1; финальный safe volume зафиксирован.

    ## Result

    **Phase 29 status:** COMPLETE / INCOMPLETE (фиксируется по факту прохождения 10 criteria).
    ```

    Заполнить каждую строку фактическим результатом по соответствующему SUMMARY-файлу. Если какой-то SUMMARY отсутствует (например, Plan не выполнен) — фиксировать FAIL с причиной.
  </action>
  <verify>
    <automated>cd /home/i17jet/Agents/Adam-Chip && test -f .planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-VERIFICATION.md && echo OK_EXISTS || echo FAIL_MISSING; grep -cE "^\| C[0-9]+ \|" .planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-VERIFICATION.md | tee /tmp/29-07-rows.txt; test "$(cat /tmp/29-07-rows.txt)" = "10" && echo OK_10_ROWS || echo "FAIL_only_$(cat /tmp/29-07-rows.txt)_rows"</automated>
  </verify>
  <done>
    - 29-VERIFICATION.md существует
    - Содержит 10 строк-criteria (C1..C10) в чеклист-таблице
    - Каждая строка имеет Evidence pointer и Result значение
  </done>
</task>

<task type="auto">
  <name>Task 2: Если Plan 05 изменил post_tts_discard_window_ms — обновить runbook финальным числом</name>
  <files>docs/RUNBOOK_JETSON_EXHIBITION.md</files>
  <action>
    Прочитать `29-05-SUMMARY.md`. Найти финальное значение `post_tts_discard_window_ms` (либо confirmed 2500, либо bumped value).

    Если значение **изменилось с 2500** — обновить runbook (Plan 06 написал placeholder 2500 на этапе планирования):

    1. В `docs/RUNBOOK_JETSON_EXHIBITION.md` найти строку с «Default 2500» в секции «Self-echo» диагностики.
    2. Заменить на новое значение.
    3. Добавить короткое примечание: «Это значение было откалибровано в Phase 29 Wave 4 на текущем hardware; см. 29-05-SUMMARY.md».

    Если значение **осталось 2500** — пропустить этот task, никаких правок не нужно.

    Команда для проверки текущего значения:
    ```
    jq '.services.asr.post_tts_discard_window_ms' System/Config.json
    ```
  </action>
  <verify>
    <automated>cd /home/i17jet/Agents/Adam-Chip && CURR=$(jq '.services.asr.post_tts_discard_window_ms' System/Config.json); echo "current=$CURR"; if [ "$CURR" = "2500" ]; then echo SKIP_no_change; else grep -q "$CURR" docs/RUNBOOK_JETSON_EXHIBITION.md && echo OK_runbook_updated || echo FAIL_runbook_not_updated; fi</automated>
  </verify>
  <done>
    - Если discard_window не менялся (== 2500) — task skipped, никаких правок
    - Если менялся — RUNBOOK содержит новое значение
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: /commit-push phase-29 audio-out — коммит и пуш через Haiku sub-agent</name>
  <what-to-do>
    Выполнить `/commit-push phase-29 audio-out` — это GSD-скилл, который спавнит Haiku sub-agent для git операций (add → commit → push).

    Что коммитится (Claude конкретные файлы, не `git add -A`):

    - `System/Config.json` (volume → 0.5; output_target → esp32_speaker; возможно post_tts_discard_window_ms если bumped)
    - `System/Config.schema.json` (volume.maximum → 1.0 + description)
    - `System/adam/tuning.py` (VoiceTuning.volume Field le=1.0)
    - `System/Orchestrator.py` (3 emit-сайта tts_finished с target field)
    - `tests/fixtures/test_440hz_1s_-12dbfs.wav`
    - `tests/fixtures/test_negative_48000hz.wav`
    - `tests/fixtures/test_negative_stereo.wav`
    - `docs/RUNBOOK_JETSON_EXHIBITION.md`
    - `.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-01-PLAN.md` .. `29-07-PLAN.md`
    - `.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-01-SUMMARY.md` .. `29-06-SUMMARY.md`
    - `.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-VERIFICATION.md`

    **Не коммитить:**
    - `data/adam/events.jsonl` (runtime state, .gitignore)
    - `data/adam/memory.sqlite3` (runtime state)
    - Любые `/tmp/29-*` файлы (диагностика)

    Push в `V-S09.1-Audio_out` (не main; мёрж в main — отдельной командой пользователя через /gsd-code-review после Phase 29).

    Sub-agent commit-message hint:

    ```
    feat(phase-29): route Adam TTS through ESP32 PCM5102A + PAM8403 + 4Ω parallel speakers

    Wave 0: Software volume cap 2.0→1.0 (schema + pydantic + Config.json starter 0.5)
    Wave 1: Hardware analog chain assembled (1:6 divider, decoupling, BTL inviolable)
    Wave 2: Loopback smoke test through :81/speaker — clean 440Hz sine + negative reject
    Wave 3: output_target flip to esp32_speaker + tts_finished payload augmentation + volume ramp 0.5→1.0
    Wave 4: 30-min endurance — N self-echo violations, thermal touch within spec
    Wave 5: RUNBOOK §Audio Output Path with failover + diagnostics
    Wave 6: 29-VERIFICATION.md — 10/10 acceptance criteria PASS

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
    ```

    После успешного push — phase-29 закрыта. Следующий шаг (отдельной командой пользователя):
    `/gsd-extract-learnings 29` → создаёт `29-SUMMARY.md` (финальный phase-level summary, агрегирующий уроки из всех 7 plan SUMMARY).
  </what-to-do>
  <resume-signal>
    Оператор подтверждает: «Commit + push прошёл, V-S09.1-Audio_out обновлена» — или: «Конфликт / fail на commit, разбираюсь».
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local git → remote (origin) | Только LAN-doming push; никаких credentials в committed files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-15 | Information Disclosure (.env или secrets в коммите) | git add scope | mitigate | Точечные file paths в commit-инструкции для sub-agent'а (не `git add -A`); .gitignore покрывает data/ и .env. |
| T-29-16 | Repudiation (изменения без attribution) | git commit | mitigate | Sub-agent добавляет Co-Authored-By trailer. |
</threat_model>

<verification>
- 29-VERIFICATION.md существует с 10 criterion-строк
- Финальный `jq` снимок Config.json показывает консистентное состояние (output_target=esp32_speaker, volume ≤ 1.0, discard_window определён)
- `git log --oneline | head -3` показывает phase-29 commit
- `git status` после push: clean
</verification>

<success_criteria>
- Все 10 acceptance criteria из 29-CONTEXT.md `<verify>` явно отмечены PASS (или FAIL с указанной причиной → требует доработки в отдельной phase)
- Phase 29 закоммичена в ветке V-S09.1-Audio_out
- Готово к `/gsd-extract-learnings 29` для финального SUMMARY и (отдельно) `/gsd-code-review` перед мёржем в main
</success_criteria>

<output>
After completion, create `.planning/phases/29-audio-out-esp32-pcm5102a-pam8403-2-8-parallel/29-07-SUMMARY.md` с секциями: «Verification table snapshot», «Final Config.json snapshot (jq output)», «Git commit hash + branch state».
</output>
