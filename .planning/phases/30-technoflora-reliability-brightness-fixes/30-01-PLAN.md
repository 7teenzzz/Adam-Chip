---
phase: 30-technoflora-reliability-brightness-fixes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_flora.py
autonomous: true
requirements: [FFIX-02, FFIX-05, FFIX-07, FFIX-10]
must_haves:
  truths:
    - "A failing test exists for the _on_answer_end barge-in guard before the fix lands"
    - "A failing test exists for raw-PWM duty math (no gamma, pct->duty linear)"
    - "A failing test exists for the flora.max_duty_pct safe-ceiling clamp"
    - "A failing test exists for feed_speech_wav running the RMS envelope off the event loop"
  artifacts:
    - path: "tests/test_flora.py"
      provides: "Wave-0 RED tests for the Jetson-side FFIX changes"
      contains: "def test_"
  key_links:
    - from: "tests/test_flora.py"
      to: "adam.flora.FloraController"
      via: "import + direct _handle/_build_params/_envelope_to_duties drive"
      pattern: "from adam.flora import FloraController"
---

<objective>
Extend `tests/test_flora.py` with the RED (failing) tests for every Jetson-side,
unit-testable change in Phase 30: the `_on_answer_end` barge-in guard (FFIX-02),
raw-PWM duty math after the gamma removal (FFIX-05 Jetson half), the new
`flora.max_duty_pct` safe-ceiling clamp (FFIX-07 Jetson half), and the off-event-loop
RMS envelope in `feed_speech_wav` (FFIX-10).

Purpose: Nyquist gate — every Jetson change in plan 02 has an automated proof.
These tests fail now (RED) and turn green when plan 02 lands. Firmware changes
(plan 03) are the documented on-hardware manual exception, not covered here.
Output: New test functions added to the existing `tests/test_flora.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-technoflora-reliability-brightness-fixes/30-CONTEXT.md
@.planning/debug/flora-stops-on-state-change.md

<read_first>
- tests/test_flora.py  (the file being extended; reuse `_FakeMCU`, `_make_controller`, `_make_sine_wav`, `_states` helpers — do NOT duplicate them)
- System/adam/flora.py  (current FloraController: `_on_answer_end` lines 208-213, `_build_params` lines 263-314, `_envelope_to_duties` lines 369-383, `feed_speech_wav` lines 387-422 — these define the contracts the new tests assert against)
- 30-CONTEXT.md D-01/D-02 (raw PWM), D-03 (max_duty_pct), D-05 (R2 guard), D-11 (R9 to_thread)
</read_first>

<interfaces>
Existing helpers in tests/test_flora.py (reuse, do not redefine):
- `class _FakeMCU` — records `set_flora_state` calls in `.calls`, `set_channels` in `.channel_calls`, `value_max = 4095`.
- `_make_controller(mcu)` — builds `FloraController(Settings.load().section("flora"), mcu, event_log=None)`.
- `_make_sine_wav(freq, duration_s, sample_rate, amplitude)` — in-memory mono 16-bit WAV bytes.
- `_states(mcu)` — list of pushed state names.

FloraController contract under test (from System/adam/flora.py):
- `_on_answer_end()` async — currently always pushes `breathe`; FFIX-02 adds early return when `_answer_active` is False.
- `_build_params(state)` -> dict with `base_duty`/`peak_duty` as int PCA9685 duties.
- `_envelope_to_duties(levels: list[float]) -> list[int]`.
- `feed_speech_wav(wav_bytes: bytes) -> None` (sync entry; spawns task).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED tests for FFIX-02 guard and FFIX-05 raw-PWM duty math</name>
  <files>tests/test_flora.py</files>
  <read_first>
  - tests/test_flora.py (helpers + existing test style)
  - System/adam/flora.py `_on_answer_end` (208-213), `_build_params` (263-314)
  </read_first>
  <action>
  Add `test_on_answer_end_guard_skips_when_not_active`: build a controller, set
  `ctrl._answer_active = False`, run `ctrl._on_answer_end()` via `asyncio.run`, and
  assert NO `breathe` state was pushed (`_states(mcu)` does not contain "breathe" /
  `mcu.calls == []`). Add a second positive case: with `ctrl._answer_active = True`,
  `_on_answer_end()` DOES push `breathe` and clears `_answer_active`. This encodes
  D-05 (FFIX-02): a late `tts_finished` after barge-in must not overwrite the
  post-barge-in state. RED now (current code always pushes breathe).

  Add `test_raw_pwm_duty_linear_no_gamma`: assert `_build_params` maps percent to
  duty as raw linear `round(4095 * pct/100)` for a known preset. Pick a preset whose
  `base_pct`/`peak_pct` are known from Config (e.g. breathe base 17 -> 696, the value
  already in firmware kPresetDefaults) and assert `params["base_duty"] == round(4095*17/100)`
  and `params["peak_duty"] == round(4095*peak_pct/100)`. This is the Jetson half of
  FFIX-05 — confirm there is exactly ONE linear transform and no perceptual curve.
  Reference the active Config `states` via `Settings.load().section("flora")["states"]`
  so the assertion tracks Config, not hardcoded values. No fenced code in flora.py.
  </action>
  <verify>
    <automated>PYTHONPATH=System python -m pytest tests/test_flora.py::test_on_answer_end_guard_skips_when_not_active tests/test_flora.py::test_raw_pwm_duty_linear_no_gamma -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
  - Both new test functions exist in tests/test_flora.py and are collected by pytest.
  - `test_on_answer_end_guard_skips_when_not_active` asserts `mcu.calls == []` (or no "breathe") in the not-active branch.
  - `test_raw_pwm_duty_linear_no_gamma` asserts `base_duty == int(round(4095 * base_pct / 100))` using the live Config preset percentages.
  - Tests FAIL now (RED) because the guard and raw-PWM mapping are not yet implemented — failure is the expected Wave-0 state.
  </acceptance_criteria>
  <done>Two RED tests collected; they fail against current flora.py for the documented reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED tests for FFIX-07 safe-ceiling clamp and FFIX-10 off-loop envelope</name>
  <files>tests/test_flora.py</files>
  <read_first>
  - System/adam/flora.py `_envelope_to_duties` (369-383), `feed_speech_wav` (387-422), `_rms_envelope` (318-367)
  - 30-CONTEXT.md D-03 (max_duty_pct clamp on every flora write), D-11 (to_thread)
  </read_first>
  <action>
  Add `test_max_duty_pct_clamps_envelope_duties`: temporarily set a low ceiling via
  the test's flora config (build the controller with a flora section where
  `max_duty_pct` is small, e.g. 40 -> ceiling `round(4095*40/100)=1638`). Feed a LOUD
  envelope (all-1.0 levels) through `_envelope_to_duties` and assert every returned
  duty is `<= ceiling`. Drive the config by constructing `FloraController` directly
  with a patched section dict (do not mutate Config.json on disk). This is the Jetson
  half of FFIX-07/D-03 — every channel write clamps to `flora.max_duty_pct`. RED now
  (no clamp exists; peak duty can exceed the ceiling).

  Add `test_feed_speech_wav_runs_envelope_off_loop`: assert FFIX-10/D-11 — the heavy
  `_rms_envelope` computation runs via `asyncio.to_thread` rather than inline on the
  event loop. Verify behaviorally inside an async test: set `ctrl._answer_active = True`,
  monkeypatch `ctrl._rms_envelope` with a sentinel-recording wrapper, await an async
  variant of the feed path, and assert the envelope was computed without blocking the
  loop. Express the contract as: after the fix `feed_speech_wav` (or a new async helper
  it schedules) routes the envelope through a thread; assert the recorded call happened
  and that `feed_speech_wav` itself returns synchronously (`is None`) in <5 ms. RED now
  (envelope is computed inline). Keep the assertion tied to observable behavior, not
  to a specific private symbol name, so plan 02 has implementation latitude (to_thread
  vs precompute).
  </action>
  <verify>
    <automated>PYTHONPATH=System python -m pytest tests/test_flora.py::test_max_duty_pct_clamps_envelope_duties tests/test_flora.py::test_feed_speech_wav_runs_envelope_off_loop -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
  - `test_max_duty_pct_clamps_envelope_duties` builds a controller with a low `max_duty_pct` and asserts `max(_envelope_to_duties([1.0,...])) <= round(4095 * max_duty_pct / 100)`.
  - `test_feed_speech_wav_runs_envelope_off_loop` asserts the envelope computation is observed AND `feed_speech_wav` returns synchronously (no long blocking) — wording avoids pinning a private symbol so plan 02 keeps latitude.
  - Both tests FAIL now (RED): no clamp; envelope inline.
  - The existing `test_flora_config`, `test_event_mapping`, `test_rms_envelope`, etc. still pass unchanged (regression check).
  </acceptance_criteria>
  <done>Two more RED tests collected; full file still imports cleanly and prior tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → adam.flora | In-process only; `_FakeMCU` replaces network. No untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | tests/test_flora.py mutating Config.json on disk | mitigate | Tests construct FloraController with patched section dicts in-memory; never write System/Config.json. |
| T-30-02 | Denial of Service | a hung asyncio test blocking CI | accept | Tests use `asyncio.run` with bounded synthetic WAVs; no network, no real ESP. Low risk. |
</threat_model>

<verification>
PYTHONPATH=System python -m pytest tests/test_flora.py -x  — the 4 new tests are RED;
all pre-existing flora tests remain green. (RED is the expected Wave-0 outcome; they
flip to green only after plan 02.)
</verification>

<success_criteria>
- 4 new test functions added covering FFIX-02, FFIX-05 (Jetson), FFIX-07 (Jetson), FFIX-10.
- New tests fail for the documented reasons; existing tests unaffected.
- No changes to System/adam/flora.py in this plan (tests only).
</success_criteria>

<output>
After completion, create `.planning/phases/30-technoflora-reliability-brightness-fixes/30-01-SUMMARY.md`
</output>
