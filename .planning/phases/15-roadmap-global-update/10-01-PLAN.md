---
phase: 10-roadmap-global-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PLAN-10-01
  - PLAN-10-02
  - PLAN-10-03
tags:
  - planning
  - roadmap
  - documentation

must_haves:
  truths:
    - "ROADMAP.md содержит все 12 новых фаз (10A, 10B, 11, 13, 21, 14, 15, 17, 20, 19, 16, 18) после Phase 9"
    - "ROADMAP.md Backlog очищен от 6 промотированных items"
    - "ROADMAP.md содержит секцию ## Milestones с M1-M4"
    - "Phase 12 помечена как DEFERRED с обновлёнными Requires"
    - "REQUIREMENTS.md содержит все 32 новых ID в 12 секциях"
    - "STATE.md: Phase 9 помечена ✓ COMPLETE, Active Phase = Phase 10"
  artifacts:
    - path: ".planning/ROADMAP.md"
      contains: "Phase 10A"
      min_lines: 400
    - path: ".planning/REQUIREMENTS.md"
      contains: "DIPL-09"
      min_lines: 120
    - path: ".planning/STATE.md"
      contains: "Phase 10: Roadmap Global Update"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/09-next-phases-planning/09-PHASE-DRAFTS.md"
      via: "12 новых фаз скопированы из Phase 9 артефактов"
      pattern: "09-PHASE-DRAFTS"
---

<objective>
Обновить ROADMAP.md, REQUIREMENTS.md и STATE.md глобальной картой будущих фаз из Phase 9 output.

**Ключевые источники:**
- `.planning/phases/09-next-phases-planning/09-PHASE-DRAFTS.md` — §1 (P0 full drafts), §2 (P1-P3 compact), §3 (32 REQ IDs), §4 (dependency graph)
- `.planning/phases/09-next-phases-planning/09-SUMMARY.md` — инструкции Phase 10 §4.1, §4.2, §4.4

**Что НЕ трогать:** код, диплом, Config.json, всё вне `.planning/`
</objective>

<context>
@.planning/phases/10-roadmap-global-update/10-CONTEXT.md
@.planning/phases/09-next-phases-planning/09-PHASE-DRAFTS.md
@.planning/phases/09-next-phases-planning/09-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Пометить Phase 9 завершённой в ROADMAP.md и STATE.md</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    **В ROADMAP.md** найти блок Phase 9 (## Phase 9: Next-Phases Planning):
    1. Заменить все `- [ ] 09-0N-PLAN.md` на `- [x] 09-0N-PLAN.md` (4 плана: 09-01..09-04)
    2. Добавить после строки с Mode: `**Completed:** 2026-05-17 (13 фаз спроектированы, 32 REQUIREMENTS-IDs, 4 артефакта)`

    **В STATE.md** в разделе Active Phase:
    1. Изменить "Phase 9: Next-Phases Planning" → "Phase 10: Roadmap Global Update"
    2. Обновить последнюю строку фазы: `Branch: \`diploma-chapter3\`\nNext step: execute 10-01-PLAN.md`
    3. В раздел Recently completed добавить: `**Phase 9 ✓ COMPLETE (2026-05-17)** — 13 фаз спроектированы (P0/P1/P2/P3), 32 REQUIREMENTS-IDs, dependency graph. Артефакты: CANDIDATES.md, 09-PRIORITIZATION.md, 09-PHASE-DRAFTS.md, 09-SUMMARY.md`
    4. Обновить `last_updated` в frontmatter на `"2026-05-17T00:00:00.000Z"`
  </action>
  <verify>
    <automated>grep -c "\- \[x\] 09-" .planning/ROADMAP.md | grep -q "^4$" &amp;&amp; grep -q "Phase 10: Roadmap Global Update" .planning/STATE.md &amp;&amp; grep -q "Phase 9 ✓ COMPLETE" .planning/STATE.md</automated>
  </verify>
  <done>ROADMAP.md Phase 9 имеет 4 [x] плана + дату. STATE.md Active = Phase 10.</done>
</task>

<task type="auto">
  <name>Task 2: Добавить 12 новых фаз в ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Добавить 12 фаз ПОСЛЕ блока Phase 9 и ПЕРЕД блоком Phase 12 (Metrics &amp; Evaluation).

    Порядок добавления: Phase 10A, Phase 10B, Phase 11, Phase 13, Phase 21, Phase 14, Phase 15, Phase 17, Phase 20, Phase 19, Phase 16, Phase 18.

    **Источник контента:**
    - Phase 10A, 10B, 11: взять ПОЛНЫЙ контент из 09-PHASE-DRAFTS.md §1 (начиная с `## Phase 10A:` до разделителя `---`). Включая Branch, Goal, Requires, Delivers (все bullets), Requirements IDs таблицу, Mode, связь с Phase 7/8.
    - Phase 13, 21, 14, 15, 17, 20, 19, 16, 18: взять compact drafts из 09-PHASE-DRAFTS.md §2 и РАЗВЕРНУТЬ в стандартный формат:

    ```
    ## Phase N: [Name]

    **Branch:** `branch-name` [(existing|new)]

    **Goal:** [1-2 предложения из compact]

    **Requires:** [из Dependencies поля compact draft]

    **Delivers:**
    - [2-4 bullets из Goal + Suggested REQ + Mode полей compact draft]
    - Code review выполнен (для merge-фаз)
    - Тесты проходят

    **Requirements:** [PREFIX-01, PREFIX-02, ...]

    **Mode:** standard

    **Priority:** P[N]
    ```

    Каждую фазу отделить строкой `---`.

    **Конкретный формат для merge-фаз (21, 20):**
    ```
    **Delivers:**
    - Финализирован код в ветке [branch]
    - Code review пройден (/gsd-code-review)
    - Merge [branch] → main выполнен
    - Регрессионные тесты зелёные после мёржа
    ```

    **Для новых branch фаз** добавить строку: `Branch: \`branch-name\` (new — создаётся при старте фазы)`
  </action>
  <verify>
    <automated>grep -c "^## Phase 1[0-9]" .planning/ROADMAP.md | awk '{exit !($1 >= 10)}'</automated>
  </verify>
  <done>В ROADMAP.md не менее 10 новых заголовков ## Phase 1N.</done>
</task>

<task type="auto">
  <name>Task 3: Обновить Phase 12 (DEFERRED) в ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Найти блок `## Phase 12: Metrics &amp; Evaluation Framework` и:

    1. Изменить строку Requires — заменить текущий блок Requires на:
    ```
    **Requires:**
    - Phase 11 (AIIM Dynamic) завершена — без рефлексивного цикла метрики RDI/CRS не имеют источника данных
    - Phase 13 (Memory Consolidation) завершена — без консолидации метрика LMRR не имеет источника данных
    - Стабилизация активных веток: `Memory-upgrade`, `Identity-tuning`, `VLM-upgrade`, `dynamic-aiim` → merged in main
    ```

    2. Добавить после строки `**Mode:** standard (full GSD cycle)`:
    ```
    **Status:** DEFERRED — разблокируется после завершения Phase 11 + Phase 13 (оба блокируют Phase 12 независимыми метриками)
    ```
  </action>
  <verify>
    <automated>grep -q "DEFERRED" .planning/ROADMAP.md &amp;&amp; grep -q "Phase 11 (AIIM Dynamic)" .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase 12 содержит DEFERRED метку и обновлённые Requires.</done>
</task>

<task type="auto">
  <name>Task 4: Добавить секцию Milestones в ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Добавить новую секцию `## Milestones` ПЕРЕД секцией `## Backlog`:

    ```markdown
    ## Milestones

    Milestone-структура для отслеживания прогресса по логическим кластерам.
    Критерии — минимально необходимые условия для объявления milestone достигнутым.

    | Milestone | Фазы | Критерий готовности |
    |-----------|------|---------------------|
    | **M1: Diploma Defence Ready** | Phase 10A, Phase 21 | `diploma-chapter3` смёржен в `main`; `Identity-tuning` смёржен в `main` |
    | **M2: System Stable Pre-Exhibition** | Phase 10B, Phase 13, Phase 20 | Config-First refactor завершён; Memory Consolidation интегрирована; VLM-upgrade смёржен |
    | **M3: Exhibition Feature Set** | Phase 11, Phase 14, Phase 19, Phase 16, Phase 17 | AIIM Dynamic работает; Proactive Speech активна; UI Rebuild завершён |
    | **M4: Research Loop** | Phase 12, Phase 15, Phase 18 | Phase 12 разблокирована (M2+M3 complete); Neural Search активна; Structural Refactor завершён |

    *Dependency graph всех фаз: [09-PHASE-DRAFTS.md §4](phases/09-next-phases-planning/09-PHASE-DRAFTS.md)*
    ```
  </action>
  <verify>
    <automated>grep -q "^## Milestones" .planning/ROADMAP.md &amp;&amp; grep -q "M1: Diploma Defence Ready" .planning/ROADMAP.md &amp;&amp; grep -q "M4: Research Loop" .planning/ROADMAP.md</automated>
  </verify>
  <done>Секция ## Milestones присутствует в ROADMAP.md с 4 milestones.</done>
</task>

<task type="auto">
  <name>Task 5: Очистить Backlog в ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    В секции `## Backlog` удалить следующие 6 подсекций целиком (включая заголовок, все параграфы до следующего `###` или `---`):

    1. `### Memory Wave 2: Neural search` → промотирована в Phase 15
    2. `### UI: Пересборка интерфейса управления` → промотирована в Phase 16
    3. `### Remote: Удалённый доступ к Jetson` → промотирована в Phase 17
    4. `### Refactor: Структурный рефакторинг` → промотирована в Phase 18
    5. `### Proactive Speech: Спонтанная речевая инициатива` → промотирована в Phase 19
    6. `### AIIM Dynamic: Рефлексивный уровень идентичности` → промотирована в Phase 11

    После удаления, если секция Backlog стала пустой (только заголовок + примечание), заменить содержимое на:
    ```
    > Все backlog-задачи промотированы в фазы (Phase 11–21). Детали: [09-PHASE-DRAFTS.md](phases/09-next-phases-planning/09-PHASE-DRAFTS.md).
    ```
  </action>
  <verify>
    <automated>grep -qv "Memory Wave 2: Neural search" .planning/ROADMAP.md &amp;&amp; grep -qv "Proactive Speech: Спонтанная" .planning/ROADMAP.md &amp;&amp; grep -qv "AIIM Dynamic: Рефлексивный уровень" .planning/ROADMAP.md</automated>
  </verify>
  <done>Backlog не содержит ни одного из 6 промотированных items.</done>
</task>

<task type="auto">
  <name>Task 6: Добавить 32 REQUIREMENTS-IDs в REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Добавить в конец `.planning/REQUIREMENTS.md` следующий блок (взять ТОЧНЫЙ текст из `.planning/phases/09-next-phases-planning/09-PHASE-DRAFTS.md` §3 — раздел `# §3 — Aggregated REQUIREMENTS-IDs Proposal`).

    Копировать от строки `## Phase 10A — Diploma Convergence Pass (proposed)` до конца §3.

    **Точный порядок секций:**
    Phase 10A (DIPL-09..15), Phase 10B (CFG-01..04), Phase 11 (AIIM-01..04), Phase 13 (MEM-01..03), Phase 21 (ID-01..03), Phase 14 (MOOD-01..02), Phase 15 (MEMN-01..02), Phase 17 (REM-01..02), Phase 20 (VLM-01..02), Phase 19 (PROAC-01..03), Phase 16 (UI-01..04), Phase 18 (REF-01..02).

    **Перед добавлением** — добавить разделитель и header:
    ```markdown

    ---

    ## Phase 10A — Diploma Convergence Pass
    ...
    ```

    Убрать суффикс `(proposed)` из заголовков секций — это уже принятые requirements.
    Убрать вводный абзац `*Этот раздел copy-paste-ready...` перед блоком.
  </action>
  <verify>
    <automated>grep -q "DIPL-09" .planning/REQUIREMENTS.md &amp;&amp; grep -q "REF-02" .planning/REQUIREMENTS.md &amp;&amp; grep -c "^| [A-Z]*-[0-9]" .planning/REQUIREMENTS.md | awk '{exit !($1 >= 32)}'</automated>
  </verify>
  <done>REQUIREMENTS.md содержит DIPL-09..REF-02, всего не менее 32 новых IDs.</done>
</task>

</tasks>

<verification>
- `.planning/ROADMAP.md` содержит Phase 10A через Phase 21 (12 фаз) + Milestones (M1-M4) + Phase 12 DEFERRED + пустой Backlog
- `.planning/REQUIREMENTS.md` содержит все 32 IDs от DIPL-09 до REF-02
- `.planning/STATE.md` Active Phase = Phase 10, Phase 9 = ✓ COMPLETE
- ROADMAP.md Phase 9 block: 4 [x] + дата Completed
</verification>

<success_criteria>
Phase 10 executor прочитал только PLAN.md и 09-PHASE-DRAFTS.md §3, и произвёл полностью синхронизированный ROADMAP.md + REQUIREMENTS.md без потери контента.
</success_criteria>

<output>
После завершения:
- Commit через `/commit-push phase-10 roadmap-update`
- STATE.md обновить: Phase 10 → ✓ COMPLETE
- Создать 10-SUMMARY.md как финальный артефакт Phase 10
</output>
