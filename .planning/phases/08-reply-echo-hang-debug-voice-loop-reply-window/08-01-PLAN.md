---
phase: 08-reply-echo-hang-debug-voice-loop-reply-window
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - System/Config.json
  - System/Config.schema.json
  - System/adam/config.py
autonomous: true
requirements:
  - REQ-REPLY-MATCHES-LISTENING

must_haves:
  truths:
    - "D-06: В System/Config.json и System/Config.schema.json больше нет ключа services.asr.reply_absolute_deadline_sec (clean delete, без deprecation stub)"
    - "D-03: В System/Config.json и System/Config.schema.json и System/adam/config.py DEFAULT_CONFIG есть ключ services.asr.reply_silence_timeout_sec со значением 4.0 (округление от текущего reply_window_sec=3.75 до 4.0)"
    - "schema description для reply_silence_timeout_sec объясняет: это таймер silence от входа в reply до перехода в standby (когда speech_ms == 0)"
    - "reply_window_sec в Config.json и schema остаётся в покое (используется на стыке listening→reply в TTS-пайплайне; см. CONTEXT D-07: один таймер reply, дефолт 4.0 — реализуется через новое имя)"
  artifacts:
    - path: "System/Config.json"
      provides: "services.asr.reply_silence_timeout_sec=4.0; reply_absolute_deadline_sec удалён"
      contains: "reply_silence_timeout_sec"
    - path: "System/Config.schema.json"
      provides: "JSON Schema описание reply_silence_timeout_sec; блок reply_absolute_deadline_sec удалён"
      contains: "reply_silence_timeout_sec"
    - path: "System/adam/config.py"
      provides: "DEFAULT_CONFIG[\"services\"][\"asr\"] не содержит reply_absolute_deadline_sec; содержит reply_silence_timeout_sec=4.0"
      contains: "reply_silence_timeout_sec"
  key_links:
    - from: "System/Config.json"
      to: "System/adam/config.py"
      via: "DEFAULT_CONFIG fallback — должны быть согласованы (DOC-06 invariant)"
      pattern: "reply_silence_timeout_sec"
    - from: "System/Config.json"
      to: "System/Config.schema.json"
      via: "lockstep edit — каждое новое поле в Config.json получает описание в schema"
      pattern: "reply_silence_timeout_sec"
---

<objective>
Cleanup Config surface перед рефактором `_vad_loop`. Удалить устаревший двойной-таймер параметр
`services.asr.reply_absolute_deadline_sec` (per D-06 — clean delete, без deprecation stub), и
ввести новый одинокий параметр `services.asr.reply_silence_timeout_sec` (default 4.0 сек) который
будет читать рефактор в Plan 08-02. Lockstep edit Config.json + Config.schema.json + DEFAULT_CONFIG в config.py.

Purpose: Config-First архитектура (CLAUDE.md) — все runtime numbers только через Config.json+schema.
Plan 08-02 не должен трогать Config; контракт «один таймер, default 4.0 сек» фиксируется здесь.

Output: чистый Config с одним полем для reply silence timeout, готовый к чтению из Orchestrator __init__.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-reply-echo-hang-debug-voice-loop-reply-window/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@System/Config.json
@System/Config.schema.json
@System/adam/config.py

<interfaces>
<!-- Поля services.asr в Config.json после правки (canonical form): -->
- services.asr.reply_window_sec        : number  (оставлен как есть, default 3.75 — used elsewhere as soft window marker; per D-07 финальный таймер живёт в новом ключе)
- services.asr.reply_silence_timeout_sec : number, default 4.0  (NEW — единственный таймер reply→standby)
- services.asr.reply_window_expired_action : string ("standby"|"stop")  (без изменений)
- services.asr.silence_after_speech_ms : integer  (без изменений)
<!-- УДАЛЯЕТСЯ: services.asr.reply_absolute_deadline_sec -->

<!-- DEFAULT_CONFIG в System/adam/config.py (строки 101-114 сейчас): зеркало Config.json -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить reply_absolute_deadline_sec и добавить reply_silence_timeout_sec в Config.json + Config.schema.json + DEFAULT_CONFIG</name>
  <files>System/Config.json, System/Config.schema.json, System/adam/config.py</files>
  <read_first>
    - System/Config.json (lines 85-104 — блок services.asr)
    - System/Config.schema.json (lines 418-440 — schema блок services.asr.reply_*)
    - System/adam/config.py (lines 100-115 — DEFAULT_CONFIG["services"]["asr"])
    - .planning/phases/08-reply-echo-hang-debug-voice-loop-reply-window/08-CONTEXT.md (decisions D-03, D-06, D-07)
    - CLAUDE.md (§Non-obvious invariants — Config-First принцип)
  </read_first>
  <action>
    Три синхронных правки (lockstep), per CONTEXT D-06 + D-07 + D-03:

    1. System/Config.json (блок services.asr, ~строка 94):
       - УДАЛИТЬ строку `"reply_absolute_deadline_sec": 7.5,` целиком.
       - ДОБАВИТЬ после `"reply_window_sec": 3.75,` новую строку:
         `"reply_silence_timeout_sec": 4.0,`
       - Не трогать reply_window_sec (оставляем как soft marker, per D-07 — выбор отдельного нового имени для нового таймера).

    2. System/Config.schema.json (блок services.asr.properties, ~строки 422-431):
       - УДАЛИТЬ JSON Schema блок `"reply_absolute_deadline_sec": { ... }` целиком (4 строки).
       - ДОБАВИТЬ после блока `"reply_window_sec"` новый блок:
         "reply_silence_timeout_sec": {
           "type": "number",
           "minimum": 0.5,
           "maximum": 30.0,
           "description": "Phase 8 (REQ-REPLY-MATCHES-LISTENING): single silence timer for reply mode. From the moment voice_state transitions to 'reply', if no qualifying speech has accumulated (speech_ms == 0) for this many seconds, voice loop returns to standby. Replaces the legacy two-timer pair (reply_window_sec + reply_absolute_deadline_sec). Runaway dictation in reply mode is guarded by media.audio.max_command_segment_ms — same as listening — per CONTEXT D-02.",
           "default": 4.0
         }
       - Description обязательно на английском (project convention).

    3. System/adam/config.py (DEFAULT_CONFIG["services"]["asr"], ~строки 107-108):
       - УДАЛИТЬ строку `"reply_absolute_deadline_sec": 7.5,`.
       - ДОБАВИТЬ после `"reply_window_sec": 3.75,` строку:
         `"reply_silence_timeout_sec": 4.0,`
       - Это держит DEFAULT_CONFIG в синхроне с Config.json (DOC-06 invariant).

    NO Python code in Orchestrator.py touched in this plan — это в 08-02.
    NO migration / deprecation stub per CLAUDE.md «no backwards-compat hacks» — clean delete (D-06).
  </action>
  <verify>
    <automated>python3 -c "import json; cfg=json.load(open('System/Config.json')); asr=cfg['services']['asr']; assert 'reply_absolute_deadline_sec' not in asr, 'reply_absolute_deadline_sec must be removed from Config.json'; assert asr.get('reply_silence_timeout_sec') == 4.0, f'reply_silence_timeout_sec must be 4.0, got {asr.get(\"reply_silence_timeout_sec\")}'; sch=json.load(open('System/Config.schema.json')); props=sch['properties']['services']['properties']['asr']['properties']; assert 'reply_absolute_deadline_sec' not in props, 'schema must not contain reply_absolute_deadline_sec'; assert props['reply_silence_timeout_sec']['default'] == 4.0, 'schema default must be 4.0'; print('OK')"</automated>
  </verify>
  <done>
    - `python3 -m json.tool System/Config.json &gt;/dev/null` — JSON валиден
    - `python3 -m json.tool System/Config.schema.json &gt;/dev/null` — JSON валиден
    - `grep -c reply_absolute_deadline_sec System/Config.json System/Config.schema.json System/adam/config.py` — все три файла возвращают 0
    - `grep -c reply_silence_timeout_sec System/Config.json System/Config.schema.json System/adam/config.py` — все три файла возвращают ≥ 1
    - Automated verify command выше печатает `OK`
  </done>
</task>

</tasks>

<verification>
- Все три файла валидны как JSON / Python (импорт не падает: `python3 -c "from System.adam.config import DEFAULT_CONFIG; print(DEFAULT_CONFIG['services']['asr']['reply_silence_timeout_sec'])"` печатает `4.0`).
- Lockstep: `grep -c reply_silence_timeout_sec` возвращает 1 в Config.json, 1 в Config.schema.json (внутри блока properties), 1 в config.py.
- `reply_absolute_deadline_sec` исчез из всех трёх файлов (grep -c = 0).
- Orchestrator.py не модифицирован в этом плане (изменения произойдут в 08-02).
</verification>

<success_criteria>
- Config.json содержит `reply_silence_timeout_sec: 4.0`, не содержит `reply_absolute_deadline_sec`.
- Config.schema.json содержит schema-блок для `reply_silence_timeout_sec` с default 4.0 и English description, не содержит `reply_absolute_deadline_sec`.
- DEFAULT_CONFIG в config.py синхронен с Config.json (DOC-06 invariant сохранён).
- Никаких изменений в Orchestrator.py — следующий плагин (08-02) подхватит новое поле.
</success_criteria>

<output>
После завершения создать `.planning/phases/08-reply-echo-hang-debug-voice-loop-reply-window/08-01-SUMMARY.md`
с краткой записью: что добавлено, что удалено, какие файлы тронуты, коммит-hash.
</output>
