---
phase: 08-reply-echo-hang-debug-voice-loop-reply-window
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - System/Orchestrator.py
autonomous: true
requirements:
  - REQ-DIAGNOSTIC-LOGS-VOICE-STATE
  - REQ-NO-HANG-AFTER-REPLY

must_haves:
  truths:
    - "D-09: _vad_loop эмитит событие voice_loop_heartbeat не чаще чем раз в 5 секунд (rate-limited); это второй элемент пары диагностических событий вместе с уже существующим voice_state_change"
    - "D-09: voice_loop_heartbeat несёт поля: state (текущий voice_state), iter (счётчик итераций цикла), uptime_sec (perf_counter с момента старта)"
    - "Heartbeat НЕ замедляет цикл — проверка по time.perf_counter(), не sleep"
    - "Существующее событие voice_state_change (уже эмитится в _set_voice_state, Orchestrator.py:565) НЕ переписывается — оно покрывает D-09 (voice_state_changed) частично; имя сохраняется для совместимости с UI/log парсерами"
    - "Heartbeat-period = 5.0 sec; значение константа в коде (не Config — diagnostic detail, не tuning)"
    - "Heartbeat работает во ВСЕХ voice_state (boot_warmup, standby, listening, reply) — это маркер живости loop, не только активных состояний"
    - "D-08: При повторении hang (который перманентный — не self-healing, оператор выключал оркестратор вручную) отсутствие heartbeat > 5 sec в events.jsonl сигнализирует точное место (последний heartbeat покажет state и iter)"
    - "D-10: SIGUSR1 → asyncio task stack dump НЕ добавляется в этом плане; deferred в follow-up фазу если рефактор + heartbeat не устранят hang"
  artifacts:
    - path: "System/Orchestrator.py"
      provides: "voice_loop_heartbeat event emission inside _vad_loop while-iteration"
      contains: "voice_loop_heartbeat"
  key_links:
    - from: "_vad_loop while True top"
      to: "event_log.append('voice_loop_heartbeat', ...)"
      via: "rate-limited via _last_heartbeat_ts"
      pattern: "voice_loop_heartbeat"
---

<objective>
Добавить периодический heartbeat event в `_vad_loop`, чтобы при повторении 6-минутного hang (CONTEXT D-08) было видно ТОЧНО когда и в каком состоянии loop перестал жить.

Контекст:
- Симптом из тестовой сессии 2026-05-17 — после `reply_window_expired absolute_deadline` events.jsonl замолкает на 6 минут (D-08: hang перманентный, пользователь выключил оркестратор вручную). Diagnose'a постфактум нет — нечем доказать что зависло, кроме «events нет».
- CONTEXT D-09 требует `voice_state_changed` и `voice_loop_heartbeat`. `voice_state_change` (similar name) уже эмитится в `_set_voice_state` Orchestrator.py:565 — НЕ ДУБЛИРОВАТЬ. Этот план добавляет ТОЛЬКО heartbeat.
- CONTEXT D-10 явно откладывает SIGUSR1 task stack dump в follow-up фазу. SIGUSR1 НЕ ДОБАВЛЯТЬ в этом плане.

После этого плана: если loop зависнет — последний heartbeat в events.jsonl покажет последнее живое состояние (state + iter), а отсутствие новых heartbeat за >5 sec будет четким сигналом о зависании. Это инфраструктура для будущей диагностики, не сам фикс hang.

Wave 2, depends_on 08-01 — может выполняться параллельно с 08-02 (тоже Wave 2, depends_on 08-01), потому что они правят разные регионы Orchestrator.py:
- 08-02 — `__init__` (~370-425) и reply/accumulation блоки в `_vad_loop` (~898-988).
- 08-03 — top-of-loop в `_vad_loop` (~819-820, перед `while self.running:` и в начале итерации).

Однако из-за того что оба плана модифицируют один и тот же файл (`System/Orchestrator.py`), при параллельном выполнении возможны merge-конфликты на уровне коммитов. Executor должен выполнять их **последовательно** (08-02 → 08-03) или один из них переоткрывать на свежем HEAD; поэтому wave один, но depends_on у 08-03 включает 08-02 для упрощения:
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-reply-echo-hang-debug-voice-loop-reply-window/08-CONTEXT.md
@System/Orchestrator.py

<interfaces>
**Текущее воркфлоу events:**

`event_log.append(name: str, payload: dict, turn_id: str | None = None)` — единая точка эмиссии. Использовать существующий канал, не вводить новый. Wallclock timestamp event_log добавляет сам; в payload `voice_loop_heartbeat` добавим relative `uptime_sec` через `time.perf_counter()` — это полезнее для измерения интервалов между heartbeat при анализе hang.

**Текущее `_vad_loop` (System/Orchestrator.py:806-820):**

```python
async def _vad_loop(self, read_fn: Callable[[int], bytes] | None, frame_bytes: int) -> None:
    """VAD + endpointing + ASR dispatch.
    ...docstring...
    """
    speech_frames: list[bytes] = []
    speech_ms = 0
    silence_ms = 0
    _reader = [read_fn]
    _empty_streak = 0
    _was_endpointing = False
    try:
        while self.running:
            if self.mic_reader is not None:
                chunk = await self.mic_reader.get_chunk(timeout=1.0)
                ...
```

Целевое: между `_was_endpointing = False` (строка 818) и `try:` (819) добавить инициализацию `_last_heartbeat_ts` и `_loop_iter_count`. Внутри `while self.running:` (строка 820, в начале каждой итерации, до `if self.mic_reader is not None:`) — инкрементировать счётчик и условно эмитить heartbeat.

**Существующее `voice_state_change` event (Orchestrator.py:565):**
```python
event_log.append("voice_state_change", {
    "from": self._voice_state, "to": state, "reason": reason,
})
```
Это покрывает D-09 (transition logging). НЕ переписывать. План 08-03 добавляет ТОЛЬКО heartbeat.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить voice_loop_heartbeat event в _vad_loop (rate-limited 5 sec)</name>
  <files>System/Orchestrator.py</files>
  <read_first>
    - System/Orchestrator.py (lines 806-830 — начало _vad_loop с инициализацией локальных переменных)
    - System/Orchestrator.py (lines 563-573 — _set_voice_state для понимания паттерна voice_state_change)
    - .planning/phases/08-reply-echo-hang-debug-voice-loop-reply-window/08-CONTEXT.md (D-08, D-09, D-10)
  </read_first>
  <action>
    В методе `_vad_loop`:

    1. После строки `_was_endpointing = False` (текущая 818) и перед `try:` (819), добавить инициализацию:
       ```
       _last_heartbeat_ts = 0.0  # perf_counter() при последнем heartbeat
       _loop_iter_count = 0      # счётчик итераций для diagnostics
       _heartbeat_period_sec = 5.0  # фиксированная константа (не Config — diagnostic detail)
       ```

    2. Сразу после `while self.running:` (текущая 820) и перед `if self.mic_reader is not None:` (821), добавить:
       ```
       _loop_iter_count += 1
       _now = time.perf_counter()
       if _now - _last_heartbeat_ts >= _heartbeat_period_sec:
           event_log.append("voice_loop_heartbeat", {
               "state": self._voice_state,
               "iter": _loop_iter_count,
               "uptime_sec": round(_now, 2),
               "vad_state": self.vad_state,
           })
           _last_heartbeat_ts = _now
       ```

    Heartbeat должен:
    - Эмититься во ВСЕХ voice_state (это маркер живости, не только активных).
    - Не вызывать `await` (только event_log.append — это синхронный вызов).
    - Не блокировать loop sleeping — проверка через perf_counter, не sleep.
    - Использовать существующий event_log канал (не вводить новый поток или файл).

    NO Config keys for `_heartbeat_period_sec` (D-10 stance: SIGUSR1/dump → follow-up phase; period — diagnostic detail not tuning).
  </action>
  <verify>
    <automated>python3 -c "
src = open('System/Orchestrator.py').read()
assert 'voice_loop_heartbeat' in src, 'voice_loop_heartbeat event missing'
assert '_last_heartbeat_ts' in src, '_last_heartbeat_ts variable missing'
assert '_loop_iter_count' in src, '_loop_iter_count variable missing'
assert '_heartbeat_period_sec = 5.0' in src, '_heartbeat_period_sec constant missing or wrong default'
# Heartbeat event должен эмититься внутри _vad_loop, а не в _set_voice_state
# (косвенно: воркфлоу - должен быть рядом с while self.running)
idx_while = src.find('while self.running:')
idx_hb = src.find('voice_loop_heartbeat')
assert idx_while > 0 and idx_hb > 0 and idx_hb > idx_while, 'voice_loop_heartbeat must appear inside while-loop'
import ast; ast.parse(src)
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "voice_loop_heartbeat" System/Orchestrator.py` — ровно 1 (один call site)
    - `grep -n "_heartbeat_period_sec = 5.0" System/Orchestrator.py` — ровно 1 (init константы)
    - `grep -n "_loop_iter_count" System/Orchestrator.py` — ≥ 2 (init + increment)
    - `grep -n "_last_heartbeat_ts" System/Orchestrator.py` — ≥ 3 (init + read + write)
    - `python3 -c "import ast; ast.parse(open('System/Orchestrator.py').read())"` exits 0
    - `python3 -c "import sys; sys.path.insert(0, 'System'); import Orchestrator; print('import OK')"` печатает import OK
    - heartbeat НЕ внутри _set_voice_state (это разные события: state_change при transition, heartbeat периодически)
  </acceptance_criteria>
  <done>Heartbeat event эмитится в _vad_loop с интервалом 5 sec; не блокирует loop; синтаксис валиден; модуль импортируется.</done>
</task>

</tasks>

<verification>
- verify.automated команда печатает OK.
- `git diff --stat` показывает только System/Orchestrator.py.
- В реальном запуске оркестратора (если smoke test делается): после первых 5 секунд работы появится первое `voice_loop_heartbeat` event в events.jsonl; затем каждые 5 секунд.
- `voice_state_change` event (существующий) НЕ переписан и НЕ дублирован — `grep -c "voice_state_change\"" System/Orchestrator.py` — ровно 1 (тот что в _set_voice_state на старой строке 565).
</verification>

<success_criteria>
voice_loop_heartbeat event добавлен в _vad_loop, эмитится не чаще раза в 5 сек, несёт state/iter/uptime_sec/vad_state. При повторении hang: последний heartbeat в events.jsonl покажет где именно loop перестал крутиться; отсутствие новых heartbeat за >5 sec будет недвусмысленным сигналом deadlock. Совместно с уже существующим voice_state_change это закрывает REQ-DIAGNOSTIC-LOGS-VOICE-STATE.
</success_criteria>

<output>
После завершения создать `.planning/phases/08-reply-echo-hang-debug-voice-loop-reply-window/08-03-SUMMARY.md` с записью:
- какой блок добавлен в _vad_loop (строки),
- константы (heartbeat_period_sec=5.0),
- подтверждение что voice_state_change не тронут (существующий event сохранён),
- подтверждение что SIGUSR1 не добавлен (deferred per D-10).
</output>
