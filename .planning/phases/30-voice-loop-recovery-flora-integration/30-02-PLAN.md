---
phase: 30
plan: 02
type: integration
wave: 2
depends_on: [01]
files_modified:
  - System/Config.json
  - System/Config.schema.json
  - System/adam/config.py
  - System/adam/flora.py
  - System/Orchestrator.py
  - Subsystem/AdamsServer/config/AdamsConfig.h
  - Subsystem/AdamsServer/src/io/FloraModule.cpp
  - tests/test_flora.py
  - .planning/phases/30-voice-loop-recovery-flora-integration/30-VERIFICATION.md
autonomous: false
requirements:
  - REQ-FLORA-MERGE
  - REQ-ESP-REFLASH-FLORA
must_haves:
  truths:
    - "Коммит 47fd0c5 влит в voice-loop-recovery как merge-commit (НЕ fast-forward — recovery-коммиты 30-01 уже на ветке); все конфликты разрешены вручную"
    - "flora-gate ПЕРЕРАБОТАН на сосуществование (D-03): _execute_action / /api/agent/scene / /api/agent/stop НЕ возвращают глухой suppressed_flora_owns_channels — моторика Адама накладывается overlay поверх флоры, флора возобновляется после жеста"
    - "Голосовой цикл после мёржа РАБОТАЕТ как в 30-01 (регрессии нет): живой turn wake→ASR→LLM→TTS→ESP проходит"
    - "Решения 30-01 сохранены при резолюции конфликтов: ESP IP=10.10.10.171, TTS output_target=esp32_speaker, Ollama не возвращается"
    - "Флора-каналы из 47fd0c5: вибро=0-3, свет=4-14; vibro cap ~95% (kFloraVibroIntensityCeiling ~3890); потолок яркости света <=71% (инвариант flora_brightness_cap)"
    - "ESP перепрошит из смёрженного Subsystem/AdamsServer (channel remap + vibro cap); вибро на каналах 0-3, свет 4-14 физически подтверждены"
    - "FLORA-04 feed_speech_wav consumer в Orchestrator._consumer работает, не ломая TTS/voice путь"
    - "tests/test_flora.py зелёный (маски каналов обновлены, отсутствие gamma)"
  artifacts:
    - path: "System/Orchestrator.py"
      provides: "Переработанный flora-gate на overlay-сосуществование вместо полного подавления"
      contains: "flora"
    - path: "Subsystem/AdamsServer/config/AdamsConfig.h"
      provides: "kFloraLightChannelLo/Hi=4..14, kFloraVibroChannelLo/Hi=0..3, vibro cap ~3890"
      contains: "kFloraVibroChannelLo"
    - path: ".planning/phases/30-voice-loop-recovery-flora-integration/30-VERIFICATION.md"
      provides: "Лог: мёрж-конфликты и их резолюция, регресс-тест голоса, проверка флоры+reflash"
      contains: "47fd0c5"
  key_links:
    - from: "Orchestrator._execute_action"
      to: "flora overlay layer"
      via: "LLM motor action накладывается поверх флоры, не подавляется"
      pattern: "flora"
    - from: "Orchestrator._consumer"
      to: "flora.feed_speech_wav"
      via: "FLORA-04 RMS-врезка без поломки TTS"
      pattern: "feed_speech_wav"
---

<objective>
Финальный шаг Phase 30: влить технофлору (коммит 47fd0c5, origin/LuxFlora-modes_V1.1, автор 7teenzzz) в восстановленный голосовой цикл с разрешением ВСЕХ конфликтов через глубокий анализ, БЕЗ реактивации поломок. Ключевое решение D-03: flora-gate переработать на СОСУЩЕСТВОВАНИЕ (моторика Адама overlay поверх флоры), а не полное подавление action-layer.

Предусловие (D-05): 30-01 завершён и закоммичен → ветка диверг'ена от 9da07f9 → мёрж 47fd0c5 даёт реальные конфликты (а не fast-forward на всю LuxFlora_V1.1). 47fd0c5 тянет всю линию флоры: flora-gate, FLORA-04 feed_speech_wav в _consumer, флора-параметры, firmware channel remap. «Прошивка обязательна» → reflash ESP в конце.

autonomous: false — ручная конфликт-резолюция + физический reflash ESP + регресс-тест голоса.
</objective>

<tasks>

### Task 1: Мёрж 47fd0c5 (--no-ff --no-commit), перечислить конфликты

**Files:** (git)

**read_first:** `BRANCH.md`, `30-CONTEXT.md` (D-03, D-05), вывод `git log 9da07f9..47fd0c5`

1. Убедиться, что на ветке есть recovery-коммиты 30-01 (иначе мёрж = fast-forward): `git log --oneline 9da07f9..HEAD` непустой.
2. `git merge --no-ff --no-commit 47fd0c5` — ожидать конфликты (Config.json, Config.schema.json, config.py, flora.py, и линия флоры в Orchestrator.py).
3. Зафиксировать список конфликтов (`git diff --name-only --diff-filter=U`) в 30-VERIFICATION.md.

**Verify:**
```bash
git log --oneline 9da07f9..HEAD | grep -q . && echo "branch diverged (not FF)"
git diff --name-only --diff-filter=U | tee /tmp/flora_conflicts.txt
test -s /tmp/flora_conflicts.txt && echo "conflicts present — resolve manually"
```

### Task 2: Резолюция конфликтов конфига и флоры (Config.json/schema, config.py, flora.py)

**Files:** `System/Config.json`, `System/Config.schema.json`, `System/adam/config.py`, `System/adam/flora.py`

**read_first:** конфликтные хунки; `47fd0c5` тело коммита; `30-CONTEXT.md` (D-01/D-02); memory `feedback_flora_brightness_cap` (потолок 71%)

1. Union-резолюция: взять флора-параметры из 47fd0c5 (вибро 0-3, свет 4-14, vibro.intensity_pct ~95, gamma из DEFAULT_CONFIG) И сохранить решения 30-01 (ESP IP 10.10.10.171, TTS output_target=esp32_speaker, никакого Ollama).
2. Сохранить инвариант: потолок яркости СВЕТА <= 71% (peak_pct/peak_duty), вибро — до ~95% (без светового потолка) — ровно как задумано в 47fd0c5.
3. Конфликтов ESP IP быть не должно (обе стороны 10.10.10.171); если 47fd0c5 трогает IP — оставить 10.10.10.171.

**Verify:**
```bash
python3 -m json.tool < System/Config.json > /dev/null && python3 -m json.tool < System/Config.schema.json > /dev/null
python3 -c "import json;c=json.load(open('System/Config.json'));print('IP',c['mcu']['base_url']);print('tts',c['services']['tts']['output_target'])"  # 10.10.10.171 / esp32_speaker
git diff --name-only --diff-filter=U | grep -E 'Config|config.py|flora.py' && echo "STILL UNRESOLVED" || echo "config/flora resolved"
```

### Task 3: Переработать flora-gate на сосуществование в Orchestrator.py (D-03)

**Files:** `System/Orchestrator.py`

**read_first:** конфликтные хунки Orchestrator.py; `47fd0c5` diff (flora-gate в `_execute_action`/`/api/agent/scene`/`/api/agent/stop`); `30-CONTEXT.md` (D-03)

1. НЕ принимать исходный полный gate (`if flora.enabled: return suppressed_flora_owns_channels`). Вместо этого реализовать overlay: при LLM-моторном действии Адама — временно отдать нужные PCA-каналы жесту (приоритет/мьют флоры на длительность), затем флора возобновляется. Флора — фон, моторика Адама — передний план.
2. Механизм определить по факту кода: приоритетная запись каналов / временный flora pause API / overlay-композитор. Соблюсти инвариант safety (motor_max_duration_ms, cooldown) и half_duplex_mute.
3. Сохранить FLORA-04 врезку feed_speech_wav в `_consumer` (Task 4 проверит, что она не ломает голос).

**Verify:**
```bash
grep -n 'suppressed_flora_owns_channels' System/Orchestrator.py && echo "WARN: full-suppression gate still present" || echo "no blunt suppression"
./.venv/bin/python -c "import ast; ast.parse(open('System/Orchestrator.py').read()); print('orchestrator parses')"
```

### Task 4: Зафиксировать мёрж и прогнать регресс голоса + тесты флоры

**Files:** `tests/test_flora.py`, `.planning/.../30-VERIFICATION.md`

**read_first:** `tests/test_flora.py` (маски каналов, assert отсутствия gamma)

1. Завершить мёрж: `git commit` (merge-commit) после полной резолюции (нет `--diff-filter=U`).
2. Прогнать `tests/test_flora.py` (обновлённые маски 0-3/4-14) и полный pytest.
3. Перезапустить оркестратор (systemd) и повторить живой turn голоса (как 30-01 Task 5) — РЕГРЕССИИ НЕТ.

**Verify:**
```bash
git diff --name-only --diff-filter=U | grep -q . && echo "UNRESOLVED REMAIN" || echo "merge clean"
./.venv/bin/python -m pytest tests/test_flora.py -q
curl --noproxy '*' -fsS 'http://127.0.0.1:8080/api/agent/turns?limit=1' | python3 -m json.tool | grep -E 'reply|tts' && echo "voice regression OK"
```

### Task 5: Reflash ESP под флору + физическая проверка каналов (REQ-ESP-REFLASH-FLORA)

**Files:** `Subsystem/AdamsServer/config/AdamsConfig.h`, `Subsystem/AdamsServer/src/io/FloraModule.cpp`

**read_first:** `47fd0c5` тело («Прошивка обязательна»); `CLAUDE.md` Subsystem (flash tools); merged AdamsConfig.h

1. Собрать прошивку из смёрженного `Subsystem/AdamsServer` (channel remap вибро 0-3 / свет 4-14, vibro cap ~3890). Сборка проверена в этой сессии: arduino-cli + fqbn esp32s3 + PrivateConfig.h.
2. Прошить по USB (esptool через `/dev/ttyACM0`, проверено) либо OTA на 10.10.10.171.
3. Физически проверить: вибромоторы на каналах 0-3, светофлора на 4-14; вибро до ~95%, свет <=71%.

**Verify:**
```bash
grep -E 'kFloraVibroChannelLo|kFloraVibroChannelHi|kFloraLightChannelLo|kFloraLightChannelHi' Subsystem/AdamsServer/config/AdamsConfig.h   # 0/3/4/14
# после flash: ESP отвечает, флора анимирует
curl --noproxy '*' -fsS http://10.10.10.171/api/status | head -c 200
```

### Task 6: Финальная верификация сосуществования (флора + голос)

**Files:** `.planning/phases/30-voice-loop-recovery-flora-integration/30-VERIFICATION.md`

**read_first:** `30-VERIFICATION.md` (предыдущие записи)

1. Доказать сосуществование: флора анимирует фоном; при моторном LLM-действии Адама — жест перехватывает каналы overlay'ем, затем флора возобновляется; голос работает параллельно.
2. Записать в 30-VERIFICATION.md: мёрж-конфликты+резолюция, регресс-тест голоса, поведение overlay, факт reflash.

**Verify:**
```bash
grep -qE '47fd0c5|сосуществов|overlay' .planning/phases/30-voice-loop-recovery-flora-integration/30-VERIFICATION.md && echo "coexistence documented"
```

</tasks>

<acceptance_criteria>
- [ ] 47fd0c5 влит как merge-commit (не FF), все конфликты разрешены (`--diff-filter=U` пуст)
- [ ] flora-gate переработан на overlay-сосуществование (нет глухого suppressed_flora_owns_channels)
- [ ] Решения 30-01 сохранены: ESP IP=10.10.10.171, TTS=esp32_speaker, Ollama не вернулась
- [ ] Флора-каналы: вибро 0-3, свет 4-14; vibro ~95%, свет <=71% (инвариант)
- [ ] tests/test_flora.py и полный pytest зелёные; Orchestrator.py парсится
- [ ] Голосовой цикл без регрессии (живой turn проходит после мёржа)
- [ ] ESP перепрошит под флору; каналы 0-3/4-14 физически подтверждены
- [ ] Сосуществование флора+моторика+голос задокументировано в 30-VERIFICATION.md
</acceptance_criteria>
