---
phase: 01-doc-refactor-c-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTEXT.md
  - README.md
  - docs/RUNBOOK_JETSON_EXHIBITION.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "CONTEXT.md не содержит 'medium' или '0.35' или 'debounce 3' — только актуальные значения"
    - "README.md Inference Stack table содержит 'small' вместо 'medium' для ASR"
    - "docs/RUNBOOK_JETSON_EXHIBITION.md не содержит 'ollama', '11434', 'gemma3:4b', 'hw:0,0'"
    - "RUNBOOK содержит ссылку на System/Config.json вместо удалённого блока defaults"
  artifacts:
    - path: "CONTEXT.md"
      provides: "Updated ASR model, threshold, debounce values"
      contains: "small"
    - path: "README.md"
      provides: "Corrected Inference Stack table"
      contains: "small"
    - path: "docs/RUNBOOK_JETSON_EXHIBITION.md"
      provides: "Cleaned defaults section"
      contains: "System/Config.json"
  key_links:
    - from: "CONTEXT.md line ~46"
      to: "Config.json services.asr.model"
      via: "manual sync"
      pattern: "small"
    - from: "RUNBOOK lines 86-94"
      to: "Config.json"
      via: "pointer reference"
      pattern: "System/Config\\.json"
---

<objective>
Точечно исправить документальные несоответствия: неправильная ASR модель ("medium" вместо "small"), неправильный wake word threshold (0.35 вместо 0.20), неправильный debounce_hits (3 вместо 2) в CONTEXT.md и README.md. Удалить Ollama-defaults блок из RUNBOOK.

Purpose: CONTEXT.md, README.md и RUNBOOK содержат данные, противоречащие System/Config.json. Это вводит в заблуждение при диагностике. Исправить хирургически, не переписывая документы (структурный рефакторинг — в Wave 3).

Output: Три файла с исправленными значениями. CONTEXT.md будет удалён в Wave 3, но до этого должен содержать верные данные.
</objective>

<execution_context>
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-01-PLAN.md
</execution_context>

<context>
@/home/i17jet/Agents/Adam-Chip/System/Config.json
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-CONTEXT.md

<interfaces>
<!-- Source of truth values from System/Config.json -->
services.asr.model = "small"
wake_word.threshold = 0.20
wake_word.debounce_hits = 2

<!-- RUNBOOK lines 86-94 to be removed (exact content): -->
<!--
Current defaults:

- `ADAM_LLM_PROVIDER=ollama`
- `ADAM_LLM_BASE_URL=http://127.0.0.1:11434`
- `ADAM_LLM_MODEL=gemma3:4b`
- `ADAM_VENV=/home/i17jet/Agents/Adam-Chip/.venv`
- `ADAM_VIDEO_DEVICE=/dev/video0`
- `ADAM_AUDIO_INPUT_DEVICE=hw:0,0`
- `ADAM_AUDIO_OUTPUT_DEVICE=default`
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CONTEXT.md — ASR model, threshold, debounce</name>
  <files>CONTEXT.md</files>
  <read_first>
    - CONTEXT.md (full file — find exact lines with wrong values)
    - System/Config.json (source of truth: services.asr.model, wake_word.threshold, wake_word.debounce_hits)
  </read_first>
  <action>
    Make five surgical edits in CONTEXT.md — two locations contain wrong values:

    **Location A: Inference Stack table (~line 46)**
    Change the ASR row cell from:
      `medium`, ru-RU
    to:
      `small`, ru-RU

    **Location B: Wake word line (~line 55)**
    Change:
      OpenWakeWord — ONNX-модель `adam.onnx`, порог 0.35, debounce 3 детекции
    to:
      OpenWakeWord — ONNX-модель `adam.onnx`, порог 0.20, debounce 2 детекции

    **Location C: Config.json snapshot block (~lines 120-140, "## Config.json" section)**
    In the JSON code block, change:
      "model": "medium"   →  "model": "small"
      "threshold": 0.35   →  "threshold": 0.20
      "debounce_hits": 3  →  "debounce_hits": 2

    Do NOT change any other content. Do NOT rewrite or reformat the file structure.
    This file will be deleted in Wave 3 (plan 01-03); these fixes ensure full correctness until then.
    The verify grep runs file-wide, so ALL occurrences of wrong values must be corrected.
  </action>
  <verify>
    <automated>grep -n "\"medium\"\|: 0\.35\|debounce_hits.*: 3\b\|debounce 3" /home/i17jet/Agents/Adam-Chip/CONTEXT.md; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - grep for '"medium"' in CONTEXT.md returns no matches (all occurrences replaced with "small")
    - grep for ': 0.35' in CONTEXT.md returns no matches
    - grep for 'debounce_hits.*: 3\b' or 'debounce 3' in CONTEXT.md returns no matches
    - grep for '"small"' in CONTEXT.md shows match on both the Inference Stack table and the JSON snapshot block
    - grep for '0.20' in CONTEXT.md shows match on both the Wake word line and the JSON snapshot block
    - grep for '"debounce_hits": 2' in CONTEXT.md shows match in the JSON snapshot block
    - grep for 'debounce 2' in CONTEXT.md shows match on the Wake word line
    - File line count unchanged (same number of lines as before — only values changed, not structure)
  </acceptance_criteria>
  <done>CONTEXT.md содержит "small", "0.20", "debounce 2" во всех трёх локациях. Ни "medium", "0.35", "debounce 3" не найдены нигде в файле.</done>
</task>

<task type="auto">
  <name>Task 2: Fix README.md — ASR model in Inference Stack table</name>
  <files>README.md</files>
  <read_first>
    - README.md (find the Inference Stack table, line ~40)
    - System/Config.json (services.asr.model = "small")
  </read_first>
  <action>
    In README.md, find the Inference Stack table (## Inference Stack section, ~line 36-43).
    Change the ASR row from:
      | ASR | WhisperX (CUDA, Docker) | medium, ru-RU, wake word «адам» | 8095 |
    to:
      | ASR | WhisperX (CUDA, Docker) | small, ru-RU, wake word «адам» | 8095 |

    Change only the word "medium" → "small" in that specific table cell. Nothing else.
    Do NOT remove parameters from the table — that structural change happens in Wave 3 (plan 01-03, per D-02).
  </action>
  <verify>
    <automated>grep -n "medium\|small" /home/i17jet/Agents/Adam-Chip/README.md | grep -i "asr\|whisper"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "medium" in README.md Inference Stack section returns no matches
    - grep for "small" in README.md returns match on the ASR row in the Inference Stack table
    - The rest of the Inference Stack table (LLM, VLM, TTS, Orchestrator rows) is unchanged
  </acceptance_criteria>
  <done>README.md Inference Stack table показывает "small" для ASR. Слово "medium" не найдено в этом файле.</done>
</task>

<task type="auto">
  <name>Task 3: Clean RUNBOOK — remove Ollama defaults block</name>
  <files>docs/RUNBOOK_JETSON_EXHIBITION.md</files>
  <read_first>
    - docs/RUNBOOK_JETSON_EXHIBITION.md (full file — locate "Current defaults:" section ~lines 86-94)
    - System/Config.json (to write the replacement pointer correctly)
  </read_first>
  <action>
    In docs/RUNBOOK_JETSON_EXHIBITION.md, remove the entire "Current defaults:" block (per D-06).

    Remove these lines entirely (approximately lines 86-94):
    ```
    Current defaults:

    - `ADAM_LLM_PROVIDER=ollama`
    - `ADAM_LLM_BASE_URL=http://127.0.0.1:11434`
    - `ADAM_LLM_MODEL=gemma3:4b`
    - `ADAM_VENV=/home/i17jet/Agents/Adam-Chip/.venv`
    - `ADAM_VIDEO_DEVICE=/dev/video0`
    - `ADAM_AUDIO_INPUT_DEVICE=hw:0,0`
    - `ADAM_AUDIO_OUTPUT_DEVICE=default`
    ```

    Replace with a single-line pointer:
    ```
    For current runtime defaults, see `System/Config.json`.
    ```

    The replacement text goes where "Current defaults:" block was, between the paragraph ending
    "The generated systemd environment file is:" block and the "## Media Policy" section.
    Keep the blank line before "## Media Policy".
  </action>
  <verify>
    <automated>grep -n "ollama\|11434\|gemma3\|hw:0,0\|ADAM_LLM_PROVIDER" /home/i17jet/Agents/Adam-Chip/docs/RUNBOOK_JETSON_EXHIBITION.md</automated>
  </verify>
  <acceptance_criteria>
    - grep for "ollama" in RUNBOOK returns no matches
    - grep for "11434" in RUNBOOK returns no matches
    - grep for "gemma3" in RUNBOOK returns no matches
    - grep for "hw:0,0" in RUNBOOK returns no matches
    - grep for "ADAM_LLM_PROVIDER" in RUNBOOK returns no matches
    - grep for "System/Config.json" in RUNBOOK returns at least one match
    - "## Media Policy" section still present and intact
  </acceptance_criteria>
  <done>RUNBOOK не содержит Ollama-defaults и hw:0,0. Блок "Current defaults:" заменён ссылкой на System/Config.json.</done>
</task>

</tasks>

<verification>
Run after all tasks complete:

```bash
# Verify no wrong values remain in any doc
grep -rn "medium\|0\.35\|debounce 3\|ollama\|11434\|hw:0,0" \
  /home/i17jet/Agents/Adam-Chip/CONTEXT.md \
  /home/i17jet/Agents/Adam-Chip/README.md \
  /home/i17jet/Agents/Adam-Chip/docs/RUNBOOK_JETSON_EXHIBITION.md \
  | grep -v "^.*#" | grep -v "Binary"
```

Expected: no output (zero matches in documentation files).
</verification>

<success_criteria>
- CONTEXT.md: ASR = "small", threshold = 0.20, debounce = 2
- README.md Inference Stack: ASR = "small"
- RUNBOOK: no Ollama/11434/gemma3/hw:0,0 references; "System/Config.json" pointer present
- All three files pass grep verification with zero wrong-value matches
</success_criteria>

<output>
After completion, create `/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-01-SUMMARY.md` with:
- Files modified: CONTEXT.md, README.md, docs/RUNBOOK_JETSON_EXHIBITION.md
- Changes made: list of exact line changes
- Verification result: grep commands and their output
</output>
