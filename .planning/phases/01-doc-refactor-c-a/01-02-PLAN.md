---
phase: 01-doc-refactor-c-a
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - System/Config.schema.json
autonomous: true
requirements:
  - DOC-05

must_haves:
  truths:
    - "System/Config.schema.json существует и содержит JSON Schema Draft-07"
    - "Каждая секция из Config.json представлена в schema (agent, power, media, services, mcu, sounds, safety, wake_word)"
    - "Каждое поле имеет description с объяснением назначения и default с актуальным значением"
    - "Файл валидный JSON"
  artifacts:
    - path: "System/Config.schema.json"
      provides: "Living documentation for all Config.json parameters"
      contains: "\"$schema\": \"http://json-schema.org/draft-07/schema#\""
    - path: "System/Config.schema.json"
      provides: "All 8 top-level sections covered"
      contains: "wake_word"
  key_links:
    - from: "System/Config.schema.json"
      to: "System/Config.json"
      via: "mirrors same key structure"
      pattern: "properties.*agent.*power.*media.*services"
---

<objective>
Создать System/Config.schema.json — JSON Schema Draft-07 с описаниями каждого параметра Config.json.

Purpose: Концепция A (Config-First) требует машиночитаемую документацию параметров. README и CLAUDE.md больше не будут содержать числа; вместо этого они ссылаются на Config.json + Config.schema.json. Файл не импортируется в код — это документация (per D-04).

Output: System/Config.schema.json с полным покрытием всех 8 секций Config.json. Descriptions на английском (единообразно с кодом, per Claude's Discretion).
</objective>

<execution_context>
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-02-PLAN.md
</execution_context>

<context>
@/home/i17jet/Agents/Adam-Chip/System/Config.json
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-CONTEXT.md

<interfaces>
<!-- Config.json top-level structure — all 8 sections must be covered -->
{
  "agent":    { name, language, mode, data_dir, persona_paths, history_turns, prompt_trace_max },
  "power":    { required_mode_name, required_mode_id, require_jetson_clocks, enforce_in_exhibition },
  "media":    {
    "video":  { primary, gstreamer_pipeline, remote_rtsp_url, preview_enabled, esp_mjpeg_url,
                esp_fail_threshold, esp_retry_interval_sec, video_device, camera_width,
                camera_height, camera_quality, camera_capture_interval_sec },
    "audio":  { input_device, output_device, sample_rate, channels, frame_ms, vad_threshold,
                min_speech_ms, max_segment_ms, mic_source, esp32_mic_profile,
                esp_mic_fail_threshold, esp_mic_retry_interval_sec, esp_health,
                webrtc_vad_aggressiveness, max_command_segment_ms, normalize_factor },
    scene_worker_enabled, scene_interval_sec, scene_stale_after_sec,
    scene_buffer_maxlen, scene_context_count
  },
  "services": {
    "llm":    { provider, base_url, model, model_path, timeout_sec, temperature, max_tokens, num_ctx },
    "vlm":    { base_url, model, timeout_sec, max_new_tokens, prompt },
    "asr":    { provider, base_url, model, language, command_endpointing_ms, reply_window_sec,
                reply_absolute_deadline_sec, sample_rate, timeout_sec, wake_words,
                wake_word_required, reply_window_expired_action },
    "tts":    { provider, base_url, model, speaker, sample_rate, timeout_sec,
                available_speakers, output_device, filler_enabled, filler_phrase, filler_delay_ms }
  },
  "mcu":      { base_url, speaker_url, timeout_sec, idle_scene, allowed_scenes, channels },
  "sounds":   { enabled, success_path, local_output_device, esp_boot_note, error_path },
  "safety":   { motor_default_duration_ms, motor_max_duration_ms, motor_cooldown_ms, half_duplex_mute },
  "wake_word":{ engine, model_path, threshold, debounce_hits, vad_threshold, wake_silence_timeout_sec }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create System/Config.schema.json</name>
  <files>System/Config.schema.json</files>
  <read_first>
    - System/Config.json (source of truth — read ALL fields, ALL current values before writing schema)
    - .planning/phases/01-doc-refactor-c-a/01-CONTEXT.md (D-04: format requirements)
  </read_first>
  <action>
    Create System/Config.schema.json as a new file (per D-04). This file is documentation only — not imported by code.

    Schema structure requirements:
    - Top level: "$schema": "http://json-schema.org/draft-07/schema#", "$id": "Config.schema.json", "title": "Adam Chip Runtime Configuration", "type": "object"
    - "properties" mirrors all 8 top-level keys from Config.json: agent, power, media, services, mcu, sounds, safety, wake_word
    - For EACH leaf field, provide:
      - "type": (string/number/boolean/array/object)
      - "description": plain English explanation of what this parameter does and what happens if you change it
      - "default": current value from Config.json (exact — not a placeholder)
      - "enum" where applicable (e.g. mode: ["maintenance", "exhibition"], provider values, mic_source values)

    Key descriptions to include (write similar level of detail for all fields):
    - agent.mode: "Runtime mode. 'maintenance' disables wake word gate and power enforcement. 'exhibition' requires wake word 'адам' before ASR activates and enforces MAXN power mode."
    - agent.history_turns: "Number of dialogue turns kept in LLM context. Higher = more context but longer prompt prefill (~9s per turn for Gemma 4 E4B)."
    - media.video.primary: "Active video source. 'esp_mjpeg' uses ESP32 camera stream (192.168.0.171:81/stream). 'jetson_gstreamer' uses local USB/CSI camera via GStreamer pipeline."
    - services.llm.max_tokens: "Maximum tokens in LLM response. Keep low (40-80) to stay in TTS latency budget. Gemma 4 E4B with SWA cache takes ~9s for full prefill regardless."
    - services.asr.model: "WhisperX model size. 'small' balances accuracy and latency on Jetson. 'medium' is more accurate but slower."
    - services.asr.reply_window_sec: "Seconds to wait for user to continue speaking after detected speech ends. After this window, ASR submits transcript."
    - services.asr.reply_absolute_deadline_sec: "Hard deadline from first VAD trigger. If user still speaking at this point, ASR submits anyway."
    - services.tts.sample_rate: "TTS audio sample rate in Hz. Must match Silero model output (24000 for v5_5_ru). Do not change without rebuilding TTS service."
    - services.tts.output_device: "ALSA device for TTS playback. 'plughw:1,3' = HDMI audio (card 1, HDA NVIDIA). hw:1,0 (Jetson APE I2S) does not produce PCM output — do not use."
    - wake_word.threshold: "Detection confidence threshold for OpenWakeWord ONNX model. Lower = more sensitive but more false positives. 0.20 is tuned for exhibition distance (~1-3m)."
    - wake_word.debounce_hits: "Number of consecutive frames above threshold required to trigger wake word. Prevents spurious triggers from single-frame spikes."
    - safety.half_duplex_mute: "When true, mic input is muted during TTS playback to prevent echo feedback loop. MUST remain true in all modes."
    - power.enforce_in_exhibition: "When true, orchestrator refuses to start in 'exhibition' mode unless nvpmodel -m 0 (MAXN) and jetson_clocks are active."

    Group nested objects (media.video, media.audio, services.llm, etc.) as nested "properties" objects.
    For media.audio.esp_health sub-object, document each sub-field.
    For services.tts.available_speakers, use "type": "array", "description": "Available Silero TTS voice IDs for the v5_5_ru model."

    The file must be valid JSON. No trailing commas. No comments inside the JSON.
  </action>
  <verify>
    <automated>python3 -c "import json; f=open('/home/i17jet/Agents/Adam-Chip/System/Config.schema.json'); d=json.load(f); sections=list(d.get('properties',{}).keys()); print('Sections:', sections); assert all(s in sections for s in ['agent','power','media','services','mcu','sounds','safety','wake_word']), 'Missing sections'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File System/Config.schema.json exists and is valid JSON (python3 json.load succeeds)
    - Top-level "properties" contains all 8 keys: agent, power, media, services, mcu, sounds, safety, wake_word
    - grep for '"$schema"' returns "http://json-schema.org/draft-07/schema#"
    - grep for '"default"' returns at least 30 matches (one per documented leaf field)
    - grep for '"description"' returns at least 30 matches
    - grep for '"wake_word"' in the properties section returns a match
    - grep for '"threshold"' returns a match with value 0.20 as default
    - grep for '"debounce_hits"' returns a match with value 2 as default
    - grep for '"max_tokens"' returns a match with value 40 as default
    - grep for '"model"' in asr section returns a match with "small" as default
  </acceptance_criteria>
  <done>System/Config.schema.json создан, все 8 секций документированы, файл валидный JSON, все default-значения соответствуют System/Config.json.</done>
</task>

</tasks>

<verification>
```bash
# Validate JSON and section coverage
python3 -c "
import json
with open('/home/i17jet/Agents/Adam-Chip/System/Config.schema.json') as f:
    schema = json.load(f)
props = schema.get('properties', {})
required = ['agent', 'power', 'media', 'services', 'mcu', 'sounds', 'safety', 'wake_word']
missing = [s for s in required if s not in props]
if missing:
    print('MISSING sections:', missing)
else:
    print('All sections present:', list(props.keys()))
"

# Count documented fields
grep -c '"description"' /home/i17jet/Agents/Adam-Chip/System/Config.schema.json
grep -c '"default"' /home/i17jet/Agents/Adam-Chip/System/Config.schema.json
```

Expected: all 8 sections present, description count >= 30.
</verification>

<success_criteria>
- System/Config.schema.json is valid JSON Schema Draft-07
- All 8 Config.json top-level sections documented
- Every leaf field has "description" and "default" matching Config.json current values
- threshold default = 0.20, debounce_hits default = 2, max_tokens default = 40, asr model default = "small"
</success_criteria>

<output>
After completion, create `/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-02-SUMMARY.md` with:
- File created: System/Config.schema.json
- Section count and field count
- Verification result
</output>
