---
phase: 01-doc-refactor-c-a
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02-PLAN
files_modified:
  - System/adam/config.py
autonomous: true
requirements:
  - DOC-06

must_haves:
  truths:
    - "DEFAULT_CONFIG в config.py отражает текущие реальные значения из System/Config.json"
    - "Запуск без Config.json (фоллбэк на DEFAULT_CONFIG) даёт рабочую конфигурацию"
    - "Все 8 расхождений из аудита исправлены"
  artifacts:
    - path: "System/adam/config.py"
      provides: "Synchronized DEFAULT_CONFIG"
      contains: "esp_mjpeg"
  key_links:
    - from: "System/adam/config.py DEFAULT_CONFIG"
      to: "System/Config.json"
      via: "manual sync — values must match"
      pattern: "\"esp_mjpeg\""
---

<objective>
Синхронизировать DEFAULT_CONFIG в System/adam/config.py с реальными значениями из System/Config.json (per D-05).

Purpose: DEFAULT_CONFIG — это фоллбэк при отсутствии Config.json. Если он содержит устаревшие значения (old ASR model, wrong max_tokens, wrong video primary), запуск без Config.json даёт нерабочую или некорректную систему. После аудита найдены 8 конкретных расхождений.

Output: config.py с обновлённым DEFAULT_CONFIG. Функциональность Settings.load() не трогаем.
</objective>

<execution_context>
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-04-PLAN.md
</execution_context>

<context>
@/home/i17jet/Agents/Adam-Chip/System/Config.json
@/home/i17jet/Agents/Adam-Chip/System/adam/config.py
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-CONTEXT.md
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-02-SUMMARY.md

<interfaces>
<!-- Exact mismatches from audit (01-CONTEXT.md specifics section) -->
<!-- All changes are within DEFAULT_CONFIG dict only, lines 14-136 of config.py -->

| Field path                                  | DEFAULT_CONFIG (old) | Config.json (target) |
|---------------------------------------------|----------------------|----------------------|
| media.video.primary                         | "jetson_gstreamer"   | "esp_mjpeg"          |
| media.video.preview_enabled                 | False                | True                 |
| media.video.esp_mjpeg_url                   | ""                   | "http://192.168.0.171:81/stream" |
| media.audio.mic_source                      | "local"              | "esp32"              |
| media.audio.min_speech_ms                   | 280                  | 200                  |
| media.scene_interval_sec                    | 8                    | 4                    |
| media.scene_stale_after_sec                 | 20                   | 8                    |
| services.llm.max_tokens                     | 220                  | 40                   |
| services.llm.num_ctx (missing)              | (absent)             | 8192                 |
| services.asr.model                          | "medium"             | "small"              |
| services.asr.reply_window_sec               | 6.0                  | 3.75                 |
| services.asr.reply_absolute_deadline_sec    | 12.0                 | 7.5                  |
| services.asr.wake_word_required             | False                | True                 |
| services.tts.sample_rate                    | 48000                | 24000                |
| services.vlm.max_new_tokens                 | 32                   | 48                   |
| wake_word.threshold                         | 0.5                  | 0.20                 |
| wake_word.debounce_hits                     | 5                    | 2                    |
| wake_word.vad_threshold                     | 0.5                  | 0                    |
| sounds.success_path                         | "data/sounds/success.mp3" | "data/sounds/success.wav" |

<!-- Fields where DEFAULT_CONFIG has extra keys not in Config.json — leave as-is (they are safe defaults) -->
<!-- Fields where Config.json has agent.prompt_trace_max=50, agent.name="Адам Чип" — add these to DEFAULT_CONFIG too -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update DEFAULT_CONFIG in config.py to match Config.json</name>
  <files>System/adam/config.py</files>
  <read_first>
    - System/adam/config.py (full file — understand current DEFAULT_CONFIG structure before editing)
    - System/Config.json (source of truth — verify every target value before writing)
  </read_first>
  <action>
    Edit only the DEFAULT_CONFIG dict in System/adam/config.py (lines 14-136). Do NOT modify any function, method, or class below DEFAULT_CONFIG.

    Apply these changes to DEFAULT_CONFIG (per D-05, from audit findings):

    1. agent section:
       - agent.name: "Adam Chip" → "Адам Чип"  (matches Config.json)
       - agent.history_turns: 8 → 2  (matches Config.json)
       - Add: agent.prompt_trace_max: 50

    2. media.video section:
       - media.video.primary: "jetson_gstreamer" → "esp_mjpeg"
       - media.video.preview_enabled: False → True
       - media.video.esp_mjpeg_url: "" → "http://192.168.0.171:81/stream"

    3. media.audio section:
       - media.audio.mic_source: "local" → "esp32"
       - media.audio.min_speech_ms: 280 → 200
       - Add: media.audio.esp_mic_fail_threshold: 6
       - Add: media.audio.esp_mic_retry_interval_sec: 10.0
       - Add: media.audio.webrtc_vad_aggressiveness: 2
       - Add: media.audio.max_command_segment_ms: 15000
       - Add: media.audio.normalize_factor: 8000

    4. media (top-level):
       - media.scene_interval_sec: 8 → 4
       - media.scene_stale_after_sec: 20 → 8
       - Add: media.scene_buffer_maxlen: 8
       - Add: media.scene_context_count: 1

    5. services.llm section:
       - services.llm.max_tokens: 220 → 40
       - Add: services.llm.num_ctx: 8192

    6. services.vlm section:
       - services.vlm.max_new_tokens: 32 → 48
       - Add: services.vlm.prompt: "Describe the scene in one brief sentence: people, movements, notable objects. No Chinese characters."

    7. services.asr section:
       - services.asr.model: "medium" → "small"
       - services.asr.reply_window_sec: 6.0 → 3.75
       - services.asr.reply_absolute_deadline_sec: 12.0 → 7.5
       - services.asr.wake_word_required: False → True
       - Add: services.asr.reply_window_expired_action: "standby"

    8. services.tts section:
       - services.tts.sample_rate: 48000 → 24000
       - Add: services.tts.filler_enabled: True
       - Add: services.tts.filler_phrase: "Хм..."
       - Add: services.tts.filler_delay_ms: 800

    9. mcu section:
       - mcu.timeout_sec: 3 → 1

    10. sounds section:
       - sounds.success_path: "data/sounds/success.mp3" → "data/sounds/success.wav"
       - Add: sounds.error_path: "data/sounds/boot.wav"

    11. wake_word section:
       - wake_word.threshold: 0.5 → 0.20
       - wake_word.debounce_hits: 5 → 2
       - wake_word.vad_threshold: 0.5 → 0
       - wake_word.wake_silence_timeout_sec: 3.0 → 3  (int, matches Config.json)

    Preserve Python True/False (not JSON true/false). Preserve string formatting (triple-quote for gstreamer_pipeline is OK). Keep all other code (Settings class, _deep_merge, etc.) completely unchanged.
  </action>
  <verify>
    <automated>cd /home/i17jet/Agents/Adam-Chip && PYTHONPATH=System ./.venv/bin/python -c "
from adam.config import DEFAULT_CONFIG
checks = [
    DEFAULT_CONFIG['media']['video']['primary'] == 'esp_mjpeg',
    DEFAULT_CONFIG['services']['llm']['max_tokens'] == 40,
    DEFAULT_CONFIG['services']['asr']['model'] == 'small',
    DEFAULT_CONFIG['services']['asr']['reply_window_sec'] == 3.75,
    DEFAULT_CONFIG['services']['tts']['sample_rate'] == 24000,
    DEFAULT_CONFIG['wake_word']['threshold'] == 0.20,
    DEFAULT_CONFIG['wake_word']['debounce_hits'] == 2,
    DEFAULT_CONFIG['media']['scene_interval_sec'] == 4,
]
print('All checks:', all(checks))
print('Results:', checks)
"</automated>
  </verify>
  <acceptance_criteria>
    - Python import of DEFAULT_CONFIG succeeds without errors
    - DEFAULT_CONFIG['media']['video']['primary'] == 'esp_mjpeg'
    - DEFAULT_CONFIG['services']['llm']['max_tokens'] == 40
    - DEFAULT_CONFIG['services']['asr']['model'] == 'small'
    - DEFAULT_CONFIG['services']['asr']['reply_window_sec'] == 3.75
    - DEFAULT_CONFIG['services']['asr']['reply_absolute_deadline_sec'] == 7.5
    - DEFAULT_CONFIG['services']['tts']['sample_rate'] == 24000
    - DEFAULT_CONFIG['wake_word']['threshold'] == 0.20
    - DEFAULT_CONFIG['wake_word']['debounce_hits'] == 2
    - DEFAULT_CONFIG['media']['scene_interval_sec'] == 4
    - DEFAULT_CONFIG['media']['audio']['mic_source'] == 'esp32'
    - grep for "jetson_gstreamer" in DEFAULT_CONFIG section returns 0 matches (only in the gstreamer_pipeline string value is OK, but not as primary value)
    - grep for '"medium"' in config.py DEFAULT_CONFIG returns 0 matches (asr model)
    - grep for '220' in config.py DEFAULT_CONFIG returns 0 matches (max_tokens)
    - grep for '48000' in config.py DEFAULT_CONFIG returns 0 matches (tts sample_rate)
    - Settings.load() class and all methods below DEFAULT_CONFIG unchanged
    - File parses as valid Python (python3 -m py_compile succeeds)
  </acceptance_criteria>
  <done>DEFAULT_CONFIG синхронизирован с Config.json. Все 8+ расхождений из аудита исправлены. Python import и py_compile проходят без ошибок.</done>
</task>

</tasks>

<verification>
```bash
# Syntax check
cd /home/i17jet/Agents/Adam-Chip && PYTHONPATH=System ./.venv/bin/python -m py_compile System/adam/config.py && echo "Syntax OK"

# Value verification
cd /home/i17jet/Agents/Adam-Chip && PYTHONPATH=System ./.venv/bin/python -c "
from adam.config import DEFAULT_CONFIG, Settings
import json

# Verify all audit-found mismatches are fixed
assertions = {
    'media.video.primary': (DEFAULT_CONFIG['media']['video']['primary'], 'esp_mjpeg'),
    'services.llm.max_tokens': (DEFAULT_CONFIG['services']['llm']['max_tokens'], 40),
    'services.asr.model': (DEFAULT_CONFIG['services']['asr']['model'], 'small'),
    'services.asr.reply_window_sec': (DEFAULT_CONFIG['services']['asr']['reply_window_sec'], 3.75),
    'services.asr.reply_absolute_deadline_sec': (DEFAULT_CONFIG['services']['asr']['reply_absolute_deadline_sec'], 7.5),
    'services.tts.sample_rate': (DEFAULT_CONFIG['services']['tts']['sample_rate'], 24000),
    'wake_word.threshold': (DEFAULT_CONFIG['wake_word']['threshold'], 0.20),
    'wake_word.debounce_hits': (DEFAULT_CONFIG['wake_word']['debounce_hits'], 2),
    'media.audio.mic_source': (DEFAULT_CONFIG['media']['audio']['mic_source'], 'esp32'),
    'media.scene_interval_sec': (DEFAULT_CONFIG['media']['scene_interval_sec'], 4),
}
failed = [(k, got, expected) for k, (got, expected) in assertions.items() if got != expected]
if failed:
    for k, got, exp in failed:
        print(f'FAIL {k}: got={got!r}, expected={exp!r}')
else:
    print('All assertions passed')

# Smoke test Settings.load without Config.json
import tempfile, os
s = Settings.load(path=tempfile.mktemp(suffix='.json'))
print('Settings.load fallback OK, mode:', s.mode)
"
```

Expected: All assertions passed. Settings.load fallback OK.
</verification>

<success_criteria>
- All 8+ audit-found mismatches corrected in DEFAULT_CONFIG
- config.py syntax-valid Python
- Settings class and all methods unchanged
- Settings.load() works with and without Config.json present
- No old values ("medium", 220, 48000, 0.5 threshold, "jetson_gstreamer" as primary) remain in DEFAULT_CONFIG
</success_criteria>

<output>
After completion, create `/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-04-SUMMARY.md` with:
- File modified: System/adam/config.py
- List of all changed values with before/after
- Verification output (all assertions passed)
</output>
