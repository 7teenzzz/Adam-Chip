---
phase: 01-doc-refactor-c-a
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01-PLAN
  - 01-02-PLAN
files_modified:
  - CONTEXT.md
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - DOC-07

must_haves:
  truths:
    - "CONTEXT.md заменён файлом-указателем из 5 строк (без удаления самого файла — содержимое заменено)"
    - "README.md Inference Stack таблица удалена или заменена ссылкой на Config.json; числовые параметры (max_tokens, temperature, num_ctx) не присутствуют"
    - "CLAUDE.md не содержит конкретных числовых значений wake word threshold и debounce в секции Gotchas"
    - "CLAUDE.md сохраняет все инварианты и conceptual gotchas без числовых параметров"
  artifacts:
    - path: "CONTEXT.md"
      provides: "Pointer file replacing full snapshot"
      contains: "Config.json"
    - path: "README.md"
      provides: "Simplified doc without inline numeric parameters"
      contains: "Config.json"
    - path: "CLAUDE.md"
      provides: "Invariants-only, no numeric params"
      contains: "адам"
  key_links:
    - from: "CONTEXT.md"
      to: "README.md, CLAUDE.md, Config.json"
      via: "pointer references in replacement text"
      pattern: "Config\\.json"
    - from: "README.md"
      to: "Config.json"
      via: "prose reference replacing removed table"
      pattern: "Config\\.json"
---

<objective>
Структурный рефакторинг трёх документов:
1. CONTEXT.md — заменить содержимое на 5-строчный указатель (per D-01)
2. README.md — убрать числовые параметры из Inference Stack таблицы; добавить ссылку на Config.json (per D-02)
3. CLAUDE.md — удалить конкретные числовые значения из Gotchas (per D-03)

Purpose: После Wave 1 значения исправлены, после Wave 2 схема создана. Теперь сами документы нужно упростить так, чтобы числа жили только в Config.json + Config.schema.json (Концепция C + A).

Output: Три упрощённых файла. CONTEXT.md становится указателем. README.md теряет Inference Stack параметры, но сохраняет архитектуру и команды. CLAUDE.md сохраняет все инварианты, теряет числа.
</objective>

<execution_context>
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-03-PLAN.md
</execution_context>

<context>
@/home/i17jet/Agents/Adam-Chip/System/Config.json
@/home/i17jet/Agents/Adam-Chip/System/Config.schema.json
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-CONTEXT.md
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-01-SUMMARY.md
@/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-02-SUMMARY.md

<interfaces>
<!-- What Wave 1 fixed (do not un-fix these) -->
<!-- CONTEXT.md already has: small, 0.20, debounce 2 -->
<!-- README.md already has: small in Inference Stack table -->
<!-- RUNBOOK already has: no Ollama block -->

<!-- What Wave 2 created -->
<!-- System/Config.schema.json exists with all parameter descriptions -->

<!-- D-01: CONTEXT.md replacement text (exact 5-line pointer) -->
<!--
# Adam Chip — Context

Этот файл упразднён. Снимок состояния системы хранится в:

- **Параметры:** `System/Config.json` + `System/Config.schema.json`
- **Архитектура и быстрый старт:** `README.md`
- **Инварианты и gotchas:** `CLAUDE.md`
- **Production runbook:** `docs/RUNBOOK_JETSON_EXHIBITION.md`
-->

<!-- D-02: README.md — Inference Stack table change -->
<!-- Current table: Компонент / Runtime / Модель / Порт -->
<!-- Keep: component names, ports -->
<!-- Remove: model parameters (max_tokens, temperature, num_ctx embedded in cells) -->
<!-- The ASR cell currently says: "small, ru-RU, wake word «адам»" -->
<!-- Remove model-specific params, keep just: "small (ru-RU)" or "WhisperX medium.en equiv" -->
<!-- Add line after table: "Runtime parameters for all services: see System/Config.json" -->

<!-- D-03: CLAUDE.md Gotchas -->
<!-- Wake word gotcha currently does NOT exist as a separate bullet (it's in Invariants #6) -->
<!-- Invariant #6: "Wake word «адам»" — keep this, it's conceptual, no numbers -->
<!-- Audio devices gotcha: contains "hw:1,0 (Jetson APE I2S) физически не работает" — keep, this is a gotcha not a param -->
<!-- No numeric threshold/debounce currently in CLAUDE.md — verify and confirm, then no change needed to CLAUDE.md -->
<!-- Check CLAUDE.md for any reference to threshold=X or debounce=N before editing -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CONTEXT.md with pointer</name>
  <files>CONTEXT.md</files>
  <read_first>
    - CONTEXT.md (full file — verify current content before replacing)
    - .planning/phases/01-doc-refactor-c-a/01-CONTEXT.md (D-01: what the pointer should say)
  </read_first>
  <action>
    Replace the entire content of CONTEXT.md with a pointer file (per D-01).
    Do NOT delete the file — replace its contents.

    New content (exact):

    ```
    # Adam Chip — Context

    Этот файл упразднён. Снимок состояния системы устарел и был удалён.

    Актуальная информация:

    - **Параметры конфигурации:** `System/Config.json` + `System/Config.schema.json`
    - **Архитектура и быстрый старт:** `README.md`
    - **Инварианты и gotchas для агента:** `CLAUDE.md`
    - **Production runbook:** `docs/RUNBOOK_JETSON_EXHIBITION.md`
    ```

    The file should be exactly these lines, nothing more.
  </action>
  <verify>
    <automated>wc -l /home/i17jet/Agents/Adam-Chip/CONTEXT.md && grep -c "Config.json" /home/i17jet/Agents/Adam-Chip/CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTEXT.md line count is 9 or fewer lines (the pointer is short)
    - grep for "Config.json" in CONTEXT.md returns at least 1 match
    - grep for "Inference Stack" in CONTEXT.md returns 0 matches
    - grep for "DEFAULT_CONFIG" in CONTEXT.md returns 0 matches
    - grep for "services.llm" in CONTEXT.md returns 0 matches
    - File still exists (not deleted)
  </acceptance_criteria>
  <done>CONTEXT.md содержит только указатель (~9 строк). Всё старое содержимое удалено.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify README.md Inference Stack — remove inline numeric params</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — read current Inference Stack table and surrounding context)
    - .planning/phases/01-doc-refactor-c-a/01-CONTEXT.md (D-02: what stays and what goes)
  </read_first>
  <action>
    In README.md, update the ## Inference Stack section (per D-02).

    Current table:
    | Компонент | Runtime | Модель | Порт |
    |-----------|---------|--------|------|
    | LLM | llama.cpp (OpenAI-compat) | gemma-4-E4B-it-UD-Q4_K_XL | 8081 |
    | VLM | nano_llm (Docker) | VILA 1.5-3b | 8084 |
    | ASR | WhisperX (CUDA, Docker) | small, ru-RU, wake word «адам» | 8095 |
    | TTS | Silero v5_5_ru | голос eugene | 8082 |
    | Orchestrator | FastAPI + asyncio | — | 8080 |
    | Log Viewer | FastAPI (always-on) | — | 8083 |

    Change the table to remove parameter details from Модель column, keep only model names:
    | Компонент | Runtime | Модель | Порт |
    |-----------|---------|--------|------|
    | LLM | llama.cpp (OpenAI-compat) | gemma-4-E4B-it-UD-Q4_K_XL | 8081 |
    | VLM | nano_llm (Docker) | VILA 1.5-3b | 8084 |
    | ASR | WhisperX (CUDA, Docker) | small (ru-RU) | 8095 |
    | TTS | Silero v5_5_ru | eugene | 8082 |
    | Orchestrator | FastAPI + asyncio | — | 8080 |
    | Log Viewer | FastAPI (always-on) | — | 8083 |

    After the table, add a line:
    "Runtime параметры всех сервисов: см. `System/Config.json` и `System/Config.schema.json`."

    Also update the CLAUDE.md reference in line 3 of README.md:
    If README.md references CONTEXT.md as @CONTEXT.md (it does not currently — check first), update to point to Config.json instead.

    Keep intact: architecture section, Структура section, all bash commands, all other content.
    Do NOT remove any bash commands or directory structure from README.md.
  </action>
  <verify>
    <automated>grep -n "wake word\|max_tokens\|temperature\|num_ctx\|Config.json" /home/i17jet/Agents/Adam-Chip/README.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep for "wake word" in Inference Stack table context returns 0 matches (moved out of table)
    - grep for "Config.json" in README.md returns at least 1 match (the new pointer line)
    - Inference Stack table still has all 6 rows (LLM, VLM, ASR, TTS, Orchestrator, Log Viewer)
    - All ports (8080, 8081, 8082, 8083, 8084, 8095) still appear in the table
    - ## Быстрый Запуск Orchestrator section unchanged
    - ## Диагностика section unchanged
  </acceptance_criteria>
  <done>README.md Inference Stack упрощён: модели без числовых параметров, ссылка на Config.json добавлена. Все команды и структура нетронуты.</done>
</task>

<task type="auto">
  <name>Task 3: Clean CLAUDE.md — verify and remove any numeric params from Gotchas</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file — check each Gotcha bullet for numeric wake word params)
    - .planning/phases/01-doc-refactor-c-a/01-CONTEXT.md (D-03: what stays, what goes)
  </read_first>
  <action>
    Read CLAUDE.md in full. Check every line in ## Gotchas (non-obvious) and ## Non-obvious invariants sections for any numeric values that belong in Config.json rather than documentation.

    Per D-03, remove: specific numeric values for wake word (threshold=X, debounce=N).
    Keep: all conceptual gotchas, all invariants, all non-numeric warnings.

    Specifically check and act on:
    - If CLAUDE.md contains "threshold=0.35" or "threshold=0.20" or "debounce=3" or "debounce=2" → remove those specific numbers from the text, keeping the conceptual warning
    - The current Invariant #6 "Wake word «адам»" — keep as-is, it has no numbers
    - "hw:1,0 (Jetson APE I2S) физически не работает" — keep as-is, this is a hardware gotcha not a parameter
    - "порт 10808" in curl+v2ray gotcha — keep, this is infrastructure fact not a tunable parameter

    Also update the first line (@CONTEXT.md reference):
    Current line 3: "See @README.md for project overview and @CONTEXT.md for full system state (inference stack, endpoints, modules, Config.json snapshot)."
    Change to: "See @README.md for project overview and @System/Config.json + @System/Config.schema.json for runtime parameters."

    If CLAUDE.md currently has NO numeric threshold/debounce values in Gotchas (which is likely based on reading it), then the only required change is the @CONTEXT.md reference in line 3.
  </action>
  <verify>
    <automated>grep -n "threshold=\|debounce=\|CONTEXT\.md" /home/i17jet/Agents/Adam-Chip/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - grep for "threshold=" in CLAUDE.md returns 0 matches
    - grep for "debounce=" in CLAUDE.md returns 0 matches
    - grep for "CONTEXT.md" in CLAUDE.md returns 0 matches (reference updated)
    - grep for "Config.json" in CLAUDE.md returns at least 1 match
    - All 6 Non-obvious invariants still present (LLM=text, action failure, Jetson-only, power gate, half_duplex_mute, wake word «адам»)
    - All Gotchas bullets still present (Gemma thinking, SWA cache, curl proxy, Silero, WhisperX, LLM model ID, audio devices, ESP32 IP)
    - grep for "half_duplex_mute" in CLAUDE.md returns match
    - grep for "nvpmodel" in CLAUDE.md returns match
  </acceptance_criteria>
  <done>CLAUDE.md обновлён: ссылка на CONTEXT.md заменена, числовые параметры конфига (если были) удалены. Все инварианты и gotchas сохранены.</done>
</task>

</tasks>

<verification>
```bash
# Verify pointer in CONTEXT.md
echo "=== CONTEXT.md ===" && wc -l /home/i17jet/Agents/Adam-Chip/CONTEXT.md
grep "Config.json" /home/i17jet/Agents/Adam-Chip/CONTEXT.md

# Verify README.md cleanup
echo "=== README.md ===" && grep -n "wake word.*adам\|max_tokens\|temperature.*=\|num_ctx\|Config.json" /home/i17jet/Agents/Adam-Chip/README.md

# Verify CLAUDE.md cleanup
echo "=== CLAUDE.md ===" && grep -n "CONTEXT.md\|threshold=\|debounce=" /home/i17jet/Agents/Adam-Chip/CLAUDE.md

# All invariants still present
grep -c "half_duplex_mute\|nvpmodel\|wake word" /home/i17jet/Agents/Adam-Chip/CLAUDE.md
```
</verification>

<success_criteria>
- CONTEXT.md: 9 lines or fewer, contains "Config.json" pointer, no "Inference Stack" content
- README.md: Inference Stack table clean (no "wake word" in table cells), "Config.json" pointer added after table
- CLAUDE.md: no "CONTEXT.md" reference, no threshold=/debounce= numerics, all 6 invariants intact
</success_criteria>

<output>
After completion, create `/home/i17jet/Agents/Adam-Chip/.planning/phases/01-doc-refactor-c-a/01-03-SUMMARY.md` with:
- Files modified: CONTEXT.md, README.md, CLAUDE.md
- Summary of changes per file
- Verification grep results
</output>
