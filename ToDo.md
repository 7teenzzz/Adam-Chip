# Пересобрка UI

- Регруппировка параметров:
  - Анализ на предмет кооректного логического разделения/группировки параметров систем и всех их модулей:
    - ESP (cam, mic, PCA9685, PCM5102A, RIP sensor, light sensor);
    - Agent (asr, vlm, llm, tts);
    - Adam .
- Страница "Статусы" должна иметь блоки с подробным состоянием каждого модуля и нод пайплайна.
- Сделать анимированный таймер в виде тонкой горизонтальной зеленой полосы отсчета под статусами голосового цикла для каждого этапа, где это необходимо.

# Рефакторинг

- Пересмотр структуры директорий и файлов.